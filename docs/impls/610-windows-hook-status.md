# 610 — Hook-based session status on Windows — implementation plan

Plan for feature [610](../features/610-windows-hook-status.md) ([#610](https://github.com/yicheng47/runner/issues/610)). The spec says *what*; this file carries the Windows hook contract measured on JASONPC, the mission sequence, and what has landed. The macOS adapters, their decisions and their log are the archived [347 program record](./archive/347-hook-status/README.md); nothing there changes.

## Status (2026-09-16)

Probed on JASONPC (Windows 11 Pro 10.0.26200, Claude Code 2.1.273, Codex 0.153.4, GitHub Copilot CLI 1.0.85, PowerShell 7.6.5, Windows PowerShell 5.1, Git for Windows). No product code has changed. Jason installed Copilot CLI through winget and signed in the same evening, so its hook forms were probed live as well. All three runtimes are ready to brief as one mission.

## What JASONPC measured

The three CLIs do not share a Windows hook shell, so there is no single reporter to port. Each adapter keeps composing its own hook command, as it does on macOS, and gains a Windows form in the shell its CLI actually uses.

| Runtime | How the CLI runs a `command` hook on Windows | What that means for Runner |
| --- | --- | --- |
| Claude Code 2.1.273 | Git for Windows bash (`$0` = `/usr/bin/bash`, `uname` = MINGW64), the shell its own Bash tool requires. The JSON payload arrives on stdin; `cat` and `uname` resolved from `/usr/bin`. Probe: `claude -p … --settings '{"hooks":{"SessionStart":[…]}}'`, the same shape the rekey hook already uses. | The existing `sh` reporter (`APPEND_SCRIPT`) works unchanged **when the feed path uses forward slashes**. Run under Git Bash with the script extracted from `claude_status.rs`: a `C:/…/feed.ndjson` feed produced `{"generation":"gen-1","hook_event_name":"Stop","payload_file":"feed.ndjson.NSCNCNzG"}` and the payload file beside it; a `C:\…\feed2.ndjson` feed produced a `payload_file` holding the whole absolute path, because `${payload##*/}` strips only `/`, and `read_report` rejects any `payload_file` containing a separator. The full `hook_command` wrapper (`(sh '…' '…' 'PreToolUse' \|\| cat >/dev/null) 2>/dev/null; exit 0`) also ran correctly. `mktemp "C:/…/feed.ndjson.XXXXXXXX"` works under MSYS. |
| Codex 0.153.4 | `pwsh -NoProfile -Command <command>` (`[Environment]::GetCommandLineArgs()` inside the hook showed pwsh 7's `pwsh.dll`, `-NoProfile`, `-Command`, the body). `%COMSPEC%` and `$0` were not expanded, so neither cmd nor bash is involved. Stdin is a redirected pipe carrying one JSON object (447 bytes for SessionStart) that closes after the payload: `[Console]::OpenStandardInput().CopyToAsync(…)` reached EOF with the payload intact. `Get-ExecutionPolicy` inside the hook is RemoteSigned. `[Console]::InputEncoding` is gb2312 on this PC. Probe: `codex exec --skip-git-repo-check -s read-only --enable hooks --dangerously-bypass-hook-trust -c 'hooks.SessionStart=[{hooks=[{type="command",command="…",timeout=8}]}]' … </dev/null`. | The Codex reporter must be PowerShell. Raw bytes through `[Console]::OpenStandardInput()` into a `MemoryStream` preserve the payload; a local pipe test with UTF-8 Chinese in the JSON came back byte-for-byte. A text-mode read (`$input`, `Read-Host`, `Get-Content`) would decode through gb2312 and corrupt any non-ASCII prompt. `hooks` is a stable feature in 0.153.4 and both `--enable hooks` and `--dangerously-bypass-hook-trust` are accepted, so the macOS injection flags carry over unchanged. The docs' `command_windows` override is not needed: Runner composes argv per platform. |
| GitHub Copilot CLI 1.0.85 | `pwsh -nop -nol -c <command>` for a `command` entry and for a `powershell` entry alike; an `exec: "pwsh"` entry runs pwsh with its `args` verbatim (`-NoProfile -Command …`). All three forms ran from three `--plugin-dir` plugins mounted together on one headless session, each received `RUNNER_COPILOT_STATUS_PATH` from the parent environment and a closing stdin pipe carrying the SessionStart payload (344 bytes: `hook_event_name`, `session_id`, `timestamp`, `cwd`, `source`, `initial_prompt`). Same pwsh 7.6.5, RemoteSigned policy and gb2312 input encoding as Codex. Probe: `copilot -p … -s --allow-all-tools --plugin-dir … </dev/null` from a temp cwd, plugins in Runner's own shape (`plugin.json`, `hooks/hooks.json` with `version: 1`, capitalised events, nested `hooks`). The [hooks reference](https://docs.github.com/en/copilot/reference/hooks-reference) documents `bash`, `powershell`, the cross-platform `command` copied into both slots when they are absent, `exec` + `args`, and `timeoutSec` with `timeout` as its alias. | Today's plugin entry (`sh '<app data>/copilot-hooks/report.sh' "$RUNNER_COPILOT_STATUS_PATH" <event>` under `command`) is handed to PowerShell on Windows and fails. The fix is one extra key per event: keep `command` (the `sh` reporter, which macOS keeps using) and add `powershell` with the shared PowerShell body; Copilot picks the slot by OS, so `install_plugin` writes the same file on both platforms and macOS behaviour is unchanged. `exec` works too but buys nothing over `powershell` here. 1.0.85 is two patches past the 1.0.83 the adapter's event mapping was verified on; the Windows smoke doubles as that regression check. |

Startup cost on JASONPC, against the 2 s hook timeout: pwsh 7 ≈ 200 ms, Windows PowerShell 5.1 ≈ 120 ms, Git `sh` ≈ 30 ms.

Facts the missions lean on:

- **Feed file sharing.** Rust's `File::open` on Windows shares read, write and delete, so a hook appending while Runner holds the feed open is fine. The PowerShell append must open the feed with `FileShare` `ReadWrite | Delete` and write its record as one byte write, so a late hook never blocks `StatusFiles::drop` and cleanup never blocks a hook. Anything cleanup cannot remove is swept by `clear_leftovers` at the next launch, as today.
- **Watching a file, not a directory.** `HookFeed` watches the feed path itself through `notify`. Whether ReadDirectoryChangesW delivers events for a file path is a mission-1 test; `drain`'s one-second re-read covers it either way, so the worst case is a second of latency, not a lost record.
- **Git's `sh` is Claude's problem, not Runner's.** Claude resolves Git Bash the way it does for its Bash tool (Git for Windows is its install prerequisite); Runner injects the same `sh` command it injects on macOS. `C:\Program Files\Git\usr\bin\sh.exe` happens to be on JASONPC's native PATH and is present on GitHub's `windows-latest`, which is what the Windows reporter test uses; it skips with a message when absent.
- **Copilot's Windows transcript** is `%USERPROFILE%\.copilot\session-state\<session>\events.jsonl` (seen for the probe session, `copilotVersion` 1.0.85 in its `session.start` record), the path `CopilotStatusWatcher` already derives through `copilot_home`, so the transcript-correlated cancellation path needs no Windows branch.
- **Version skew.** macOS verified Claude Code 2.1.270, Codex 0.154.0 and Copilot 1.0.83; JASONPC has 2.1.273, 0.153.4 and 1.0.85. The Codex hook flags and events are identical across those two Codex versions; the smoke records name the versions they ran on. One 1.0.85 change to watch: its `~/.copilot/config.json` is headed "User settings belong in settings.json. This file is managed automatically." and holds only login state, while `copilot_trust` seeds `trustedFolders` into that same `config.json`; if the first Copilot spawn on JASONPC shows the folder-trust dialog, the seed has moved and that is a 540 follow-up, not a hook problem.
- **Codex exec probes need `</dev/null`.** With an open stdin `codex exec` prints `Reading additional input from stdin...` and waits forever; two probes hung that way before the third closed stdin.

## Decisions that bind

1. **Per-runtime gate.** `hook_feed::hooks_supported(windows)` becomes `hooks_supported(runtime, windows)` and `inject_codex_hooks` reads it, so each bridge is lifted the day its JASONPC smoke passes and re-gated alone if it regresses. After mission 1 all three runtimes return true on both platforms; a runtime whose smoke fails is re-gated alone before merge, and the others still ship.
2. **No new binary.** Each adapter's hook command gets its Windows form in the shell its CLI already uses: Claude keeps the `sh` reporter under Git Bash; Codex and Copilot share one PowerShell reporter body. A Runner-owned native reporter executable was considered and rejected: PowerShell does not hand its stdin to a native child without piping, so PowerShell would still have to read the bytes, and the executable would add a sidecar to package, install and upgrade for no transport gain.
3. **The PowerShell reporter is inline, bytes only, and 5.1-compatible.** The body travels inside the hook command rather than as a `.ps1` file, so it runs under any execution policy and leaves nothing for `clear_leftovers`. It copies stdin as raw bytes to `<feed>.<8 hex chars>`, then appends one UTF-8 record line (`generation`, `hook_event_name`, `payload_file`, the shape `read_report` already parses) through `[IO.File]::Open` in Append mode; never `Add-Content`, never `$input`, never text mode, and no double-quote character in the body: PowerShell strings are single-quoted (a `'` inside a path doubles to `''`), the record's JSON quotes come from `[char]34`, and its newline from `[char]10`, so the TOML, JSON and argv layers never have to escape anything. No pwsh-7-only syntax, so a Codex fallback to Windows PowerShell 5.1 keeps working; Copilot requires pwsh 7 regardless. It always exits 0; the Codex form ends with `'{}'` so the CLI gets its acknowledgement. The generation comes from the runtime's env var as today; the feed path is inline for Claude and Codex (per-spawn commands) and from `RUNNER_COPILOT_STATUS_PATH` for Copilot (static plugin). With no reporter file, the Windows `HookFeed` treats the feed as its reporter path, so removing the feed still releases the latch and the bridge-loss tests keep their meaning.
4. **Forward-slash feed paths on Windows.** Every status feed path handed to a hook command or a `RUNNER_*_STATUS_PATH` env var uses `/` separators. Rust, PowerShell and MSYS accept both; Git Bash needs `/` for the basename strip. The rekey drop path keeps its backslashes: it works today through `cat` and `mv` and is not touched.
5. **macOS byte-for-byte unchanged, same vocabulary, no new surfaces.** Every macOS command string, plugin file and test keeps passing; Windows forms are `cfg!(windows)` branches in the functions that already compose the commands. Phase 2 details and the phase-3 heuristic removal follow the gate lifts, never ride with them.
6. **A missing shell is the designed fallback.** If Claude cannot find bash, or pwsh is absent for Copilot, the hook fails, no record is written, and the baseline never latches off. Status stays estimated, which is exactly what the spec's `disableAllHooks` case already proves; Runner adds no detection of its own for this.

## Missions

| Mission | Scope | Crew | State |
| --- | --- | --- | --- |
| 1 | Windows bridges for all three runtimes: per-runtime gate, forward-slash feed paths, Claude's `sh` command under Git Bash, the PowerShell reporter inline for Codex and as the plugin's `powershell` entry for Copilot, Windows watcher and reporter tests, docs, three smoke records. All gates lifted; Jason's JASONPC smoke per runtime before merge, and a runtime that fails its smoke is re-gated alone. | codex peer on JASONPC, brief under `archive/gpui-rewrite/briefs/610-m1-windows-hook-status.md` | Ready to brief |
| 2 | Phase 2, the deferred details one at a time, canvas first: `Working · Compacting context` and `Using tools`, sidebar attention for response failures, the interruption outcome in the Idle tooltip, elapsed time on a wait, the nonblocking-ask `Still working` case. | per item; inline where the diff is smaller than a brief | Not started |
| 3 | Phase 3: drop title-spinner classification for Claude Code and Codex on both platforms once mission 1 is merged; byte activity stays the baseline for TRAE, shells and bridge loss. | inline | Not started |

## Mission 1 — Claude Code, Codex and Copilot on Windows

What it touches:

- `session/hook_feed.rs`: the per-runtime gate; a Windows path-string helper (forward slashes) used by every command and env composition; the PowerShell reporter body builder beside `hook_command`; a Windows `HookFeed` constructor for reporter-less runtimes (decision 3). `clear_leftovers` gains nothing unless a mission decision adds a file type.
- `session/codex_status.rs`: `hook_command` returns the PowerShell form on Windows, ending in `'{}'`.
- `session/claude_status.rs`: the gate re-export and tests only; `APPEND_SCRIPT` is unchanged.
- `session/copilot_status.rs`: `install_plugin` adds a `powershell` key beside the existing `command` in every event entry, same Notification matcher, the body reading the feed from `$env:RUNNER_COPILOT_STATUS_PATH` and the generation from `$env:RUNNER_COPILOT_STATUS_GENERATION`; the file is identical on both platforms because Copilot picks the slot by OS. `report.sh` is still written everywhere, so `start_external`'s reporter check keeps working; if the crew wants bridge loss on Windows to mean "hooks.json gone" rather than "report.sh gone", the Windows reporter path is `hooks/hooks.json`.
- `router/runtime.rs`: `inject_codex_hooks`, `codex_status_args`, `claude_settings_args` and `copilot_status_args` use the per-runtime gate; `toml_edit::Value::from` serializes the PowerShell body (it contains `'`, `$`, `[`, `|`, `;`), and the body's lack of double quotes keeps the argv layer simple. `claude_settings_args` carries the `sh` command as today, with the forward-slash feed path.
- `session/manager/spawn.rs`, `session/pty_runtime.rs`, `session/manager/mod.rs`: gate call sites and env values.
- Tests: the Windows-empty assertions flip to per-runtime; new Windows-native reporter tests run the PowerShell body through `pwsh -NoProfile -Command` (and Windows PowerShell 5.1 where installed) with piped stdin covering pretty and large JSON, UTF-8 non-ASCII, feed paths with spaces and apostrophes, a missing feed directory (exit 0, nothing written), and teardown; the Claude `sh` reporter runs through Git's `sh` when present; the real PTY bridge tests gated `#[cfg(unix)]` get Windows counterparts or lose the gate where they are shell-agnostic; one test appends to a watched feed from another process to prove the single-file `notify` watch on Windows.
- Docs, in the same PR: `docs/arch/arch.md` §5.3 (the `--plugin-dir` sentence) and §5.10 (the Windows-agents sentence); `docs/arch/windows.md` first paragraph; `README.md` and `README.zh-CN.md` lose footnote ³ (hook status on both platforms for all three) and the 540 spec's Windows footnote closes; `docs/tests/610-claude-hooks-windows-smoke.md`, `docs/tests/610-codex-hooks-windows-smoke.md` and `docs/tests/610-copilot-hooks-windows-smoke.md` in the shape of the [Codex](../tests/347-codex-hooks-smoke.md) and [Copilot](../tests/540-copilot-hooks-smoke.md) records; this plan's status and log.

Verification by the crew, on JASONPC: `cargo test --locked -p runner-backend --profile ci`, then the three Windows checks from [Windows development](../arch/windows.md#validation) on the final diff; both CI jobs green on the PR. The coder does not launch Runner; live checks that a reviewer makes coder-blocking go to the session through `target\debug\runner-mcp.exe`.

Jason's JASONPC smoke, per runtime, before merge:

1. A prompt shows Working with no estimated tooltip and Idle on completion, in a direct chat and a mission slot; stop and resume keep the conversation and the next turn still moves Working → Idle.
2. Claude: a command approval dialog shows Approval needed while it is visible and clears on the decision; AskUserQuestion shows Answer needed as it opens, an answer returns Working, Escape returns Idle without a completion dot; `/clear` still rekeys the pane.
3. Codex: Working through quiet tool work, Idle on Stop, Escape or Ctrl+C reaches Idle after the rollout's `turn_aborted`; no Approval needed or Answer needed appears, as on macOS.
4. Copilot: `echo` auto-approves with no Approval needed; an Edit in Default mode shows Approval needed while its dialog is open and clears on Enter; `ask_user` shows Answer needed as it opens and Escape ends Idle/Interrupted without a completion dot; a disposable repository with `.github/copilot/settings.local.json` `{"disableAllHooks":true}` stays estimated; a role's own `--plugin-dir` composes with Runner's.
5. The feed and payload files appear under `%APPDATA%\com.wycstudios.runner-dev\session-status\` while the session runs and are gone after it stops; a role with its own `--settings`, `-c hooks.…`, `-c features.hooks=false` or `--disable hooks` shows estimated status and creates no feed.
6. In a crew, a message posted during a Claude or Copilot dialog is held until the dialog resolves; posted while Working or Idle it delivers; a typed draft still protects the input.
7. The [606](../features/archive/606-rail-glyph-liveness.md) rail check: both themes, a project with a running session and one without, a shell tab, a mission row, a provider chat, a collapsed and an expanded project, a dragged row's accent drop indicator.

## Phases 2 and 3

Phase 2 items are designed on `design/runner.pen` before implementation, each additive to the shipped header, row and card layout, and each lands alone. Phase 3 removes `classify_title` only for Claude Code and Codex, the two runtimes whose title animation it was built on, after both platforms have validated hook coverage; the fixtures `codex-title-working.ndjson` and `claude-session.ndjson` then prove the byte baseline alone, and TRAE, shells and bridge loss keep byte activity.

## Log

- 2026-09-16 — Probed the Windows hook contract on JASONPC from the planning session, no product code touched: Claude Code 2.1.273 runs `--settings` hooks under Git Bash and the existing `sh` reporter needs only forward-slash feed paths; Codex 0.153.4 runs hooks as `pwsh -NoProfile -Command` with a closing stdin pipe, RemoteSigned policy and a gb2312 console input encoding, so its reporter is inline PowerShell reading raw bytes; Copilot was not installed at first, its docs put Windows hooks under PowerShell with `exec` as the shell-free alternative. Wrote this plan with Copilot as a second mission.
- 2026-09-16, later — Jason installed Copilot CLI 1.0.85 and signed in. Three scratch plugins mounted together on one headless `copilot -p` run proved `command`, `powershell` and `exec` entries all execute under pwsh 7 with the parent env and the stdin payload. Folded Copilot into mission 1: the plugin gains a `powershell` key per event and the mission covers all three runtimes.
