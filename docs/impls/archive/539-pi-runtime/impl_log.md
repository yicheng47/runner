# pi runtime — impl log

Dated log for [#539](https://github.com/yicheng47/runner/issues/539); oldest entry first. [README](README.md) has the condensed state, [plan.md](plan.md) the mission table.

- 2026-09-17 — spec refreshed from a live probe of pi 0.85.1: `--session-id` accepts a Runner UUID, `--append-system-prompt` takes a file or inline text and is composed at startup rather than persisted (codename test: a resumed session without the flag does not know it, with the flag it does), `--fork` takes a new `--session-id`, `--thinking` is case-sensitive, `--` protects a leading dash but not a leading `@`, extension events recorded for one turn, `project_trust` reaches a `-e` extension before the trust dialog and the dialog emits no `ui_prompt_start`, terminal modes and `--list-models` output recorded, the session slug read from `dist/core/session-manager.js`. Orca's and herdr's pi extensions read as prior art. A first pass of the spec claimed trust was invisible to extensions; that was a probe script that never passed its environment to the child, corrected the same day. Jason moved the system-prompt channel from a follow-up phase into mission 1 and widened it from the persona alone to all three prompt layers.
- 2026-09-18 — mission 0: the spec frame drawn from the 540 frame's shape, with the mark three ways beside the four shipped marks, the rail, the Agents row, trust as decision 6 and the tint options live and stopped on both themes. Jason signed off decision 6, noted that mission agents bypass everything by default (already true through #527), and chose the theme-foreground silhouette (decision 11). The frame moved into `design/specs/539-pi-runtime.pen`, the first spec in its own file: `runner.pen` had 152 top-level frames and a 32,000 px spec row, and changed in 43 of its last 44 commits. `cmp/MarkPi` stays on `runner.pen`; the three-colour component lives only in the spec file.
- 2026-09-18 — mission 1 (codex peer, `01M2S4DF48DH2T66QY0TG9PXAD`): phase 1 implemented in one sitting, three review rounds. Round 1 found four must-fixes worth recording: a restart deleted the prompt file it had just written (the forwarder's cleanup ran after the rewrite; now the file is written after the kill and removed before the exit status lands); the spec's Windows slug example was wrong (pi maps each separator on its own, so `C:\Users\x` is `--C--Users-x--`); launch-time resume refused an untouched pi worker, since pi writes its session file only after the first assistant message (pi is now exempt when its kept key has no file); and the lead split searched the composed text for section headers, which user text could contain (now composed from the inputs as sections). Real-binary evidence: persona in force without a first turn, an edited persona applied on resume, `--approve` suppressing the trust dialog in this repository, the goal delivered exactly once, `--list-models` listing 26 credentialed rows only. Follow-ups for mission 3: `split_session_prompt` keeps an unreachable pi lead arm, the fast resume-failure heuristic clears pi keys on auth or model errors, and forking an untouched pi chat fails inside pi until a source file exists.
- 2026-09-18 — mission 2 (codex peer, `01M2SGW74SGGMFASKK0PN99F5D`): phase 2 implemented in one sitting, one review round. Two must-fixes: the first `session_start` published Idle, which recorded a spurious completion on a pi lead the moment it launched (now, like the three sibling adapters, nothing is published until the hook owns the session, while `new`, `resume` and `fork` still publish Idle with no outcome); and the rekey guard compared against `RUNNER_PI_SESSION_KEY`, so `/new` then `/resume` back to the launch conversation left the row on the wrong key (decision 10 corrected: compare against the last id reported, seeded from the key). Non-blocking fixes taken: the feed append opens existing-only so a late write cannot recreate a feed teardown removed, and a dead `errorMessage` field went. Real-binary evidence through a PTY: Working to Idle only at `agent_settled`, Using tools, a manual compaction span, Escape as Interrupted, a bad credential as Response failed, a `ctx.ui.confirm` as Approval needed, `/new` writing the drop file with the new id, a nested `pi -p` writing nothing; a `/compact` on a short session emitted only `session_compact_failed`, which the watcher ignores. Known limitation: a pi from before the npm scope rename (`@mariozechner/pi-coding-agent`) cannot resolve the version import and is treated as current. Smoke record `docs/tests/539-pi-hooks-smoke.md`; the Windows run is mission 3.
