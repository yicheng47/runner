# 540 — Mission 1: GitHub Copilot CLI as a runtime, adapter and surfaces

Jason requested a Codex crew mission on 2026-09-16. Work in `/Users/jason/repos/yicheng47/runner` on the existing branch `feat/540-copilot-runtime`, which already carries this brief and the impl plan. Read `AGENTS.md`, `docs/features/archive/540-copilot-cli-runtime.md` (its "What a new runtime touches" tables are this mission's checklist and its "Probe evidence" section is the ground truth for every flag, path and file shape), `docs/impls/540-copilot-cli-runtime.md`, and `docs/impls/archive/0036-trae-runtime.md` for the last time a runtime was added. The TRAE plan's capture-thread parts do not apply here: Copilot keys are caller-assigned like claude-code.

## Ownership and authorization

The coder owns implementation and checks; the reviewer waits for an explicit Runner handoff, then audits the whole working-tree diff. Iterate through Runner until no must-fix findings remain. No additional crew, nested subagents, new checkout, or worktree. Stop at a clean working-tree review and give Jason a focused smoke checklist. Commits, push, PR, merge and restarting the development app are not authorized; the branch's two docs commits are the only commits that exist and stay as they are.

`copilot` 1.0.83 is installed at `/Users/jason/.local/bin/copilot` and logged in. Probes that spend Copilot credits are allowed but keep them minimal: `copilot -p '…' --allow-all-tools -s --no-auto-update`. Never write into `~/.copilot` by hand; exercise the code under test against a throwaway `COPILOT_HOME` and unit-test fixtures. Do not delete anything under `~/.copilot/session-state`. The trust dialog shows for any cwd not in `config.json` `trustedFolders`; answer "1. Yes" (session only) if a manual probe hits it.

## Deliverable

Phases 1 and 2 of the spec: the runtime lands end to end on macOS and Windows without hook status. Every row of the spec's five inventory tables except the hook adapter rows (`copilot_status.rs`, `HookStatusWatcher::Copilot`, the `--plugin-dir` env injection) and the terminal fixture, which Jason records during smoke. In detail:

- `Runtime::Copilot`, wire name `copilot`, in `model.rs`; `RUNTIME_DEFINITIONS` entry (display `GitHub Copilot CLI`, command `copilot`, `native_fork: false`, `skills_dirs: [".copilot/skills", ".agents/skills"]`), appended last.
- Argv adapter in `router/runtime.rs`: `--model`, `--effort` (seven levels, verbatim); first turn on `["-i", body]`, empty on resume; empty `system_prompt_args`; permission Default → nothing, AcceptEdits → `--allow-tool=write`, Bypass → `--yolo`, Auto → nothing and not offered; strip set `--allow-tool` (both `=` and split forms), `--yolo`, `--allow-all`, `--allow-all-tools`, `--allow-all-paths`, `--allow-all-urls`; mission mode Bypass → `--yolo`; `--add-dir <mission dir>`; `--no-auto-update` on every spawn; `resume_plan` pre-assigns a UUID and passes `--session-id <uuid>` for fresh and resume alike, with `assigned_key` set so the row's `agent_session_key` is persisted before the process starts; `copilot_conversation_exists` checks `$COPILOT_HOME`-or-`~/.copilot` `session-state/<key>/events.jsonl`; no fork.
- Spawn path: first-turn warning gate and Windows batch fallback list include Copilot; the conversation-missing check gains the `(Copilot, Some(key))` arm; `sessions_root_for` returns `None`; a new `session/copilot_trust.rs` seeds the spawn cwd into `config.json` `trustedFolders` before every spawn, mirroring `codex_trust`, keeping the file's leading `//` comment lines byte-for-byte, parsing the remainder with `preserve_order`, touching only that key, creating the file when absent. `--yolo` does not skip the trust dialog, so this is what keeps unattended mission slots alive.
- `runtime_defaults.rs`: `~/.copilot/settings.json` → `model` and `effortLevel`, tolerant of `//` comments.
- `ops/runtime.rs` catalog: description, `default_enabled: true` on both platforms, efforts `none / minimal / low / medium / high / xhigh / max`, models = `auto` first then the ids `copilot help config` prints on the installed binary (take the list from the binary, not from memory). `DISCOVERY_RUNTIMES` unchanged.
- MCP: `McpClientId::Copilot` → `~/.copilot/mcp-config.json`, the Claude JSON writer under `mcpServers` with entry `{"type":"local","command":<runner-mcp>,"args":[],"tools":["*"]}`, file created when absent, other servers and formatting preserved; `mcp_defaults.rs`, the Settings → MCP copy hints and tests, the `cli_install.rs` doc comment.
- Skills: nothing beyond `skills_dirs`; the global toggle error text says Claude Code and Codex only.
- Title: `session/title.rs` `decoration` gains `"github copilot"` and `"copilot"`; tests for `GitHub Copilot`, `<prompt> - GitHub Copilot`, `Run Shell Command Echo - GitHub Copilot`.
- Identity: `assets.rs` `COPILOT` from lobe-icons `githubcopilot.svg` at commit `a94750e3f5f8fc33757b839d85030e742284e43a` (MIT, LobeHub line already in the licence block), `ASSETS` row `copilot.svg`; `chat_icon.rs` `Some(Runtime::Copilot) => ("copilot.svg", gpui::rgb(0x8534f3).into())`, fixed in both themes; every icon test table gains the row. Check the mark at 12 px beside the others in the running app; if it looks heavy, change the file's viewBox to `-2 -2 28 28` like the other three.
- Runner form: `permission_modes` → `[Default, AcceptEdits, Bypass]`; `permission_mode_description` copy verbatim from the spec frame's section D: Default "Copilot's own manual mode: read-only tools run, writes and shell commands ask. Governed by defaultPermissionMode in ~/.copilot/settings.json."; Accept edits "File creates and edits run without asking; shell commands, URLs and paths outside the cwd still prompt."; Bypass "Every tool, path and URL is allowed. Same flag for the app-wide mission permission mode; chats never carry it (#596)."
- Every runtime-enumerating test and error string: `router/runtime.rs` loops and permission matrix, `ops/runner.rs` create/update baking, `ops/slot.rs` valid-runtime list, `mcp/tools/session.rs` doc comment, `runtime_status.rs`, `ops/mcp.rs`, `chat_icon.rs`, `command_palette.rs`, `settings/archived.rs`, `panes.rs`, `sidebar/tests.rs`, `ui/select.rs`, `app_settings.rs`, `skills.rs`.
- Docs: `README.md` and `README.zh-CN.md` together (supported-agents row "GitHub Copilot CLI | Supported | Supported" with a subscription note, the MCP registration sentence, the hook-status sentence saying Copilot uses estimated status until its adapter lands); `docs/arch/arch.md` enumerations at §1.1, §3, §4, §5 and §5.10 plus the `--settings` sentence; `docs/arch/windows.md`.

Out of this mission: the hook status adapter (mission 2), the terminal fixture, model discovery, the persona on a native channel, global skill toggle, fork, and Windows hooks. Do not pre-build any of them.

## Verification

`make verify` green, plus `cargo test --locked -p runner-backend -p runner-app --no-fail-fast` and `cargo clippy --locked --workspace --all-targets --profile ci -- -D warnings`. Cover: `runtime_list` includes `copilot`; `model = gpt-5.4`, `effort = high` → `--model gpt-5.4 --effort high`; a fresh spawn's argv ends `--session-id <uuid> --no-auto-update -i <body>` after the runner's own args and the key is persisted before spawn; resume carries `--session-id <uuid>` and no `-i`; the missing-directory fallback; all three permission mappings and the strip set in both flag forms; a chat strips all six flags; the trust seed on a file with the comment header, on an absent file, and on a file that already lists the cwd; runtime defaults with and without comments; the MCP write on an absent file and beside another server; the three title shapes; every icon table.

Prove the spawn end to end once with the real binary: a direct chat and a mission slot start, the first turn arrives once through `-i`, the pane paints, and relaunch resumes by id. Report what you saw, not what the tests imply.

## Handoff

Final Runner handoff: branch, implemented behaviour and what stays unsupported, checks run with results, the exact manual smoke checklist for Jason (direct chat with persona, mission slot with `runner msg` round trip under Bypass in a never-trusted directory, resume after relaunch, Settings → Agents row and `mcp-config.json` registration, tab title after a generated topic, the mark at 12 px in both themes), and the reviewer's explicit no-remaining-must-fix verdict. Leave the implementation uncommitted for Jason to smoke-test.
