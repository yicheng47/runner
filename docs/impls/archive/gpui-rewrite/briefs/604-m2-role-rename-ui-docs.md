# 604 — Mission 2: rename the runner entity to role — UI copy, `surfaces/roles/`, docs

Jason requested a Codex crew mission on 2026-09-16. Work in `/Users/jason/repos/yicheng47/runner` on the existing branch `feat/604-m1-backend` (PR #618, the single PR for #604), whose tip is this brief's commit on top of mission 1 (`2c54b7c`). Build on it; do not create another branch, rebase or squash. Mission 1 renamed the data, backend and MCP layers; every identifier is already `role`. This mission changes the words users read and the directory name, nothing structural.

Read first: this brief; `docs/features/604-rename-runner-to-role.md` (binding: decisions 1–4, the UI copy table, Out of scope); `docs/impls/604-role-rename/README.md` §Decisions that bind (4 and 6 matter here: persisted values write new and read old; `runner_status` stays); the mission 1 brief beside this one for what keeps the name. Then the code: `crates/runner-app/src/surfaces/runners/` (10 files), `surfaces/start_chat.rs`, `surfaces/start_mission.rs`, `surfaces/crews/`, `surfaces/mission_workspace/{rail,view,state,mod}.rs`, `surfaces/panes.rs`, `ui/select.rs`, `ui/session_control.rs`, `keymap.rs`, `surfaces/command_palette.rs`, `surfaces/settings_page.rs`, `surfaces/settings/agents.rs`, and in `runner-backend` `ops/role.rs`, `ops/slot.rs`, `ops/mission.rs`, `router/runtime.rs`.

## Ownership and authorization

The coder owns implementation and checks; the reviewer waits for an explicit Runner handoff, then audits the whole working-tree diff against the branch tip at mission start. Iterate through Runner until no must-fix findings remain. No additional crew, nested subagents, new checkout, or worktree. Stop at a clean working-tree review. Commits, push, PR, merge and restarting the development app are not authorized; leave the work uncommitted on top of that tip. Do not touch `design/runner.pen`, the README screenshots, `docs/tests/`, `docs/impls/`, or `router/prompt.rs`.

## Deliverable

**The rule.** A rendered string that means the stored entity says *role*; a string that means the product (Runner the app), the crates, the bundled CLI and its verbs, the `runner_status` signal, `RUNNER_*`, the theme keys `runner` / `runner-light` / `runner-dark`, or a file name keeps *Runner*. The legacy readers from mission 1 (`"runners"` route path, `"runner-default"`, start-chat mode `runner`) keep their old spelling on the read side only. Where a literal swap reads circular or wrong, rewrite the sentence; list every rewrite in the handoff.

**Directory.** `git mv crates/runner-app/src/surfaces/runners crates/runner-app/src/surfaces/roles`; `mod runners` → `mod roles` and the `pub(crate) use roles::RoleSurfaces` re-export in `surfaces/mod.rs`. The diff must show the ten files as renames (`git diff -M --stat`), with copy edits inside them as the only content change.

**Start a chat** (`start_chat.rs`). Toggle `Direct | Role`; field label **Role**; `Role default (Claude Code)`; "Overriding runs this role on another agent…"; "No roles yet" / "No roles yet. Create one from the Roles page first." Two fixes from the spec: (1) the subtitle under "Start a chat" becomes "A one-on-one terminal with an agent, no mission required." (2) **Chat name** opens genuinely blank: the derived default (`@handle` for a role, the runtime's display name for Direct) becomes the field's *placeholder* instead of its text, `update_auto_title` moves the placeholder when the selection changes, and `user_chat_title` still returns `None` for an untouched field so the backend derives the same label as today. Cover the blank-submit and the type-then-clear paths in the existing tests.

**Everywhere else.** `start_mission.rs` ("Role default or home directory", "This crew has no roles…", "Each role starts here…", per-role args, "{n} role(s)"); the Roles surface (titles, tooltips, buttons, empty states, delete confirm, "Roles" breadcrumb, "Crews using this role", "slot override · blank inherits role default (…)", and `edit.rs:378` rewritten as "slot override · blank follows the role's agent; an explicit agent pins this slot's engine"); `crews/` (add-slot search and empty states, "+ Create new role…", "keeps the role's persona" → "keeps the role's prompt", "Uses the selected role's default prompt…", "Roles that work missions together", the list captions and "{n} roles" count, `logic.rs` "Role default (…)" and "Use the runtime configured on the role.", `slots.rs` "Runtime override — role default is …", "Edit role"); mission workspace (`rail.rs` section "Sessions", "No sessions yet.", tooltips "Open / Collapse sessions panel", `state.rs` "@handle to address one session", `mod.rs` `"role-default" => "role default"` with its test); `panes.rs` and `session_control.rs` "Back to role(s)"; `keymap.rs` "…chats, roles, and crews" and "…open session tabs"; `command_palette.rs` empty line; `settings_page.rs` "Whatever each role carries." and "the terminal canvas"; `settings/agents.rs` "roles with a custom command"; `ui/select.rs:731` "Role default". Backend strings shown to users: `ops/role.rs` ("role handle must…", "role not found: …", "role {id} has unarchived chats; archive them before deleting this role"), `ops/slot.rs` "role not found", `ops/mission.rs:510` "@{target} is not in the mission roster", `router/runtime.rs:604` "Role default". Windows: `platform_ui/windows.rs` carries only the product name; confirm nothing else there names the entity.

**Docs, all in this mission.** `AGENTS.md` §Product Context: "Users create reusable roles, compose them into crews, start missions, and interact with each session through a real PTY"; vocabulary bullets **Role**: a configured CLI agent runtime and system prompt, reusable across crews; **Crew**: a named set of slots, each filled by a role, with exactly one lead. `README.md` and `README.zh-CN.md` together, same hunks: the tagline "Spawn a runner. Create your crew. Ship the feature." → "Write a role. Create your crew. Ship the feature."; the Crew bullet; line 98 → "A **role** is a reusable agent configuration: runtime, system prompt, working directory. A **crew** composes roles into named slots…"; "PTY with a role"; in zh-CN the entity is **角色（role）** and the product stays Runner. `docs/product/vision.md`: "runner template" → "role"; a live participant is a *session* or *crew member*; keep `runner signal`, `runner_status` and "Remote runners / SSH". `docs/arch/arch.md`: the schema listing (`roles`, `slots.role_id`, `sessions.role_id`), the file-map row `roles/`, `roles.system_prompt`, "runner template" → "role", "Runner-backed chats keep `role_id`"; the coordination and prompt-stack sections keep the CLI and signal names. Tick phases 4 and 5 in the 604 spec.

## Verification

`make verify`; `cargo test --locked --workspace --no-fail-fast --profile ci` with the total unchanged from `2c54b7c` (1,330; report both numbers); `cargo clippy --locked --workspace --all-targets --profile ci -- -D warnings`; `cargo fmt --all -- --check`; `git diff --check`. Gate: `git grep -n -i -P '\brunners?\b' -- crates/runner-app/src crates/runner-backend/src` before and after, every survivor grouped under one of the meanings in the rule; `git grep -n -P 'surfaces/runners|runners::' -- crates` empty. Docs gate: `git grep -n -i -P '\brunners?\b' -- AGENTS.md README.md README.zh-CN.md docs/product docs/arch` with each survivor named as product, CLI, signal or hosts.

## Handoff

Final Runner handoff: branch and base; the rename proof for the directory; the copy table (file:line, old → new) with the rewrites called out; the doc hunks per file with the README pair shown as matching; the two gates with counts and grouped survivors; checks with results; the reviewer's explicit no-remaining-must-fix verdict. Leave the work uncommitted.
