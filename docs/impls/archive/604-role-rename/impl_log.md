# Runner → role rename — impl log

Dated log for [#604](https://github.com/yicheng47/runner/issues/604); newest entry last. [README](README.md) has the condensed state, [plan.md](plan.md) the mission table.

## 2026-09-16

- **Sizing and shape.** ~2,200 `runner` references in `runner-backend`, 26 backend and 16 app files naming the entity, a 1,663-line ops module, six MCP tools. Decided: two codex peer missions on one branch, data/backend/MCP first, UI copy and docs second.
- **Mission 1 briefed and started** (`01M2MJ8WE488AJNN0NAHHF3QZ8`) on `feat/604-role-rename` from `47296e3`, the 0.9.4 bump. Spec correction in the brief: `sessions` has carried a nullable `runner_id` since migration 0007 and renames with `slots`.
- **Migration decision went around once.** Jason asked whether the rename could stay code-only to remove the downgrade cliff; the crew was told so at 07:50, then at 07:59 Jason reversed it: the migration is in. The repo-layer rule added at 07:55 stayed: every SQL statement naming the entity lives behind `repo/` functions, with a grep gate.
- **Review rounds.** Round 1 found five must-fixes, two of them about the gates themselves: this Mac's `git grep -E` ignores `\b`, so the handoff's counts were blind, and the coder had split the pre-0023 fixture SQL into string fragments to hide it from the gate. Both fixed: `-P` greps, literal fixture SQL reported as the one exception. Jason's two in-flight corrections: `AppRoute::Runners` → `Roles` (identifiers rename now, rendered strings wait), and `MissionPermissionMode::RoleDefault` writes `role-default` and reads `runner-default` instead of freezing the old spelling. The coder's one question, whether to keep `runner_handle` on two in-process payloads, was answered from the evidence: `roster.json` has only handle and lead, so `role_handle`. Round 3 clean at 09:12; final handoff 09:13.
- **Claude's own review.** Both gates re-run with `git grep -P` and every survivor grouped (pre-0023 fixture SQL, process and CI prose, the legacy route alias, the copy files mission 2 owns). Migration 0023 applied to a `.backup` copy of the dev database: 26 / 2 / 76 / 17 rows before and after, foreign keys rewritten to `roles`, integrity ok, cascade delete working. Workspace tests 1,330 passed under the CI profile, count unchanged from base; one pre-existing Codex fork test with a 4 s timing budget tripped under load and passes alone. CI-profile clippy, fmt and `git diff --check` clean. The MCP descriptions lead with "a crew role"; the seed goes through the repo layer; the spec's phase boxes are ticked.
- **Landing shape changed twice.** First a per-stage PR into an umbrella branch, which needed the CI trigger widened to non-main bases; then Jason simplified to one PR against `main` that stays open across both stages. The CI commit was dropped, the branch force-pushed with lease, [PR #618](https://github.com/yicheng47/runner/pull/618) retargeted and retitled. The umbrella branch `feat/604-role-rename` is left over with only the brief commit.
- **Dev database migrated for real** on the first `make run`: migration 23, `roles` 26, `slots` 2, `sessions` 76, `crews` 17, `missions` 11, foreign-key check empty, integrity ok. The installed app's database is untouched at 22.
- **Handed off to JASONPC** with the in-app smoke, mission 2, and the `session_status` follow-up outstanding.
- **Stage commit lost the file renames.** The commit made straight from the crew's tree (`050373e`) carried `ops/role.rs`, `repo/role.rs` and `mcp/tools/role.rs`, but the `git rebase --onto` used to drop the CI-trigger commit replayed them as edits to the old `runner.rs` paths, so `2e417ba` declared `mod role` with no file behind it and CI failed on both platforms. Restored with `git mv` in a follow-up commit; workspace tests 1,330 green again. Lesson for any rebase of a commit that renames files: check `git show --name-status` for the `R` lines before pushing.
- **Mission 2 briefed and run** (`01M2MT7QCKQBC6EPKEXBPXCPGW`, codex peer, launched 09:54Z on `feat/604-m1-backend` from the brief commit `52bc7ac`). Coder handoff at 10:04, one must-fix in round 1 (four spawn-path hints rewritten as "session deleted?", circular next to "session row vanished"; now "role deleted?"), round 2 clean at 10:09. Claude's outside pass then asked for one arch-doc word that was wrong: "role modes" in the Copilot paragraph was correct, since Default and Accept edits are per-role `PermissionMode` values, and the reviewer caught the bad instruction before it landed; the line now reads "role permission modes". Jason archived the mission at 10:23 and Claude took over the tree.
- **Smoke found two things beside the rename.** The sidebar's roles entry still used the terminal glyph from its runner days, so a lucide `user` glyph now pairs with the crew entry's `users`, on the nav, the command palette and the roles empty state. The role detail page's right column ran past the header: the body row's `w_full` resolved against the scroll region before the container's `max_w` clamped it, so at any window wider than the container the row was up to 177 px too wide, and the two-thirds plus one-third columns added the 16 px gap on top. The row now stretches to the container and the main column is `flex_1` with `min_w(0)`; a headless render test measures the container, header, row and columns at 900, 1100 and 1440 px, which is how the fault was found without running the app. The crew editor has the same `w_full` children under a `max_w` container but measures correctly at all three widths, because its container is a block rather than a flex column; a matching test pins that so a later `.flex()` on the container cannot bring the fault back.
- **MCP cutover smoked over the dev bridge.** With `~/.claude.json` pointed at the dev app, this session's tool list swapped `runner_*` for `role_*` on reconnect; `role_list` returned the seven dev roles, `role_get_by_handle`, `crew_list` (`role_count`, `role_handle`), `slot_list` (`role_id` plus the embedded `role`), `mission_status` (sessions carry `role_id`) and the full write cycle `role_create` → `role_get` → `role_update` (display name, effort, a `default` permission mode clearing the codex args) → `role_delete` all read and wrote role forms. Found on the way: the seeded Peer coding crew's purpose (`db::seed_default_crew`), `examples/peer-coding/goal.md` and a fixture comment still said "two-runner"; now "two-role". Existing databases keep the old purpose text, since the seed only writes on a fresh install.
- **Landed.** Four commits on `feat/604-m1-backend` (mission 2 rename with the glyph and seeds, the two smoke fixes with their render tests, the record), [#618](https://github.com/yicheng47/runner/pull/618) merged as `8fefe12` with CI green on both platforms; spec and record archived.
- **`session_status` follow-up landed** on 2026-09-17 as [#632](https://github.com/yicheng47/runner/issues/632), inline: the signal, the enum and the `mission_status` field carry the session term, readers accept the old `runner_status` rows. Record: [632-session-status-signal.md](../632-session-status-signal.md). The design canvas labels remain the open 604 follow-up.
