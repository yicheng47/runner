# GPUI rewrite — program log

Progress record for the whole gpui-rewrite program ([README](README.md)), from the Phase 1 spike onward — the 0031-era log is folded in here. Newest entries at the bottom; keep entries short: what happened, what's next, blockers.

## Current state (update with each entry)

- **Branch**: `gpui-nightly` (M0+M1 merged via PR #407, M2 via PR #408, both 2026-08-17; M3.1 via PR #409, M3.2 via PR #410, M3.3 via PR #411, M3.4 via PR #414, crate renames via PR #415, docs sync via PR #416, M3.5 via the `feat/0046-m3-runtimes` PR, the M4 entry inventory as `2a1eb9f`, and M4.1 via the `feat/m4-1-app-shell` PR, all 2026-08-18; M4.2 via PR #420, M4.3's sidebar half via PR #421, M4.3's chat-surface half via PR #422, and the direct-chat working-directory validation fix via PR #423, all 2026-08-19; M4.4 via PR #425, M3.6 committed directly to `gpui-nightly` as `3341e9b`, M3.7 via local merge of `feat/m3-7-mission-feed`, M3.8 via local merge of `feat/m3-8-misc-backend`, M4.5 via local merge of `feat/m4-5a-mission-workspace`, and the resume/fork width chain via local merge of `fix/resume-fork-width-chain`, all 2026-08-20; PR-less local-merge landing adopted this session for rewrite velocity).
- **Version**: `0.6.0-nightly` since 2026-08-20 (`runner-app`/`runner-backend`/`runner-terminal` in lockstep) — the 0.6 line marks the switch to the GPUI implementation; main's Tauri line stays 0.5.x.
- **Done**: 0046 M0 (gpui-ce swap), M1 (repo-and-below at `main` parity, node tree adopted, protocol crates wholesale; human-verified 2026-08-17), M2 (terminal split + shell modularization), M3.1–M3.4 (session-hardening slice), M3.5 (runtimes: discovery, Qoder/TRAE, model catalog, model/effort overrides, codex trust preseed), crate renames (decision 7), terminal-pane IME (M4 pull-forward; Pinyin human-verified 2026-08-18), direct-chat composer removal (parity restore, smoke-tested 2026-08-18), Start Chat modal + pane close (M4 pull-forward over M3.5), single-canvas design merge (`design/runner.pen`, bands + `cmp/SettingsNav`), M4 entry surface inventory and stage refinement, M4.1 app shell (human-verified 2026-08-18), M4.2 shared widget set (human-verified 2026-08-19), M4.3 sidebar half (tasks 1–2, A6–A7) and M4.3 direct-chat half (tasks 3–6, B1–B11) — both human-verified 2026-08-19 — the direct-chat working-directory validation fix (PR #423; mirrored to `main` as PR #424, merge pending CI infra), M4.4 entry surfaces (inventory D1–D7 + the M3 pagination seam; human-verified 2026-08-20), M3.6 router/inbox backend parity, M3.7 mission-feed backend parity, and M3.8 misc backend parity (all reviewed CLEAN, test-suite-verified 2026-08-20 per the backend-slice deviation) — **M3 is complete** — M4.5 mission surfaces (full stage, tasks 1–6 in one mission per Jason; reviewed CLEAN with a live smoke-fix loop, landed 2026-08-20), the resume/fork width chain (doubled-lines resume fix; human-verified 2026-08-20), and terminal wheel reporting (mouse-mode scroll routing to agent TUIs, 2026-08-20).
- **Next**: terminal block/box glyph fix (procedural U+2500–U+259F cells; parity with xterm.js customGlyphs — Jason triggers) → M4.6b command palette + sidebar search wiring → rest of M4.6 (shortcuts registry, Agents/MCP/Archived, multi-window, then the Sparkle/Updates slice last) → nightly channel (plan decision 10) → M5 (sweep + watermark). **Source of truth is `main`'s shipped React code (`src/`) on every axis including visual treatment; `design/runner.pen` is a reference only** (Jason, 2026-08-18) — this supersedes the earlier "visual source of truth where a matching frame exists" framing.
- **M4 inventory**: [m4-surface-inventory.md](m4-surface-inventory.md) classifies 75 current-`main` surfaces: 3 functional-but-unstyled, 22 partial, 50 missing. It also records ready backend ops, remaining M3 dependencies, and Phase 5 service prerequisites.
- **Parity watermark**: `origin/main` fully ported for repo-and-below as of `1b7ee92` (v0.5.2 line, 2026-08-17); feature-logic lag is M3 scope.

## 2026-07-18 — Phase 1 kickoff

- Plan doc committed on `rust-ui-nightly` (`163c048`), then moved into this dedicated dir (`docs/impls/0031-rust-native-ui-rewrite/`).
- Branch strategy per Jason: Phase 1 work happens on `phase1-terminal-spike` (off `rust-ui-nightly`); merge back into nightly only after the spike works.
- Scaffolded `crates/native-spike/` as a new workspace member. Dependency versions resolved: `gpui 0.2.2`, `alacritty_terminal 0.26.0`, `portable-pty 0.9` (matches `src-tauri`).
- Next: fixture recorder + replay harness (no GUI dependency), then the GPUI window wiring PTY → `Term` → grid element, then composer/IME, then the throwaway .app packaging pass.

## 2026-07-18 — Phase 1 spike built (same session)

- Environment: gpui's shader build needed the Xcode 26 Metal Toolchain (`xcodebuild -downloadComponent MetalToolchain`) — installed once; noted as a build prerequisite.
- `crates/native-spike/` complete: `terminal.rs` (portable-pty ⇄ `alacritty_terminal::Term`, event pump answers PtyWrite/ColorRequest/TextAreaSize queries), `terminal_element.rs` (custom GPUI element: bg quads + shaped runs + cursor; ASCII spans shaped together, wide/non-ASCII glyphs shaped per cell at their own column origin so fallback-font advances can't skew the grid), `composer.rs` (full `EntityInputHandler` IME field adapted from gpui's Apache-2.0 input example), `main.rs` (window, focus, keybindings, scroll, paste).
- gpui is optional behind the default-on `gui` feature; harness + recorder build without Metal via `--no-default-features`.
- Fixture corpus started (spec 42 debt): `record-fixture` bin logs real PTY bytes to NDJSON; replay tests snapshot the grid. Corpus: real interactive `claude-session` (boot → prompt → streamed reply → /exit palette), `top-busy`, `width-torture`. 10/10 tests green, including wide-char spacers, combining marks, leading-spacer wrap, reflow round-trip (asserts alacritty's real cursor-pinned scrollback-push semantics), alt-screen restore, scrollback clamp.
- Packaging criterion: `package.sh` builds release, assembles `Runner Native Spike.app`, codesigns with the local Developer ID (validates). Notarization prepared (`--notarize`, NOTARY_PROFILE or APPLE_ID trio) — needs Jason's credentials.
- Licensing note: Zed's `terminal`/`terminal_view` crates (GPL) were used as architectural reference only; all code here is original or adapted from the Apache-2.0 `gpui` crate itself (its `examples/input.rs`).
- Pending: human-eyes criteria (busy-output smoothness, IME typing, resize feel, notarize+staple) — checklist in `crates/native-spike/README.md`; reviewer pass on the working-tree diff.

## 2026-07-18 — Review round 1 (same session)

- Reviewer found 6 issues; all addressed:
  1. (High) `TerminalSession` Arc cycle — event thread held the session while blocking on the event receiver whose senders the session (transitively) owned, so Drop never ran and the PTY child leaked. Event thread now holds only writer/size/title Arcs; Drop kills **and reaps** the child. Regression: `tests/session_lifecycle.rs` proves dropping the last session Arc terminates the child.
  2. OSC 10/11/12 color queries clamped named slots (256/257/258) into the 0-255 palette — every probe got grayscale slot 255. Added `palette::resolve_index` handling alacritty's named color-table slots; `tests/osc_color_query.rs` asserts the emitted OSC 10/11/12/4 replies and the DA `PtyWrite` path.
  3. Renderer span extension let an ASCII cell join a span started by a narrow non-ASCII glyph (box drawing/braille), re-creating the fallback-advance skew. Spans now track `ascii_only`; only pure-ASCII spans extend.
  4. IME marked-text selection offsets were converted against the whole content; NSTextInputClient sends them relative to the marked string. Extracted `text_util::marked_selection` (convert against `new_text`, then anchor) with multibyte-prefix tests.
  5. Composer navigation/backspace used scalar boundaries, tearing ZWJ emoji/skin tones/flags/combining marks. Now grapheme-cluster boundaries via `unicode-segmentation` (`text_util`), tested against exactly the width-torture classes.
  6. Spec gap: plan said "spawn via the existing session manager"; the mission brief explicitly allowed direct portable-pty for the spike. Recorded as an explicit deviation in the decision memo and narrowed its claims — integration seam validation is Phase 2/3, not spike-validated.
- Post-fix: fmt/clippy clean, 23 native-spike tests green (10 fixture + 6 OSC + 6 text_util + 1 lifecycle), full workspace battery (fmt --check, clippy --workspace, test --workspace) re-run green.

## 2026-07-18 — Review round 2 (same session)

- Re-review passed findings 1/3/4/5 and accepted the documented session-manager deviation. One must-fix remained from finding 2: color-query replies ignored runtime OSC overrides (a program that sets OSC 10 then queries it got the static default, disagreeing with the renderer which reads `content.colors`).
- Fix: new `terminal::query_color` prefers `Term::colors()[index]` then falls back to `palette::resolve_index`; the event pump reaches the Term through a `Weak` (upgrade per query) so the drop-cycle fix from round 1 stays intact. The OSC tests now resolve replies through `query_color` against the live Term — including set-then-query (OSC 10, OSC 4;1) and OSC 104 reset-then-query coverage.
- Final state: 26 native-spike tests green, workspace battery green (fmt clean, clippy zero warnings, 473 tests passing).
- Round 3: reviewer's sandbox denies `ps`, which made the lifecycle regression environment-dependent (it treated any `ps` failure as process-dead). Rewritten hermetically: hold `Weak<TerminalSession>`, drop the last Arc, assert `upgrade()` is None — the exact signal the original event-thread Arc cycle trips — with child teardown implied by Drop's synchronous kill+wait. 6/6 isolated runs green.
- Review clean after round 3; spike committed on `phase1-terminal-spike` (`6d6ee2e` code, `43d0b28` docs).

## 2026-07-19 — Phase 1 verdict: GO

- Jason smoke-tested the spike: live claude session renders and drives correctly ("works mostly"), and after clarifying that the IME target is the composer bar (the terminal pane intentionally has no IME in the spike), **Pinyin input in the native composer confirmed working** — the hard requirement.
- Jason added Makefile targets for the spike and `default-run = "native-spike"` (two bins made bare `cargo run -p native-spike` ambiguous).
- Known cosmetic artifact triaged: block-glyph striping in the claude logo (glyphs don't fill the 1.4× line box); fix options noted (tighter line height now, cell-box glyph stretching in Phase 3).
- Notarization (criterion 5's second half) deferred to Phase 5 by Jason's decision — codesign of the hand-rolled bundle is proven, notarytool flow already ships Tauri releases, so the residual risk is negligible.
- Decision memo in [plan.md](plan.md) finalized: **go on GPUI**. Next: merge `phase1-terminal-spike` → `rust-ui-nightly` on Jason's word, then Phase 2 (app-core extraction on `main`).

## 2026-07-19 — Phase 2 shipped; branch strategy revised

- Phase 2 (app-core extraction) implemented on `refactor/0031-app-core` off `main`: new workspace crate `crates/runner-app` holds the UI-agnostic core — db/repo/model, session manager, event bus, router, MCP server, and the ~85 command bodies as plain fns in `runner_app::ops` over a new `AppCore`. Tauri command wrappers are one-line delegations (invoke contract byte-identical); event fanout swapped to a `tokio::sync::broadcast` channel with the Tauri layer as a single forwarding subscriber (one `app.emit` left in the whole crate). Event-name/payload inventory verified identical pre/post. No schema, stored-format, or frontend changes.
- Review clean in one round (no must-fix; reviewer confirmed command-registration parity, zero tauri deps in runner-app via cargo tree, and accepted the two documented tradeoffs: Weak session-manager upgrade in the emitter, bounded 8192 broadcast queue). `make ci` green (441 tests); PR [#310](https://github.com/yicheng47/runner/pull/310) open against `main`, CI green. Reviewer's maintenance note applied: release skill now bumps `crates/runner-app/Cargo.toml` too.
- **Branch-strategy change (Jason's decision)**: `rust-ui-nightly` becomes a Tauri-free pioneer branch — after `main` merges in post-#310, delete `src/`, `src-tauri/`, and the JS toolchain from nightly; nightly = shared backend crates + native UI only. Cutover becomes merging nightly into `main`. Modify/delete merge conflicts on the deleted trees are the accepted cost. Details in [plan.md](plan.md) §Branch strategy.
- Next: Jason merges #310 → merge `main` into nightly → Tauri-deletion commit on nightly (incl. Makefile/CI adjustments, TBD in plan) → Phase 3 walking skeleton as `crates/runner-native/`.

## 2026-07-19 — Branch renamed to `gpui-nightly`; #310 re-homed off `main`

- `rust-ui-nightly` renamed to `gpui-nightly` (local + remote, old ref deleted, same SHA `8584caa`) — the branch is a GPUI bet specifically, so the name should say so. plan.md references updated; historical log entries above keep the old name.
- Jason reset `main` after merging #310, so the PR reads MERGED on GitHub but its tree is not on `main`; the extraction lives on `refactor/0031-app-core` (`461356e`). Nightly composition therefore sources `crates/runner-app` by merging that branch (plus current `main`) rather than getting it via `main`. Whether the extraction ever re-lands on `main` is open — not needed for the nightly line.

## 2026-07-19 — Nightly composition

- Created `phase3-walking-skeleton` from `gpui-nightly`, merged current `origin/main` (frontend fixes #309/#311 plus v0.3.17), then merged the Phase 2 extraction from `refactor/0031-app-core` (`461356e`). Hand-check confirmed #309/#311 have no backend half to preserve; their `src/` changes are pure deletions on the Tauri-free line.
- Deleted the React/Tauri trees and JS toolchain. `crates/runner-app` is the surviving backend and was aligned to v0.3.17; the workspace lockfile drops the Tauri dependency graph.
- Build decision: the Makefile exposes only `fmt`, `clippy`, `test`, and `run-native`; CI is one `macos-26` Rust-workspace job for pushes/PRs targeting `gpui-nightly`. The obsolete Tauri release workflow is removed, with native packaging intentionally deferred to Phase 5.

## 2026-07-19 — Phase 3 walking skeleton

- Replaced `crates/native-spike/` with `crates/runner-native/`, preserving its GPUI terminal element, palette, IME-capable composer, and terminal fixture corpus. The binary boots `runner_app::AppCore` against the same release/debug SQLite database and logs directories as Tauri, lists existing direct chats, and opens or resumes a selected chat in a live terminal with a working composer.
- Repaired the Phase 1 deviation at the integration seam: `runner-app`'s canonical `SessionManager` now owns spawn/resume, environment composition, input, resize, and lifecycle. `runner-native` only attaches an `alacritty_terminal::Term` to the manager snapshot plus `AppCore` broadcasts; it has no direct `portable-pty` lifecycle. A `/bin/cat` integration test proves direct-session output reaches the rendered grid through that path.
- Removed the superseded spike and its throwaway recorder, lifecycle test, and packaging script; updated the workspace default binary and `make run-native` to `runner-native`. Native validation covers 27 tests, including all recorded PTY fixture replays; `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo test --workspace` are green.
- Next: Jason smoke-tests with `make run-native`, clicks a direct chat, verifies terminal input/output, scrolling, resize, and Pinyin composition in the composer. The branch remains local and unmerged until that confirmation.

## 2026-07-19 — Phase 3 review round 1

- Reviewer found three branch-composition issues: the authoritative plan still described Phase 2 as present on `main` and left Makefile/CI undecided; a Tauri sidecar-staging script and release skill survived their deleted targets; CI did not install GPUI's Metal Toolchain prerequisite.
- Fixed all three: the plan now records the extraction's actual branch and the completed pioneer-line decisions, stale Tauri automation is removed, and CI installs the Metal Toolchain component before building. The bug skill's adjacent version lookup now reads `crates/runner-app/Cargo.toml`.
- Post-fix validation is green: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, and `git diff --check`.

## 2026-07-19 — Human smoke-test follow-ups

- The walking skeleton rendered and drove existing direct chats successfully; Jason confirmed the final switch/reattach fix and authorized merge back to `gpui-nightly`.
- `make run-native` incorrectly used a release build and therefore opened the production database during testing. The working-tree fix switches the development command to the debug profile and its `com.wycstudios.runner-dev` database; reviewer found the delta clean.
- Chinese IME works in the composer but not the focused terminal because the terminal handles raw key events without a GPUI input handler. Jason declared terminal IME a pre-release requirement; it is now explicit in the Phase 4 direct-chat slice and remains a Phase 6 cutover blocker.
- Switching between running chats replayed cursor-addressed TUI output at a hard-coded `100×30` before resizing, corrupting the grid when the live PTY used the actual pane width. The fix inherits the outgoing rendered size for resume and reattach; reviewer found it clean and Jason confirmed it by retest. Known parity limitation: after resizing the window while a shell-runtime chat is inactive, its old-width snapshot can remain mis-wrapped because shells do not repaint on `SIGWINCH`; Claude Code and Codex repaint after the genuine width change.

## 2026-07-19 — Repo split: `gpui-nightly` → `runner-gpui`

- The nightly line moved to its own repository, [yicheng47/runner-gpui](https://github.com/yicheng47/runner-gpui), seeded by pushing `gpui-nightly` (`7da6fea`, post-Phase-3 merge) as `main` — full history preserved. The name deliberately says the framework; "runner-native" was considered and rejected as implying multi-platform, and this line is macOS-only by plan.
- Rationale: after `main` was reset off #310, the shared-crates merge premise broke (main's backend at `src-tauri/src/*`, this line's at `crates/runner-app/*`), so every branch sync would have leaned on rename detection across the extraction plus modify/delete conflicts — monorepo costs without monorepo guarantees. The split makes the fork honest, and cutover-on-success becomes a repo rebrand (archive `runner`, rename this) instead of an in-place tree-swap merge.
- The `runner` repo is wired as the `upstream` remote here; backend fixes on the Tauri line are ported by conscious cherry-pick, not automatic merges. `gpui-nightly` and the `runner-wt1` worktree are retired; `runner`'s `main` remains the shipping 0.3.x Tauri app in maintenance.
- Docs cleanup: the open Tauri-line specs (features 05/19/21/24/37, impl 0019 landing page) moved to their `archive/` dirs unshipped — frozen during the parity-only rewrite, noted as such in the features README. `product/vision.md` stays as-is; `arch/arch.md` keeps its model/protocol sections but its stack section still says Tauri 2 + React — update pending, tracked as Phase 4-adjacent doc debt.

## 2026-08-17 — Single repo restored, spec 0046, docs regrouped

- Jason reverted the repo split: `gpui-nightly` is back as a long-lived branch on `yicheng47/runner`, pushed at `774aa35` (no force needed — the branch didn't exist on the remote). The standalone `runner-gpui` GitHub repo is retired; locally the line lives in the `runner-gpui` worktree. CI re-targeted from `main` to `gpui-nightly` (`dde41e7`).
- Impl 0046 (main-parity catchup) authored and committed (`14e9680`): decisions (main owns design + schema, repo-and-below verbatim, product UI never diverges, gpui-ce, Zed-style terminal split on upstream alacritty), workstreams A–D, milestones M0–M4. 0031's Phase 4 slice list superseded.
- Docs regrouped: gpui-rewrite impls now live under `docs/impls/gpui-rewrite/` (0031, 0046, this log, README).

## 2026-08-17 — M0: gpui-ce swap (same session)

- `gpui 0.2` (crates.io, stale since 2025-10) → `gpui = { package = "gpui-ce", version = "0.3" }`. True drop-in: zero source changes; the whole diff is Cargo.toml + lockfile.
- Gates green: fixture corpus (10/10 incl. CJK/emoji/box-drawing/reflow), workspace tests, clippy, binary links.
- `make run-native` renamed to `make run` (Makefile, AGENTS.md, READMEs).

## 2026-08-17 — M1: repo-and-below catchup (same session)

- First `make run` against the dev DB hit `sqlite: no such table: tabs` — the dev DB (shared with the Tauri dev build by design) was at main's migration v20, where 0015 dropped `tabs`/`folders`. Exactly the incompatibility 0046 predicted.
- Ported wholesale from `origin/main` (merge-base `d9483ca`): migrations 0014–0020 + Rust backfills, `db.rs` (LKG login-shell env in `_app_state`, peer-coding seed + `examples/peer-coding/`), `model.rs`, `repo/` (node.rs in, tab.rs/folder.rs out), `shell_path.rs`, `crates/runner-core`, `cli/`. Migrations 0001–0013 verified byte-identical across branches — the chain never forked, so seamless open-either-way holds.
- Adapter layer: `ops/node.rs` ported from main's `commands/node.rs` (bodies + tests verbatim, mechanical Tauri→`AppCore` translation). Node-maintenance hunks applied to mission (start/archive/pin/reset/unarchive/set-project), project (create node; delete archives children through the tree), session (archive removes from node; pin pins the tab node; set-project reconciles placement). Resume path adopts the caller→persisted-last-size→default geometry resolution (migration 0016).
- UI minimally rewired: `pane_layout` + sidebar read tab nodes via `node_list`, write via `node_tab_upsert`. Full node sidebar (projects/folders/pinned) deferred to M3.
- Gates: 498 workspace tests green (incl. main's ported node/db suites), clippy `-D warnings` clean.
- Dev DB sharing restored: the parked main-schema dir was copied back live (v20, nodes present); backup deleted after. Both dev builds share `com.wycstudios.runner-dev` — run one app at a time.
- Deliberately not ported (M3 scope): session hardening (reaping, resume seams, resize-storm coalescing), runtimes (discovery, Qoder, TRAE, model catalog, model/effort override plumbing beyond schema), router/inbox, mission feed, pagination, update checks. `main`'s impl docs 0033–0045 are the port guides.

## 2026-08-17 — Phase 5 updater decided: Sparkle via the pulse pattern (same session)

- Studied pulse (`~/repos/yicheng47/pulse`, pure gpui-ce at a pinned git rev): `SPUStandardUpdaterController` via ~130 lines of `objc2` bindings behind an `updater` feature with a no-op dev fallback; `Sparkle.framework` embedded by `script/bundle-mac` (pinned + SHA-256 checked); appcast on GitHub Releases (`SUFeedURL`, `SUPublicEDKey`).
- Zed's hand-rolled `auto_update` (~2k lines: dmg mount/replace/relaunch, own release API, per-OS helpers) reviewed and rejected — its complexity pays for multi-platform + own-server needs Runner doesn't have.
- Cutover bridge stands: last Tauri release updates users into the first native release via `tauri-plugin-updater`'s artifact format (same minisign keypair + bundle id); Sparkle owns native→native after. Recorded in 0031 Phase 5.

## 2026-08-17 — M2: terminal split + main.rs modularization

- New `crates/runner-terminal` — the `terminal` half of the Zed-style split: `terminal.rs` (`TerminalSession` + `TerminalBridge`), `palette.rs`, `fixtures.rs`, `replay.rs`, and a new `mappings.rs` consolidating keystroke/paste encoding as pure functions (mode state in, bytes out). The fixture corpus (data + `fixture_replay.rs` + `osc_color_query.rs` tests) moved with the model; the replay-race unit test now builds its own minimal `AppCore` instead of borrowing runner-native's bootstrap. No gpui dependency — the crate stays UI-agnostic; the PTY remains `SessionManager`'s (the documented deviation from Zed). The `alacritty.rs` API-isolation module from the 0046 mapping table is deferred until the view side needs it.
- `runner-native/src/main.rs` (1,405 lines) split for M3's parallel lanes: `chat.rs` (session attach/lifecycle, tab activation, key/scroll/paste routing, chat start/resume, split resize), `sidebar.rs` (tab list rendering + labels), `panes.rs` (active-tab surface, layout picker, pane tree). `main.rs` is down to 291 lines of app shell (structs, boot, render root).
- Gates: `make verify` green (check + 498-scale test suite + clippy `-D warnings` + fmt), corpus green in its new crate, binary links.

## 2026-08-18 — M3.1: session process reaping (A4 session-hardening slice begins)

- Ported `main`'s `48d2231` (verify and reap killed processes, #342) and `0dadea7` (reap mission siblings after slot exit, #346) via `feat/0046-m3-session-reaping`: PTY stop verifies death and reaps with a SIGKILL process-group fallback; the startup orphan sweep identity-checks recorded processes; manager kill sweeps aggregate failures and log DB reconciliation errors; unexpected mission-slot exit or spawn failure cancels and reaps live siblings, with the `was_killed` guard keeping intentional kills from recursing. `pty_runtime.rs` SHA-256-matches main's post-commit source; manager tests ported line-identical.
- Reap calls land in `mission_start_impl_with_size` and `mission_reset_impl`'s spawn-failure branch; `ops/node.rs` already carried `48d2231`'s kill aggregation since M1, so no diff there.
- Recorded divergence: `runner-native/src/bootstrap.rs` makes `cleanup_orphan_processes_on_startup` fatal where main's Tauri builder logs-and-continues — deliberate, matching `boot_core`'s existing fatal stale-row convention; the only error paths are DB failures that would abort one line earlier anyway. Also noted: `stop()` may now block ~500ms per stuck session (callers are quit/close handlers and MCP ops).
- Gates: `make verify` green; 428 `runner-app` tests including the ten ported reaping tests; codex-peer review clean with independent SHA and hunk-level verification.

## 2026-08-18 — M3 task breakdown (mission numbering)

M3's slices (0046 §Sequencing) run as serial codex-peer missions, one task at a time; mission titles use this numbering:

- **M3.1** — session reaping: `48d2231`, `0dadea7`. Merged via PR #409 (2026-08-18).
- **M3.2** — resume seams + geometry: `9f53047` (#349), `02e7502`, `cb32720`, `3f3bc04`. Port guides: main's impl 0032 (archived), 0035, 0038. M1 already ported `repo/session.rs`, migrations 0016/0017, and the resume geometry resolution — diff against `origin/main` before applying hunks.
- **M3.3** — resize-storm coalescing: `38af26c` (#373), `e78718f`. Port guide: 0039. Large frontend half needs GPUI translation in `runner-native`.
- **M3.4** — input latch + native quit stamping: `f926581` (impl 0041), `c5b1ce4`.
- **M3.5+** — later slices decomposed when reached, in 0046 dogfood order: runtimes/model-effort pickers (0033/0034/0036/0043/0045), node sidebar polish + pinned section (Workstream B), router/inbox group, mission feed (0042/0044), pagination/update checks/misc.

## 2026-08-18 — M3.2: resume seams + terminal geometry

- Ported `9f53047` and `02e7502` into `runner-app`: resume now emits an in-band clean seam for keep-ring runtimes or RIS reset for purge runtimes before the child forks; terminal tracking covers bracketed paste plus mouse modes; every spawn resolves and persists a concrete PTY size; stopped-pane measurements persist without requiring a live handle; unknown-session resize still errors without creating manager state. Main's source manager tests were ported, and a `runner-terminal` regression proves alacritty applies both seam policies to the mounted native grid.
- Native frontend mapping: active stopped panes already stay mounted and call `TerminalSession::resize` from GPUI prepaint, so the backend liveness split is the stopped-geometry persistence trigger; manual resume already supplies the attached pane's current size. Main's invisible `PersistentSurfaces` behavior has no native counterpart yet because this line has no route-retained chat/mission surface, so there is no hidden cross-surface layer to resize.
- `cb32720`'s repo state machine and tests were already present line-for-line from M1. Its command-side claim finalization remains deferred because the native app does not yet expose launch auto-resume or quit-time resume stamping; there is no claim consumer to finalize in this slice.
- Ported `3f3bc04`'s structured `SessionUpdatedEvent` emission with `mission_id`. Its mission resume-all refresh/retry helper remains deferred because `runner-native` has no mission workspace surface; direct-chat resume already refreshes synchronously and through the session event bridge.
- Gates: `make verify` green; 432 `runner-app` tests plus the native seam/reset regression pass.

## 2026-08-18 — M3.3: resize-storm coalescing + owner-pane size gating

- Ported `38af26c` and follow-up `e78718f`: width changes for full-repaint runtimes now settle after 175 ms of quiescence, collapse a storm to one ioctl/purge, skip the purge and force a rows-nudge repaint when the storm round-trips, persist requested geometry immediately, and abandon stale work across kill/resume/respawn. A real width settle purges to one in-band `ESC[2J ESC[H` event emitted under the session-state lock immediately ahead of repaint bytes; failed ioctls leave the ring and cols gate untouched. M3.2's resume seams, concrete spawn sizing, and stopped-geometry persistence remain intact.
- Native frontend mapping: `TerminalElement` derives the grid from real GPUI prepaint bounds and refuses zero/unplaced geometry; `PaneLayout` selects exactly one resize owner for a session in a grouped pane tree; non-owners neither refit the shared `alacritty` grid nor push PTY geometry. Pure Rust tests translate main's pane-geometry and size-verdict coverage, and the manager test hunks cover storm collapse, round trips, failed settle, kill/respawn linearization, automatic settle, and live in-band clear emission.
- Deferred with reason: React's `PersistentSurfaces`, hidden pool, `visibility:hidden` mounts, transitional `resizeDisabled` wrapper, and `frontend_log` placement/suppression telemetry have no native mount counterpart. `runner-native` renders only the active tab tree; retained `TerminalSession`s are models, not hidden surfaces, and keep parsing at the last owner-consistent grid until active prepaint refits and pushes. New-chat terminals mount only after spawn, while resume retains the already placed owner grid; backend `resuming` linearization covers stale settles. The explicit grouped-pane owner gate remains as protection against duplicate persisted slots and for future surface expansion.
- Gates: `make verify` green (workspace check, 439 `runner-app` tests, all native/helper tests, clippy `-D warnings`, fmt-check); the 10-test terminal fixture corpus stays green. The first sandboxed run's Unix-socket bind test hit the expected `EPERM`; the identical permitted run passed.

## 2026-08-18 — Terminal-pane IME

- Added a per-session GPUI input handler to focused terminal panes: marked text stays in local composition state and renders as an underlined cursor-cell overlay, candidate-window bounds follow the rendered cursor cell, committed UTF-8 goes through `TerminalSession` into `SessionManager`, and raw dispatch remains reserved for control/navigation/function keys and app shortcuts.
- Pure tests cover marked-text replacement/clear transitions, UTF-16 selection conversion for multibyte text, raw-versus-IME arbitration, and function-key encoding; a `/bin/cat` integration test covers the committed-text SessionManager path. Chinese Pinyin composition and candidate placement in normal and split/scrolled panes remain human-verified before landing.

## 2026-08-18 — Direct-chat composer removed (parity restore)

- With terminal-pane IME verified, the bottom composer under direct-chat terminals is gone: running chats render the terminal as the only input, at parity with `main`'s `RunnerChat` (the composer was this line's IME workaround, never a `main` surface). The terminal grid gains the reclaimed height; the stopped/crashed Resume bar stays.
- `composer.rs` deleted rather than parked — dead code fails the clippy gate, and the mission-feed channel composer (0042, later M3 slice) will build on `terminal_ime` + pulse's `text_input` instead. `text_util` and its tests stay (the terminal IME consumes them); `theme::selection` dropped as dead.
- Gates: runner-native build + tests green, clippy `-D warnings` clean, fmt clean. Human smoke test passed (typing incl. Pinyin straight into the terminal, shortcuts intact, Resume bar present).

## 2026-08-18 — M3.4: conservative input latch + native quit stamping

- Ported the session-manager half of `f926581` (impl 0041's conservative end state): printable/UTF-8 input and bracketed/image paste latch pending input; Enter and Ctrl-C clear it; all escape-prefixed terminal protocol traffic is activity-only; failed writes roll the latch back; respawn, kill, and exit clear it. The source classifier test ports with OSC 10/11 coverage.
- Deferred the router and frontend halves precisely: this branch's router predates `828c9fc`'s reservation/outbox/listener contract, so the latch is tracked but routing does not consume it until the router/inbox slice; pulling that contract now would drag the whole group. `InboxBlockedPill` and `MissionWorkspace` have no native mission surface yet and remain M4 scope; the pill design is the `InboxBlockedPill` component in `design/runner-mission.pen`.
- Ported `c5b1ce4` at the native seam: every real quit path atomically calls `mark_running_for_resume_on_launch` before stopping the returned live sessions, including the final event-loop fallback, and stop failures are aggregated after every marked id is attempted. This activates `cb32720`'s feasible quit-side claim handling: an in-flight launch claim is requeued before teardown. Claim consumption/finalization remains deferred because native has no `take_resume_on_launch` queue, `SessionManager::resume_on_launch` path, setting, or launch UI; manual resume must not finish a launch claim. Until a consumer lands, quit stamps accumulate as an undrained `PENDING` backlog, so the launch-queue slice must explicitly drain and discard the pre-existing backlog on first boot or impose a cutoff before enabling auto-resume.
- Gates: `make verify` green (workspace check, 440 `runner-app` tests, all native/helper tests, clippy `-D warnings`, fmt-check); the 10-test terminal fixture corpus stays green. The sandboxed run hit the expected temporary Unix-socket `EPERM`; the identical permitted run passed.

## 2026-08-18 — Crate renames (decision 7)

- One mechanical commit right after M3.4, per the revised decision 7: `crates/runner-app` → `crates/runner-backend` (package `runner-backend`, lib `runner_backend`), `crates/runner-native` → `crates/runner-app` (package/bin `runner-app`, pulse's `-app` convention). Imports, workspace members, Makefile `run` target, and normative doc references updated; historical log/plan narrative keeps the old names.
- From here the port mapping is `src-tauri/src/*` ↔ `crates/runner-backend/src/*`; **`runner-app` now names the binary** — every future mission goal states this to defuse the name-reuse hazard.
- Gates: `make verify` green.

## 2026-08-18 — M3.5: runtime discovery, agent catalog, overrides, and Codex trust

- Ported main's executable discovery slice into `runner-backend`: startup is non-blocking and seeded from the persisted login-shell last-known-good environment, refresh/override/status ops emit `runtime/changed`, and every mission, runner-backed direct, runtime-only, and resume spawn resolves catalog commands while preserving custom runner commands and still-live recorded absolute paths.
- Added Qoder and TRAE end to end in the backend runtime registry, permission/model/effort argv adapters, resume and rollout-capture paths, MCP configuration, resize/resume output policies, and selectable availability-aware catalog. Main's model and effort option tables now live in backend ops for the next native Start Chat modal; Codex project trust is pre-seeded best-effort before each Codex spawn.
- Direct chats accept runtime/model/effort overrides, slot model/effort layering matches main, runtime-only and options-only sessions persist enough state for resume, and changing a runner's runtime clears only inheriting slots' stale model/effort values. The React settings/picker components remain deferred because no native settings or Start Chat modal exists yet; M3's next modal task can consume the backend catalog and ops without reconstructing the tables.
- Gates: `make verify` green (workspace check, 496 backend tests plus app/CLI/core/terminal suites, clippy `-D warnings`, fmt-check); the 10-test terminal fixture corpus stays green. The sandboxed run hit the expected Unix-socket `EPERM`; the identical permitted run passed with exit 0.

## 2026-08-18 — Native Start Chat modal and pane close

- Replaced the sidebar-swap new-chat skeleton with a GPUI modal matching `main`'s Runner and Direct flows, discovery-backed Agent/Model/Thinking effort controls, sticky auto-derived chat names, working-directory entry plus GPUI's asynchronous native folder selection, persisted mode preference, empty-pane runner preselection, and override-aware spawn/rename wiring. A reusable Pulse-pattern text input provides native IME composition for the modal fields and prevents Enter submission while composing.
- Ported `main`'s empty-pane close control and pure tree-collapse semantics, including sibling promotion, preset/name/focus rebuild, persistence, and leaving the removed pane's session alive. Unit coverage pins title derivation, effort lookup, mode persistence, IME Enter behavior, and pane-tree close/rebuild behavior.
- Gates: `make verify` green with exit 0 after granting the existing MCP Unix-socket test its required sandbox permission; the 10-test terminal fixture corpus stays green.

## 2026-08-18 — M4 entry: surface inventory and stage refinement

- Audited all of `origin/main`'s `src/` at `09532d2` (7 pages, product components, settings, UI primitives, hooks, support modules, and behavior tests), the native `crates/runner-app` surface and `crates/runner-backend` operations, and `design/runner.pen` through the Pencil MCP. The canvas contained 68 top-level nodes and 8 reusable `cmp/` components; no `.pen` filesystem read was used.
- Added [m4-surface-inventory.md](m4-surface-inventory.md): 75 current-product surfaces classified as 3 functional-but-unstyled, 22 partial, and 50 missing. The only behavior-complete native surfaces are the split layout/resizing model, six-preset picker, and empty-pane create/close flow; everything else is partial or absent.
- Corrected two stale planning assumptions exposed by the audit: current nodes are `project | tab | mission` with no folder type or Projects page, and `main` now ships four app palette variants plus three terminal palettes while native still hard-codes Tokyo Night. Pencil conflicts are recorded rather than promoted into features: its flat sidebar and Settings Chat/Remember-window-position/combined-About-updates treatments do not override current `main`.
- Refined M4.1–M4.6 into ordered task lists and swapped the entity and mission stages: M4.4 now matures the lower-complexity list/form/modal/drawer/drag primitives and Crew Editor launch path before M4.5 composes them into the mission workspace. All remaining M3 backend slices still complete before M4 starts. Sparkle/update UI, logs/reveal, URL opening, and native window restore are explicitly Phase 5 prerequisites instead of inert M4 mock surfaces.
- Docs-only gate: `make verify` green with exit 0. The sandboxed first run hit the existing MCP Unix-socket `EPERM`; the identical permitted run passed check, all workspace tests, clippy `-D warnings`, and fmt-check.

## 2026-08-18 — M4.1: app shell

- Owned window chrome replaces the system titlebar: hidden titlebar with positioned traffic lights, drag regions, double-click zoom, and fullscreen treatment in a new `mac_chrome.rs`, following pulse's gpui-ce patterns (decision 8) rather than Zed's GPL terminal crates.
- Theme layer rebuilt in `theme.rs` + `app_settings.rs`: Auto/Light/Dark intent resolution over `main`'s four shipped app variants (Runner/Carbon, Catppuccin Mocha, Codex Light, Catppuccin Latte) plus the terminal palettes, app fonts, zoom, base typography/spacing, focus rings, and scrollbars. Tokyo Night is gone from both `runner-app/src/theme.rs` and `runner-terminal/src/palette.rs` — the terminal element now reads theme data instead of fixed constants, and `runner-terminal` stays gpui-free.
- App frame landed as `app_shell.rs` + `toast.rs` + `assets.rs`: route-capable frame and header zones, the resizable/collapsible sidebar slot (persisted width/collapse, hover preview, keep-open pin, shortcut, panel glyph — slot only; its node-tree contents remain M4.3), persistent surface layer, settings takeover layer, queued toast host replacing the single undismissable error strip, loading/error boundary, and the existing Runner icon as the in-app mark.
- Source-of-truth rule tightened mid-mission (Jason): `main`'s React code wins on every axis including visual treatment; Pencil is reference only. Values already taken from the canvas were re-derived from `src/`.
- Gates: `make verify` green, terminal fixture corpus green, and human smoke test confirmed the shell plus the existing chat/pane/modal/IME flows.

## 2026-08-18 — M4.2: shared widget set

- Added the reusable GPUI widget layer for inventory rows F1–F17: buttons and icon buttons, cards/badges/status treatments, deterministic runner avatars and presence, copy feedback, fields and multiline textareas, model/working-directory fields, runtime and styled selects, toggles and steppers, modal/drawer/confirm shells, popover menus, tooltips, session controls, search/list/empty/pager primitives, settings shells, and app/terminal scrollbars. The variants, copy, sizing, colors, and state treatments follow `main`'s React components; stale inventory wording such as the SearchInput shortcut keycap and avatar initials did not override the current source.
- Replaced Start Chat's local input, picker, model menu, working-directory row, button, and overlay implementations with the shared controls. The existing direct-chat Resume bars, pane toolbar buttons, sidebar row controls, modal scroll area, sidebar list, and terminal panes now consume their corresponding shared controls where `main` has an equivalent; zoom-scaled app chrome and specialized controls such as Start Chat's segmented mode switch and the layout preset grid remain local because `main` also implements those separately.
- Start Chat mounts its modal as the root's final direct overlay rather than deferring the whole surface: selects, popovers, and keyboard tooltips own their deferred layers, and GPUI forbids creating those layers while already drawing a deferred parent. Opening the modal also dismisses the deferred collapsed-sidebar preview so it cannot overdraw the backdrop.
- Preserved the human-verified IME seam while extracting `TextField`: UTF-16 marked ranges, replacement composition, candidate-window bounds, grapheme selection/deletion, clipboard actions, and Enter suppression during composition are covered by regressions alongside multiline behavior and field validation. Pure coverage also pins pager windowing, avatar hue/pixels, select/menu keyboard navigation and disabled filtering, working-directory precedence, runtime availability/default-enabled policy, and scrollbar math.
- Native settings now persist the default working directory, default runtime, and explicit enabled/disabled agent policy consumed by Start Chat. `runner-terminal` exposes GPUI-free scroll metrics and absolute display-offset movement; GPUI owns both app and terminal scrollbar rendering.
- Gates: `make verify` green; `cargo test -p runner-terminal` green.

## 2026-08-19 — M4.3: sidebar (tasks 1–2, A6–A7)

- Replaced the fixed direct-chat tray with `main`'s node hierarchy: Workspace runner/crew entries, brand/search trigger, pinned rows, collapsible project containers, root chats/missions, create and row context menus, Start Project, project-scoped Start Chat, inline rename, pin/unpin, archive, remove-from-project, delete confirmation, and drag reorder/reparent. The durable tree remains strictly `project | tab | mission`; no folder node or Projects page was reintroduced.
- Single-pane tabs and multi-pane accordion groups now carry active/focused treatment, pane-order children, compact kebab controls, busy/unread priority, pinned and collapsed-container rollups, persisted section/container collapse, and window-focus-aware viewed watermarks. Tab and pane activation still attach/resume the same PTY and restore terminal focus; empty-pane and global starts preserve the active project scope.
- Working-tree review tightened the risky seams: GPUI drag listeners are hit-bound and expose end/empty-scope drop targets, accordion grouping counts occupied members rather than empty layout panes, active project scope is re-derived after every tab activation/reload, mission refreshes filter appended events to the six shipped signals, and archive kills run off the UI thread with in-flight indicators. Sidebar row type is 13px at 100% after Jason's daily-drive feedback and remains rem-based so app zoom scales it.
- Daily-drive zoom testing exposed fixed-pixel sizing across the M4.2 widget set, not just the sidebar menus. Shared buttons, icon buttons, fields, selects and their popovers, model pickers, overlays, tooltips, scrollbars, session controls, avatars, list/settings primitives, and Start Chat's local treatments now resolve design dimensions through the window rem size; only measured geometry, explicitly zoomed shell/terminal coordinates, zero constraints, and physical hairlines remain pixel-based.
- Added shared context-menu support plus compact icon-button hover reveal, and pure coverage for attention priority, collapsed rollups, allowed drag/reparent targets, hidden-row preservation, pinned/project ordering, and ordering within a project scope.
- Deliberate later-stage targets render in place but remain inert until their owning surfaces exist: runner/crew pages and project mission starts are M4.4/M4.5, mission navigation/workspace is M4.5, search/command palette is M4.6, and secondary-window/update UI requires Phase 5 services. Stale-unread cleanup remains the recorded M3 dependency; this slice does not fake it.
- Gates: `make verify` green outside the sandbox after the known MCP temporary Unix-socket test hit `EPERM` inside it; `cargo test -p runner-terminal` green; `git diff --check` clean.

## 2026-08-19 — M4.3: direct chat (tasks 3–6, B1–B11)

- Rebuilt the direct-chat surface around `main`'s current `RunnerChat`: the owned topbar now carries sidebar/layout/panel controls, single-chat and group action menus, stop/resume aggregation, lifecycle locking, and inline warning/error treatment. The full-height runner/runtime panel persists its collapsed state and 200–480px design width, eases open and closed over 200ms without reflowing its contents, resizes from its left edge, and renders identity, command, cwd, session-key copy, and system-prompt metadata for the focused chat.
- Daily-drive optical tuning intentionally departs from React's raw spacing utilities at a few points: both chat headers use a 12px icon/title gap instead of `gap-2`, right-edge topbar and panel-header insets are 8px instead of `pr-4`/`px-4`, both 13px titles carry a 1px downward ink-alignment offset, and the ended card uses a 36px nominal bottom inset so the flush edge of the bordered Archive button optically matches the 20px inset above the title's text line box. These preserve mathematical center alignment while matching GPUI's rendered font and the human-verified native chrome.
- Daily-drive shell follow-up stops the owning drag region from propagating a handled double-click into the overlapping shell drag region, so macOS receives one titlebar action instead of zooming and immediately bouncing back. First launch retains React's 1440x900 default; later launches restore the last normal window size from `ui-settings.json`, clamped to the current primary display's usable bounds, with debounced resize writes and a window-close checkpoint so maximized/fullscreen geometry cannot replace it.
- Restyled the existing six-preset pane tree without changing its assignment or per-split persistence model: connected hairline gutters keep a wider drag target, grouped headers carry the terminal icon, CHAT badge, busy/idle/stopped/crashed semantics, scoped stop/resume/archive controls, and focus ring, and empty panes match current copy without the native-only sidebar instruction. The layout picker is the shipped six-diagram grid and now closes on pick, outside click, or Escape; Cmd+[ / Cmd+] cycle panes and Cmd+W collapses the focused split pane while retaining the session.
- Added a shared session-overlay primitive and pure lifecycle resolver with `Archiving > Resuming > Starting > ended` precedence. Starting/Resuming loaders wait for a TUI-ready signal or the same output-idle/minimum-visible/hard-timeout settlement contract as React; stopped/crashed panes retain dimmed, scrollable conversation history under the exact preserved/fresh-process copy and lock immediately into Resume or Archive progress.
- Closed Start Chat's remaining native contract: project entry points seed both `project_id` and project cwd ahead of runner/default-directory fallback, empty-agent copy links to the existing Settings takeover, and successful pane or tab spawns enter the Starting transition without disturbing the shared field/select/modal or IME paths. Pure coverage pins project/cwd precedence in addition to the existing request/title/runtime tests.
- Terminal rendering now applies the persisted terminal palette, font family, font size, cursor shape/blink, and 1k/5k/10k/50k scrollback settings. Visible stopped panes continue measuring and scrolling while input, paste, and focus stay disabled; route-hidden and transitional panes release resize/input ownership. GPUI image clipboard entries use the existing backend NSPasteboard repair seam before injecting Ctrl-V, while ordinary text paste and the human-verified marked-text path remain unchanged.
- Project placement remains the node-sidebar drag/reparent/remove flow landed in the first M4.3 half; no duplicate topbar move affordance was invented because current `main` has none. Deliberate prerequisite-bound seams remain explicit: copied-file-path paste and the real app-wake bridge await M3, Cmd-click URL opening awaits the Phase 5 opener, duplicate-subject/secondary-window overlays await M4.6's window adapter, and the Settings Agents destination currently opens the existing takeover until M4.6 supplies the pane. Runner editing remains M4.4; archived/secondary Back destinations likewise render only when those owning routes exist.
- Gates: `make verify` green outside the sandbox after the known MCP temporary Unix-socket test hit `EPERM` inside it; `cargo test -p runner-terminal` green; `git diff --check` clean.

## 2026-08-20 — M4.4: runners, crews, and project entry (in flight — checkpoint)

- Ported `main`'s paginated runner/crew list operations into `runner-backend` (closing the M3 pagination seam), then assembled section D on the M4.2 widgets: shared list-page scaffold (debounced search, count, loading/error/empty/no-match, pager), Runners list/detail/Create Runner modal/edit drawer, Crews list/create/delete/editor with inline name/goal/conventions editing, Add/Edit Slot with runtime/model/effort layering, Set lead, Remove, and slot drag ordering, plus a project-entry audit against the M4.3a sidebar. Also in this branch: bundled Inter + OFL, in-app icon asset, TextField mouse selection, and a sidebar `WorkingDirField` argument-order fix.
- Jason's daily drive surfaced a performance thread with three root causes, found and fixed in order: (1) a **route-blind terminal repaint storm** — every PTY output chunk woke the root entity regardless of route, so any streaming chat continuously rebuilt whichever page was active; fixed by gating the wake notify on `route.terminal_visible()`. (2) a **destructive `load_crew_editor`** — every crews-flavored refresh recreated the inline name TextField, clobbering focused text/caret; reloads are now non-destructive (field entities persist, `TextField::mark_clean`, identity-guarded recreation). (3) an **unconditional root notify in the Add Slot filter observe** — every keystroke rebuilt the whole app tree even when nothing changed; now change-guarded, with the same guard shape swept across the crews/runners field observes.
- **Open — the problem is only partly solved.** Typing lag persists in the Crew Editor's Add/Edit Slot drawer; the page's own inline inputs now feel fine (Jason, 2026-08-20). `RUNNER_FRAME_TRACE=1` eprintln frame instrumentation (root render / crew editor / add-slot drawer / sidebar) is committed temporarily for a measured pass — remove before the PR.
- Review state: working-tree review CLEAN including the perf follow-ups; `make verify` and `cargo test -p runner-app` green. Reviewer's pre-PR list stands: split out the five separable fixes as their own commits, two cosmetic nits (drag-handle tooltip, kebab opacity reveal), a departures list for the PR body, and Jason's three visual confirmations (conventions overlap, sidebar scrollbar short/long, streaming-chat repaint feel).
- Checkpoint commit pushed on `feat/m4-4-entry-points`; PR deferred until the drawer lag is resolved and confirmations pass. GPUI lessons distilled to the memory repo (`coding/graphics/gpui/entity_identity_invalidation.md`): entity identity is manual, notify scope is rebuild cost — both now standing reviewer hunts for M4.5/M4.6.

## 2026-08-20 — M4.4 landed: drawer lag root-caused, sidebar flattened

- The remaining drawer typing lag is closed. Measured pass (`RUNNER_FRAME_TRACE` + a deferred end-of-frame probe + macOS `sample` on the live process): ~75% of every frame sat in taffy flexbox layout, worst under the drawer overlay's subtree — because the dev profile compiled all dependencies at opt-level 0, and taffy's generic layout code monomorphizes inside gpui-ce, so optimizing taffy alone wouldn't help. Fix: `[profile.dev.package."*"] opt-level = 2` (gpui-ce/taffy at 3) in the workspace `Cargo.toml`. Drawer frames 98→17ms, page frames 15→5ms; Jason confirmed by feel. Workspace crates stay at opt 0, so incremental dev rebuilds are unaffected; deps compile optimized once.
- The measured pass also flushed out a guaranteed crash: the Add Slot query observer called `root.update()` while `NativeRoot` was already leased by the observe callback — abort on the first keystroke in the runner search field. Fixed to use the callback's own `&mut self`. Standing rule: never `Entity::update` the observing entity inside its own `cx.observe` callback.
- Sidebar grouped tab tree (chevron header + nested per-pane member rows) removed as stale design (Jason, canvas node e2vrOO): `main`'s `ChatTabGroup` renders one flat row per tab, split icon + joined label, click activates the focused pane's session. Dead collapse machinery and the `sidebar_collapsed_tabs` setting dropped (old settings.json files still load).
- Crew goal/conventions inline editors now open at the size of their text: `TextField::auto_grow(max_rows)` makes the textarea's scroll container content-driven between base rows and a cap, resolved in the same layout pass (a first measure-then-resize attempt visibly flipped; replaced). Goal 4→12 rows, conventions 6→24.
- Frame-trace instrumentation removed after the pass. Deferred to follow-ups: the reviewer's two cosmetic nits (drag-handle tooltip, kebab opacity reveal). Landed as five focused commits on top of the checkpoint; `make verify` green.

## 2026-08-20 — M3.6: router/inbox backend parity (same day)

- Ported main's router/inbox group at `276a3a4` into `runner-backend` (crew mission, one review round; single must-fix was a stale duplicate of a test main had renamed): reservation/outbox/listener delivery contract, 0041 draft-aware delivery gate (draft byte buffer replaces the `local_input_pending` latch; 10-minute abandonment backstop; Ctrl-U manual clear), deferred mission nudges during local input (`828c9fc`), reconciliation tick, inbox sender filtering. Router/event-bus files byte-identical to main apart from the Tauri→EventChannel and `commands/`→`ops/` seams; main's router (106) and manager (81) suites taken wholesale — 529 backend tests, `make verify` green. Landed directly on `gpui-nightly` (`3341e9b`; Jason: skip the PR).
- **Carry into M4.5** (from review): (1) `inject_direct_stdin` parks on the DeliveryGate condvar and runs `flush_outbox`/`notify_delivery_event` inline — in GPUI that path is reachable from the render thread (`chat.rs` on_key_down, `terminal_ime` commit_text). Inert today because chat panes host direct chats only and direct chats never register delivery listeners, but the M4.5 slot-terminal work must move mission-slot terminal writes off the main thread or bound the gate wait. (2) `router/delivery-blocked` is an inert emit until M4.5's InboxBlockedPill consumer exists (`SidebarRefreshKind::for_event` ignores unknown names, so no mis-scoped refresh meanwhile).
- Next: M3.7 mission-feed backend (0042/0044 backend halves), then M3.8 update-checks/misc, then M4.5.

## 2026-08-20 — M3.7: mission-feed backend parity (same day)

- Ported main's mission-feed backend group at `276a3a4` (crew mission, two review rounds): `mission_post_human_message` (0042's backend half — roster-validated `to`, MCP tool + server registration) and the `7157efc` archive ops half round 1 missed (completed/restored missions visibility-archive without lifecycle mutation or a second terminal event; `repo::mission::archive_if_stopped` gains its caller; already-archived races report cleanly). 0044 confirmed frontend-only — its backend seams were already present. Main's tests wholesale; 536 backend tests, `make verify` green. Landed as a local `--no-ff` merge of `feat/m3-7-mission-feed` (session flow: no PRs).
- **Carry into M4.5** (inherited parity holes, named before the composer surfaces them): (1) `to` validation has three disagreeing sources after a mid-mission crew edit — `post_human_message` checks the live crew table, the router nudges the mount-time launch roster, the CLI validates `roster.json`; (2) human-channel text has no size cap where `runner msg post` caps at 32KB.
- Process note: two stray single-token corruptions appeared in the working tree during this slice (`fixtures.rs` `FixtureHeader` deleted mid-review, `router/mod.rs` `SessionDeliveryListener`→`SessionDeliveryLWtener` after review close) — neither authored by the crew or Claude; both restored to HEAD with Jason's OK and re-verified. Likely stray keystrokes into an open editor; watch for recurrence.
- Next: M3.8 update-checks/misc backend (periodic update checks, macOS wake bridge, stale unread cleanup, paste file paths) — the last M3 slice before M4.5.

## 2026-08-20 — M3.8: misc backend parity — M3 complete (same day)

- Ported the last M3 group at `276a3a4` (crew mission, CLEAN in one round): `de53950` wake bridge (byte-identical observer + tests; installed from `Application::run` app-init so NSWorkspace registration stays on the main thread; emits `app/woke` via EventChannel), `328da9c` stale-unread startup clear (repo logic pre-existed from the node catch-up; boot hook wired; `eprintln!` over inert `log::` calls matches bootstrap convention), `a8e7cf3` pasteboard file-path op (`ops::session::session_clipboard_file_paths`; NSURL decode preserved). Update checks (`30bdbf6`) excluded per the 2026-08-20 re-scope. Reviewer nits folded in at landing. **All M3 backend slices are now done — M4.5 is unblocked.**
- **Carry into M4 (from review):** (1) `app/woke` is an inert emit — main's consumer was #360's WebGL atlas clear, which has no GPUI analogue (gpui-ce 0.3.3 has no wake observer or atlas invalidation); keep as insurance until a Metal-side stale-glyph symptom appears after real sleep/wake, or drop it — Jason's call. (2) Paste-file-paths consumer: the dead zone in `chat.rs::on_paste` is in TWO places — `read_from_clipboard()` returning `None` (the actual Finder file-copy case; gpui-ce's clipboard reads only text/image flavors) AND the no-String early return; wiring only the latter misses the common case. `ClipboardEntry::ExternalPaths` is drag-and-drop-only, so the ported op is genuinely required. Port main's `terminalPaste.ts` quoting (`shellQuotePath`, space-joined) rather than re-deriving, and inject via stdin (no trailing Enter).

## 2026-08-20 — M4.5a: native mission workspace (in flight)

- Built the first native mission-workspace checkpoint against `main`'s React surface: mission navigation/load state, feed and slot tabs with remembered terminal selection, mission controls and paused semantics, retained slot terminals with lifecycle/archiving precedence, duplicate-subject and delivery-blocked treatments, plus the resizable runners/mission rail and metadata actions. The 2026-08-20 scope amendment keeps EventFeed, MissionInput, Start Mission, and Reset confirmation in this same M4.5 working-tree slice; they follow this reviewed checkpoint rather than a separate M4.5b mission.
- Closed M3.6's main-thread DeliveryGate carry-note with an explicit terminal input mode. Direct chats retain inline writes, while every mission slot attaches with `UserInputMode::Queued`: key, IME, paste, and Enter-based clear-input bytes enqueue immediately onto one ordered worker per session, and only that worker calls `inject_direct_stdin`. DeliveryGate waits and its inline outbox flush/event notification can no longer park GPUI's render thread; asynchronous write failures return through `session/input-error` and surface in the mission error banner. A regression test pins the non-blocking enqueue/error-event contract. The initial brief's Ctrl-U parenthetical was corrected during review: backend `classify_local_input` treats Ctrl-U as activity-only, while Enter emits `InputCleared`, flushes the parked outbox, and matches main's `Clear input ↵` affordance (Jason, 2026-08-20).
- Mission feed/slot tabs share the existing native pane actions, so the default Cmd+[ / Cmd+] bindings cycle backward/forward through Feed and every open slot while preserving pointer selection, close, and last-terminal restoration.
- At Jason's request (2026-08-20), the native mission tab strip intentionally gives Feed and every slot one fixed 136px width; main sizes both tab types to their contents. Slot handles truncate inside that shared geometry and retain their full value in a hover tooltip.
- Ported EventFeed replay/live projection and grouping, runner/target/time/goal treatments, GFM-style message structure and URL opening, signal disclosures, ask cards, mission divider, new-message/autoscroll behavior, plus the mission-wide paused overlay. Lagged broadcast receivers now replay and merge/deduplicate instead of silently dropping the missed interval.
- Native feed Markdown is intentionally a dependency-free subset rather than a full CommonMark engine: headings 1–3, hard breaks, flat ordered/unordered lists, quotes, fenced code, rules, tables, emphasis/strong/strikethrough, inline code, Markdown links, and angle-bracket autolinks are covered. Bare URL autolinking, deeper headings, nested lists, and full CommonMark delimiter rules remain a parity carry so the later sweep does not mistake the boundary for complete GFM support.
- Ported MissionInput with the shared IME-owned textarea, channel/direct delivery, live-roster `@` picker, target chip, mouse and keyboard selection, and React-equivalent Enter/Shift-Enter/Escape/Backspace semantics. The composer posts through `mission_post_human_message`; M3.7's live-crew/router-launch-roster/`roster.json` disagreement and uncapped human-channel size remain backend contracts rather than UI workarounds.
- Ported the Start Mission modal from the sidebar, project menu, and Crew Editor entry points with project/default cwd precedence, launchability/session count, native folder selection, inert Advanced disclosure, and sized initial spawn. Ported the amber Reset confirmation and fresh workspace/session/event reattachment flow. Jason explicitly chose the existing unsized reset operation with a documented gap: `mission_reset_impl` forks at the backend default 80×24, so the first real pane resize can trigger `clears_on_resize` and purge agent opening output. M5 carry: add the native equivalent of main's sized reset path (`mission_reset_impl_with_size`, mission fork/grid hint storage and producer, plus the unsized MCP consumers) rather than hiding the risk in UI code.

## 2026-08-20 — M4.5 landed; CI timing accommodation; resume-width bug found

- M4.5 landed as a local merge of `feat/m4-5a-mission-workspace` (~6.9k insertions; full stage in one mission per Jason's scope amendment — details in the in-flight entry above). Jason drove a live smoke-fix loop during review: uncolored SVGs (gpui `svg()` needs its own `.text_color()` — parent text color does not cascade; fixed class-wide), unified 136px feed/slot tab width (deliberate deviation from main's content-sized tabs), Cmd+[/] mission tab cycling.
- CI red since the M3.6 push explained: the verbatim-ported router/manager timing tests fail consistently on GitHub's shared macOS runners (millisecond sleeps oversleep several-fold; main's CI has not been running, so these were never exercised there). Fixed by scaling `wait_until` timeouts and elapsed budgets 10× under `CI=1` — local budgets stay at main parity; recorded as a deviation for the M5 sweep and worth upstreaming to main when its CI returns.
- Smoke found the next blocker: **resumed claude sessions render doubled/torn lines in slot terminals** — sessions spawn/resume at the hard `DEFAULT_PTY_SIZE` 80×24 (no sizing chain), claude replays its transcript at that width, then the real pane size arrives and the repaint+reflow leaves the mis-wrapped history behind. Root cause is the un-ported #367 `size_source`/fork-size chain (main's #167 resume PTY sizing, #314 sized mission respawns, #372 backend-started fork sizing — exactly the four tests the M3.6 review flagged absent). Fix mission next: port the chain + wire slot/pane size hints at mission start/attach/resume. Zed comparison checked and rejected as a lead: Zed uses alacritty's own tty (not portable-pty) but avoids the bug by never spawning at a wrong size and never replaying transcripts — the fix is our own already-shipped width chain, not a PTY-stack change.

## 2026-08-20 — Width-chain fix landed (same day)

- The resume/fork width chain landed (crew mission on `fix/resume-fork-width-chain`, local merge): main's #167/#314/#372 `size_source` group ported (hint → persisted `last_cols`/`last_rows` → default, with the source logged per fork), `mission_reset` gained the sized path the M4.5 entry flagged as an M5 carry (`mission_reset_impl_with_size` + fork/grid-hint storage), and the GPUI shell now feeds a live grid hint each frame so slot attach, mission reset, and launch-time resume all spawn at the pane's real size. Launch-time resume was split from manual resume (`resume_on_launch`) so it refuses the fresh-conversation fallback instead of silently starting over. The four manager tests the M3.6 review flagged absent are restored. `make verify` green (exit 0); Jason confirmed the doubled-lines resume artifact is gone.
- Smoke immediately surfaced the next issue: **mission-slot terminal scrollback is stuck** — the wheel does not reach session history in a claude slot. Root-caused and fixed the same day (next entry).

## 2026-08-20 — Terminal wheel reporting; width-chain follow-ups (same day)

- **Scrollback-stuck root cause**: not a scrollback bug — a missing terminal capability. claude (and codex) enable SGR mouse reporting (1000/1006 — the reason `KEEP_RESUME_SEAM` disables 1000/1002/1003/1006), so in the Tauri app xterm.js automatically forwarded wheel events to the agent as mouse reports and the agent scrolled its own transcript. The GPUI port always called `scroll_display` on alacritty's local viewport, which a mouse-capturing TUI ignores — the view appeared frozen. Fix: `mappings::encode_scroll` routes the wheel the way a real terminal does — mouse-mode apps get SGR (or X10 fallback) wheel-button reports through the user-input path (queued worker on mission slots), alt-screen apps with DECSET 1007 get arrow keys, plain apps keep local viewport scrolling, and shift+wheel forces the viewport (xterm convention). Reports carry cell (1;1): the single-transcript agent TUIs we host ignore wheel coordinates, so pane-geometry plumbing was deliberately skipped (noted for revisit only if a multi-pane TUI ever needs it). Three routing tests added.
- The width-chain mission's post-landing review round closed CLEAN and its follow-ups landed as their own commit: grid-hint sync gated to the primary mission route/window (a synthetic estimate can no longer overwrite a measured pane hint from other routes or secondary windows), `mission_fork_size` tightened to `pub(crate)`, main's #366 log-placement comments restored. Reviewer's recorded known property: while a mission is open with the Feed tab active, the hint falls back to the estimate rather than a cached slot measurement — strictly better than 80×24, revisit only if it ever needs to be airtight.
- Process note: the first width-chain landing happened while the crew's review round was still open — the coder preserved its uncommitted follow-ups across the branch switch and the loop closed cleanly, but landing mid-review created avoidable cross-talk (the reviewer initially flagged the operator's wheel fix as an out-of-scope ride-along). Rule for next time: check mission state before landing, or tell the crew the tree is being landed.

## 2026-08-20 — Terminal font: Meslo Nerd Font option (same day)

- Terminal styling questions closed out. The claude splash-vs-banner difference Jason spotted is claude's own one-shot post-update release-notes screen (keyed on `lastReleaseNotesSeen` in `~/.claude.json`), not a rendering difference. The real styling gap against Zed is configuration: our Menlo 13 / 1.4 line-height / `#15161b` carbon palette is faithful to main (verified — main's font stack resolves to Menlo 13 on machines without its preferred Nerd Fonts), while Zed ships its own IBM Plex Mono variant at a larger size.
- Added `TerminalFontFamily::MesloNerdFont` → `MesloLGSDZ Nerd Font` (Jason's ghostty font; Apache-2.0 Menlo descendant with dotted zero + Nerd Font glyphs, family name verified against the installed TTF). Jason's dev settings now use it at 14px. Deliberate deviation boundaries: the shipped default stays Menlo for main parity; making Meslo the default and bundling the TTFs into the binary (license permits) are deferred to the M4.6 Terminal settings pane and Phase 5 packaging. Side effect: a Nerd Font family closes the parity gap where main's CSS fallback stack could pull Nerd-glyph coverage but GPUI's single-family font setup could not.

## 2026-08-20 — M4.6a settings core

- Deliberate native deviation requested by Jason during the visual smoke pass: the General pane's long default-working-directory value ellipsizes while its input is unfocused instead of hard-clipping at the right edge like `main`'s HTML input; focus restores the complete editable path, and persistence always reads the untouched full buffer. The shared Browse button retains `main`'s medium size because its 34px bordered height aligns exactly with the native 34px input; the apparently oversized screenshot was taken at 130% app zoom.
- Deliberate shared-menu improvement requested in the same smoke pass: popover and context-menu rows use the visible `sidebar_selected` surface and synchronize pointer hover with keyboard highlight. `main` uses `line` over `raised`, tokens that are identical in both shipped dark themes, so its hover is invisible there; light themes already had visible hover. The replacement stays visible in all four variants but moves lighter than the surface in Carbon and both light themes and darker in Catppuccin Mocha. This affects every consumer of the two shared menu primitives, not just Settings.
- Jason superseded the earlier fixed-width mission-tab deviation during this smoke pass: Feed is compact and content-sized again, while runner tabs size to their icon, handle, and close control with the shipped 140px handle cap. This restores `main`'s tab geometry and keeps long handles truncated without forcing short handles into a 136px box.
- Jason also approved a deliberate terminal-font deviation from `main`: new settings default to bundled MesloLGS Nerd Font, with Menlo as the only alternative. All four MesloLGS NF faces and the Apache-2.0 license ship with the app; legacy unavailable font selections normalize to Menlo, while existing Menlo users remain on Menlo.
- Two visual smoke corrections apply across their shared primitives: focused terminal cursors now paint at the terminal theme's full cursor color instead of 55% alpha, and header Stop controls use a 15px square glyph inside the unchanged 28px hit target. The latter is a Jason-requested 2px increase over `main`; pill controls and the other header lifecycle icons keep their shipped sizes.
- The runner-session rail's remaining source drift was removed in the same comparison: the header now keeps avatar/handle/badge as one truncating group opposite the fixed PTY control, rail monospace copy uses `main`'s JetBrains-first UI mono face, session keys use the shipped 14px snug line height and 6px copy gap, and the PTY control exposes its tooltip. GPUI has no letter-spacing style, so section-label tracking is approximated with Unicode thin spaces.
