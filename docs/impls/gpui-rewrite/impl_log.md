# GPUI rewrite — program log

Progress record for the whole gpui-rewrite program ([README](README.md)), from the Phase 1 spike onward. Newest entries at the bottom; keep entries short: what landed, deviations, carries, blockers. Compacted 2026-08-21: same-day entries merged, gate boilerplate dropped — every landing below passed `make verify` (check + workspace tests + clippy `-D warnings` + fmt) and the terminal fixture corpus unless an entry says otherwise, and every mission was reviewed CLEAN on the working-tree diff before Jason daily-drove it.

## Current state (update with each entry)

- **Branch**: `gpui-nightly`. Landed through PRs #407–#425 until 2026-08-20, then by local `--no-ff` merges of task branches (PR-less landing adopted for rewrite velocity). Version `0.6.0-nightly` since 2026-08-20 (`runner-app`/`runner-backend`/`runner-terminal` in lockstep); `main`'s Tauri line stays 0.5.x.
- **Done**: Phases 1–3 (spike GO, app-core extraction, walking skeleton — July); M0 gpui-ce swap, M1 repo-and-below parity, M2 terminal split (2026-08-17); M3.1–M3.5 session hardening + runtimes, crate renames, terminal-pane IME, Start Chat modal, M4 inventory, M4.1 app shell, M4.2 widget set (2026-08-18); M4.3 sidebar + direct chat (2026-08-19); M4.4 entity pages, M3.6–M3.8 (**M3 complete**), M4.5 mission surfaces, width chain, wheel reporting, M4.6a settings core, procedural glyphs (2026-08-20); M4.6b shell split, PTY/grid width fix, M4.6c keymap registry + command palette, M4.6d Agents/MCP/Archived panes, M4.6e multi-window, M4.7 terminal selection/copy + file-path paste, M4.6f Sparkle updater + packaging + nightly workflow + Updates/Diagnostics/About, M4.8 parity fix-ups + inventory re-audit — **M4 complete, exit gate passed** (inventory 75 done / 0 partial / 0 missing; 2026-08-21).
- **Nightly channel live** (2026-08-21): `0.6.0-nightly.20260821.1003` (run 32469468215, 27 min cold) installed by Jason; `…1018` (dispatch) arrived through Sparkle and relaunched — updater proven native→native; Jason's daily driver is now the nightly and the Phase 6 clock has started. `releases/latest` verified still on `v0.5.2`.
- **Current nightly**: `0.6.0-nightly.20260823.0341` (**first universal build**, arm64 + x86_64 — M6.17; run 32615982200 from `4fa233d`). Previous: `0.6.0-nightly.20260822.1633` (M6.18 silent update checks + RECENT / Archived nav; run 32585065476 from `8ab0807`, arm64 — the last single-arch build before M6.17); `0.6.0-nightly.20260822.1305` (M6.13 feed text + M6.12 audit + M6.3 backend items; run 32574751856 from `b8771ea`); `0.6.0-nightly.20260822.0744` (M6.9 update hint + mission reset removed; run 32560379906 from `434bf81`, 10 min warm); `…0822.0427` (M6.6 + M6.8 + M6.10 + M6.11 + Claude fullscreen default; run 32551720876 from `5b72acc`).
- **Nightlies are cut on demand** since 2026-08-21 evening (`gh workflow run nightly.yml --ref gpui-nightly`); a push to `gpui-nightly` only runs CI. Decision 12 amended.
- **Next — the cutover (plan decision 14, 2026-08-23):** Cutover 1, the native release workflow ([briefs/cutover-1-release-workflow.md](briefs/cutover-1-release-workflow.md)) → the human's `dry_run` dispatch into a throwaway draft → hand bump `0.6.0-nightly → 0.6.0` in the three crates → tree-swap merge into `main` → tag `v0.6.0` → the workflow fills the draft → release notes → publish (the switch: `releases/latest` flips) → bridge check by installing v0.5.2 from the releases page and letting it hop into native 0.6.0 → bump `main` to `0.7.0-nightly` and cut the first post-cutover nightly. Post-GA, as nightlies: M6.16, M6.19, M6.20, M6.15, then M6.2 hooks, M6.7, M6.4, the M6.3 remainder. Landed 2026-08-23: M6.18 (`6b9bbe4`), M6.17 (`59a8b87`).
- **M4 inventory**: [m4-surface-inventory.md](m4-surface-inventory.md) — 75 current-`main` surfaces (3 functional-but-unstyled, 22 partial, 50 missing at entry), ready backend ops, Phase 5 prerequisites.
- **Parity watermark**: `origin/main` fully ported for repo-and-below as of `1b7ee92` (2026-08-17); backend feature groups ported at `276a3a4` (2026-08-20). `main` has not moved since and will not (decision 11), so `276a3a4` is the final watermark; M5 closed by plan decision 13 (2026-08-21).

## Deviations from `main` (kept on purpose; with M5 closed this register is the parity record)

Backend, under decision 11 and recorded per entry below: orphan-sweep failure is fatal at boot (M3.1); router/manager timing tests scale budgets 10× under `CI=1` (M4.5); mission forks honor a resize persisted before the PTY existed and terminals attach at the recorded PTY size (2026-08-21); Tauri update checks (`30bdbf6`) are not ported — Sparkle replaces them.

Product-visible, each requested or approved by Jason: bundled MesloLGS Nerd Font is the default terminal font with Menlo the only alternative (M4.6a); popover/context-menu hover uses the visible `sidebar_selected` surface with pointer/keyboard highlight in sync (M4.6a); the General pane's default-working-directory value ellipsizes while unfocused (M4.6a); header Stop glyph is 15px, 2px over `main` (M4.6a); an expanded project with no children says "Empty" (M4.6c); the Agents executable-override row's Browse button carries no ellipsis (M4.6d); New chat is ⌘N and New window is ⇧⌘N where `main` binds ⌘T / ⌘N (decided 2026-08-21, applied by M4.6e; `AGENTS.md`, `arch.md`, feature 60, and the inventory updated the same day); the live window set, last normal frames, restorable routes, and focus rank continuously checkpoint to `window-layout.json` and restore after every graceful quit, extending `main`'s main-only 0027 contract without changing `window-state.json` (Jason, M4.6e daily drive); chat headers use a 12px icon/title gap, 8px right insets, and a 1px title ink offset (M4.3); the Updates pane is slim — version, Check for updates, automatic-check toggle, last checked — and Sparkle's standard sheet owns the available/download/relaunch flow, replacing `main`'s in-pane state machine and the sidebar `UpdatePromptCard`, which existed only because Tauri's updater drew no UI of its own (decided 2026-08-21, applied by M4.6f; the app-menu "Check for Updates…" item is the macOS convention); the Terminal pane has no scrollback preference — every terminal keeps a fixed 10,000-line history where `main` offers 1k/5k/10k/50k (Jason, M4.6f; old `ui-settings.json` keys are ignored); the M4.3 chat-header treatment (12px icon gap, 8px insets, 1px title nudge, flex title group) now also applies to the mission header through a shared `WorkspaceHeader`, and the mission rail animates open/closed like the chat side panel (Jason, M4.6f); ⌥+letter chords encode ESC plus the plain key so readline M-b/M-f/M-d work, where `main` sends the accented `key_char` — a deliberate improvement over frozen `main` (Jason, M4.8).

Product-visible, decided 2026-08-22 (M6.9, `518a7bf`): `main`'s in-app update prompt — the Arc-style "New Runner version available" pill above the Settings row with its hover card, per-launch dismiss and auto-install checkbox — is replaced by one accent `circle-arrow-down` icon at the right end of the Settings row, shown while Sparkle has found an uninstalled update, with a version tooltip and a click that resumes Sparkle's own install prompt; the Updates pane shows the same version (designed in `runner.pen` `cmp/SidebarC`, Jason's call; the M4.8 inventory had missed the card). **Mission reset is removed entirely** — the menu item and amber dialog, `ops::mission::mission_reset_impl*`, `repo::mission::reset_to_running`, `repo::session::archive_all_for_mission`, and the `mission_reset` MCP tool: almost nobody used it and it was the only irreversible data-loss action in the app (it deleted `events.ndjson`); archive + start a new mission covers the need. Existing DBs keep the archived session rows old resets left behind — no migration, the archived-list guards already filter them. Also recorded: the hint clears on `didAbortWithError` as the brief mandated, so a transient scheduled-check failure can hide a previously found version until the next successful check.

Product-visible, decided 2026-08-22/23 (M6.18, `6b9bbe4`): the sidebar section `CHATS & MISSIONS` is `RECENT` (the command palette already said RECENT); Settings drops its one-row ARCHIVED nav group — the Archived pane is the last row of the App group, labelled "Archived" in the nav and as the page heading, a structural deviation from `main`'s settings nav (inventory row E11's pane is unchanged inside); and a background update check never shows Sparkle's sheet — the delegate declines `SPUUpdateCheckUpdatesInBackground` after lighting the Settings-row hint, so the sheet appears only from the hint click, the Updates pane, or the app menu, where `main`'s Tauri updater had no scheduled-check UI to suppress.

Release pipeline, decided 2026-08-22 and landed 2026-08-23 (M6.17, `59a8b87`): nightlies and releases are one universal DMG — arm64 and x86_64 slices `lipo`'d into each of the three binaries — where `main`'s Tauri line shipped two per-architecture DMGs; Sparkle never looks at architecture, so the feed and the updater are unchanged, and the Tauri bridge manifest will point both platform keys at the one tarball. The sidecars are signed before the main executable because a lipo'd binary carries the linker's ad-hoc signature on its arm64 slice only.

Platform-bound: preferences do not migrate from the Tauri app — `main` kept every UI setting in the webview's localStorage, the native app reads `ui-settings.json`, so a first launch starts from defaults (both apps default resume-on-launch to off, which is why sessions stamped by the last Tauri quit were not auto-resumed on the first nightly boot; set preferences once, native→native restarts honor them from then on — Jason, 2026-08-21); section-label tracking approximated with thin spaces (GPUI has no letter-spacing); keymap overrides persist the layout-dependent `Keystroke.key` rather than a physical key code (separate store from React, no compat break); wheel reports to mouse-mode TUIs carry cell (1;1); terminal mouse reporting deliberately lets Shift force local selection on macOS, following the M4.7 contract and native terminal convention rather than xterm 6.1.0-beta.220's macOS default; M4.7 implements the requested button-motion subset of DECSET 1003 but not no-button hover reports, and DECSET 1005 stays on the legacy 223-cell coordinate limit while 1006 carries arbitrary coordinates; native terminal paste checks AppKit file URLs before GPUI's text entry because Finder exposes a copied file's basename as `public.utf8-plain-text`, whereas `main`'s webview gets file/text precedence from `DataTransfer`; feed Markdown is a dependency-free subset (no bare-URL autolinks, nested lists, headings 4–6, or full CommonMark delimiter rules) — a parity carry, not complete GFM; gpui-ce has no per-window hide, so closing `main` destroys that root while explicit quit mode keeps the process alive and Dock activation or ⇧⌘N recreates the `main` window (M4.6e); unlike `main`'s retained DOM chat surfaces, a GPUI root drops its terminal-view attachments when its route, tab, or ownership changes because `TerminalBridge` has one view attachment per session, while the process-wide PTY stays live and a returning owner replays the bounded output ring (M4.6e).

## Standing GPUI rules (learned here; every brief cites them)

- Never `cx.new` a stateful child in a render or data-load path; child entities are created once and reused. Entity identity is manual; notify scope is rebuild cost — a child change must not notify the root unless the root's layout depends on it (M4.4).
- Never `Entity::update` the observing entity inside its own `cx.observe` callback — use the callback's `&mut self` (abort on double lease, M4.4). A child must never read the shell on a path the shell can enter synchronously; route assignments funnel through `NativeRoot::set_route`, child→shell hops are `cx.defer`red (M4.6b, "B1").
- A window with no focused handle has an empty dispatch-context stack, so every contextual binding fails there, including negated predicates such as `!ChatSplit`; every route a root can initially render must focus a tracked handle. Bind OS-level fallbacks with no context when they must work independently of surface focus (M4.6e daily drive).
- A no-context key binding receives the deepest dispatch precedence and therefore beats every contextual binding for the same keystroke; compound fallbacks such as ⌘W must route their surface-specific behavior themselves rather than expecting a narrower binding to win (M4.6e daily drive).
- `window.current_view()` is valid only while rendering (prepaint/paint): it unwraps the rendered-entity stack, which is empty during event dispatch, so calling it inside a mouse/hover/key listener panics and — because the listener runs inside an AppKit callback that cannot unwind — aborts the process. Capture the owning `EntityId` at render time (`cx.entity_id()` in the view, or `window.current_view()` in prepaint as the terminal element does) and move it into the closure (nightly `…1306` crash loop, 2026-08-21).
- Never inject `ESC[2J` into a live grid: on the primary screen alacritty implements it as `Grid::clear_viewport`, scrolling the whole viewport into scrollback, so a synthetic clear duplicates history (M6.6). A TUI's own SIGWINCH repaint plus alacritty's reflow is the whole resize story, as in Zed.
- Terminals outlive views: a session's `Term` lives in the `TerminalBridge` registry for the session's lifetime and panes borrow it through leases; never rebuild a grid from a replay buffer, never attach from render (M6.8).
- A GPUI `div()` is `Display::Block` until `.flex()` is called. A block parent lays its children out at content height and ignores their `flex_1` / `min_h_0`, so a scroll chain (`frame → overflow_y_scroll container → sections`) must be `.flex().flex_col()` at every level for `min_h_0 flex_1` to mean anything; otherwise the viewport grows to its content, `max_offset` stays 0, the scrollbar never shows, and unclipped rows paint past the container (sidebar, M6.10 — the first diagnosis blamed the children). Clipping a root with `overflow_hidden` also shrinks its hitboxes.
- `svg()` needs its own `.text_color()` — parent text color does not cascade (M4.5). Deferred layers (selects, popovers, tooltips) cannot be created while already drawing a deferred parent — mount modals as the root's final direct overlay (M4.2). Explicit sizes beat flex measurement for rows whose children paint definite heights, and wrapped text inside an `items_center` column needs a definite-width non-flex text block (`w_full`, `min_w_0`, and `whitespace_normal`) or GPUI measures or paints it at intrinsic single-line width (M4.6c/e).
- Dev profile compiles dependencies at opt-level 2 (gpui-ce/taffy at 3) — taffy monomorphizes inside gpui-ce, so an unoptimized dep graph made every frame layout-bound (M4.4).
- Process: check mission state before landing a branch the crew is still reviewing, or tell the crew the tree is being landed (width-chain mission). Two stray single-token corruptions appeared in the tree during M3.7 from outside the crew — watch for recurrence.

## 2026-07-18/19 — Phase 1: terminal spike, verdict GO

- `crates/native-spike/` on `phase1-terminal-spike` (gpui 0.2.2 + alacritty_terminal 0.26 + portable-pty 0.9): PTY ⇄ `Term` event pump answering PtyWrite/ColorRequest/TextAreaSize, custom grid element (ASCII spans shaped together, wide/non-ASCII glyphs per cell at their own column origin), `EntityInputHandler` IME composer adapted from gpui's Apache-2.0 input example. Build prerequisite discovered: gpui's Metal shader build needs the Xcode 26 Metal Toolchain component. Zed's GPL terminal crates were architectural reference only.
- Fixture corpus started (spec 42 debt): `record-fixture` logs real PTY bytes to NDJSON, replay tests snapshot the grid — `claude-session`, `top-busy`, `width-torture`; pins wide-char spacers, combining marks, leading-spacer wrap, cursor-pinned reflow, alt-screen restore, scrollback clamp.
- Review fixes that still shape the code: the event thread must hold only Weak/side Arcs (an Arc cycle leaked the PTY child); OSC 10/11/12 named slots resolve through the palette, replies prefer live `Term::colors()`; only pure-ASCII spans extend (a narrow non-ASCII glyph re-introduces fallback-advance skew); IME marked-text selection offsets are relative to the marked string; composer editing moves by grapheme cluster.
- `package.sh` proved release build → .app → Developer ID codesign; notarization deferred to Phase 5 by Jason's decision. Spike committed `6d6ee2e`/`43d0b28`.
- Verdict (Jason, 2026-07-19): **GO on GPUI** — live claude session drives correctly, Pinyin in the native composer confirmed (the hard requirement). Known cosmetic artifact, block-glyph striping in the claude logo, later closed by procedural glyphs. Decision memo in [plan.md](plan.md).

## 2026-07-19 — Phase 2 app-core extraction; branch strategy; Phase 3 walking skeleton

- Phase 2 on `refactor/0031-app-core` off `main` (`461356e`): UI-agnostic core crate (then `crates/runner-app`, now `runner-backend`) — db/repo/model, session manager, event bus, router, MCP server, ~85 command bodies as plain fns over `AppCore`; Tauri wrappers one-line delegations; event fanout a bounded `tokio::sync::broadcast` (8192) with the Tauri layer as the single subscriber. PR #310 reads merged but Jason reset `main` afterwards; the extraction is consumed by merging that branch. Release skill bumps the core crate's version too.
- Branch strategy (Jason): the nightly branch, renamed `rust-ui-nightly` → `gpui-nightly`, is a Tauri-free pioneer line; composition merged `origin/main` (v0.3.17) plus the extraction, deleted `src/`, `src-tauri/`, and the JS toolchain; Makefile exposes `fmt`/`clippy`/`test`/`run`; CI is one `macos-26` Rust-workspace job targeting `gpui-nightly` and installs the Metal Toolchain. The repo split to `yicheng47/runner-gpui` (2026-07-19) was reverted 2026-08-17 — history in plan §Branch strategy.
- Phase 3: `crates/runner-native` (now `runner-app`) boots `AppCore` against the same SQLite DB as Tauri, lists direct chats, opens/resumes them in a live terminal with the IME composer. The Phase 1 deviation is repaired at the seam: `SessionManager` owns spawn/resume/input/resize/lifecycle, the native side only attaches a `Term` to the manager snapshot plus broadcasts (`/bin/cat` integration test).
- Smoke follow-ups: `make run` uses the debug profile and `com.wycstudios.runner-dev`; switching chats replayed TUI output at a hard-coded 100×30 — fixed by inheriting the outgoing rendered size (known limitation: shell runtimes do not repaint on SIGWINCH, so an inactive shell chat can keep an old-width snapshot); terminal-pane IME declared a pre-release requirement (done 2026-08-18). Open Tauri-line specs (features 05/19/21/24/37, impl 0019) archived unshipped; `arch/arch.md`'s stack section still says Tauri — doc debt.

## 2026-08-17 — Program restart: single repo, spec 0046, M0–M2, updater decision

- `gpui-nightly` restored as a long-lived branch on `yicheng47/runner` (`774aa35`), worktree `runner-gpui`; CI re-targeted (`dde41e7`). Impl 0046 (main-parity catchup) committed (`14e9680`); docs regrouped under `docs/impls/gpui-rewrite/`.
- **M0**: `gpui 0.2` → `gpui-ce 0.3`, a true drop-in (Cargo.toml + lockfile only).
- **M1**: first `make run` hit `no such table: tabs` — the shared dev DB was already at `main`'s v20 (0015 dropped `tabs`/`folders`), exactly as 0046 predicted. Ported wholesale from `origin/main` (merge-base `d9483ca`): migrations 0014–0020 + backfills, `db.rs`, `model.rs`, `repo/` (node.rs in, tab.rs/folder.rs out), `shell_path.rs`, `crates/runner-core`, `cli/`; 0001–0013 verified byte-identical. Adapter `ops/node.rs` ported verbatim; node-maintenance hunks applied to mission/project/session ops; resume adopts caller → persisted last size → default geometry. Sidebar minimally rewired onto tab nodes. Both dev builds share `com.wycstudios.runner-dev` — run one app at a time.
- **Updater**: Sparkle via the pulse pattern, Zed's `auto_update` rejected (plan §Phase 5).
- **M2**: `crates/runner-terminal` is the model half of the Zed-style split — `TerminalSession`/`TerminalBridge`, palette, fixtures/replay, and `mappings.rs` (keystroke/paste encoding as pure functions); gpui-free, PTY stays with `SessionManager`; `alacritty.rs` API isolation deferred until the view side needs it. `main.rs` split into `chat.rs`/`sidebar.rs`/`panes.rs` for parallel M3 lanes.

## 2026-08-18 — M3 numbering and session hardening (M3.1–M3.4)

M3 ran as serial codex-peer missions; numbering (cited by the plan): **M3.1** reaping `48d2231` `0dadea7` (PR #409) · **M3.2** resume seams + geometry `9f53047` `02e7502` `cb32720` `3f3bc04` (#410) · **M3.3** resize-storm coalescing `38af26c` `e78718f` (#411) · **M3.4** input latch + quit stamping `f926581` `c5b1ce4` (#414) · **M3.5** runtimes (`feat/0046-m3-runtimes`) · **M3.6** router/inbox (`3341e9b`) · **M3.7** mission feed · **M3.8** misc backend — the last three on 2026-08-20. Port guides: `main`'s impl docs 0032–0045 per group (plan §A4).

- **M3.1**: PTY stop verifies death and reaps with a SIGKILL process-group fallback; startup orphan sweep identity-checks recorded processes; unexpected slot exit or spawn failure cancels and reaps live siblings. `pty_runtime.rs` SHA-matches `main`. Deviation: `bootstrap.rs` makes `cleanup_orphan_processes_on_startup` fatal where Tauri logs-and-continues (matches `boot_core`'s fatal stale-row convention). `stop()` may block ~500 ms per stuck session.
- **M3.2**: resume emits an in-band clean seam (keep-ring runtimes) or RIS reset (purge runtimes) before the fork; bracketed-paste and mouse modes tracked; every spawn persists a concrete PTY size; stopped panes persist measurements without a live handle. Structured `SessionUpdatedEvent` with `mission_id`. Its deferrals — launch-claim finalization and resume-all refresh — were closed by M4.6a and M4.5.
- **M3.3**: width changes for full-repaint runtimes settle after 175 ms, collapse a storm to one ioctl/purge, rows-nudge repaint on a round trip, in-band `ESC[2J ESC[H` clear emitted under the state lock ahead of repaint bytes. Native mapping: `TerminalElement` measures from real prepaint bounds and refuses unplaced geometry; `PaneLayout` names one resize owner per session in a grouped tree. React's `PersistentSurfaces`/hidden-pool/`visibility:hidden` machinery has no native counterpart — retained `TerminalSession`s are models, not hidden surfaces.
- **Terminal-pane IME** (M4 pull-forward): per-session GPUI input handler — marked text as an underlined cursor-cell overlay, candidate bounds on the cursor cell, committed UTF-8 through `TerminalSession`; Pinyin human-verified. The direct-chat bottom composer (this line's IME workaround, never a `main` surface) was then removed; `composer.rs` deleted, `text_util` kept.
- **M3.4**: printable input and pastes latch pending input, Enter/Ctrl-C clear it, escape-prefixed protocol traffic is activity-only, failed writes roll back. Every quit path stamps `mark_running_for_resume_on_launch` before stopping sessions. Router consumption of the latch waited for M3.6; the launch-claim consumer arrived with M4.6a (boot clears claims when the toggle is off, takes them when on — no stale backlog).
- **Crate renames** (decision 7, one mechanical commit, PR #415): `runner-app` → `runner-backend`, `runner-native` → `runner-app`. Port mapping is `src-tauri/src/*` ↔ `crates/runner-backend/src/*`; **`runner-app` names the binary** — every brief says so.

## 2026-08-18 — M3.5 runtimes; Start Chat modal; M4 inventory; M4.1 app shell; M4.2 widgets

- **M3.5**: non-blocking executable discovery seeded from the persisted login-shell environment, `runtime/changed` on refresh/override/status, catalog resolution on every spawn kind; Qoder and TRAE end to end (registry, argv adapters, resume/rollout capture, MCP config, resize/resume policies); model/effort option tables in backend ops; direct-chat runtime/model/effort overrides, slot layering as `main`, Codex project trust pre-seeded before each Codex spawn.
- **Start Chat modal + pane close** (M4 pull-forward): GPUI modal matching `main`'s Runner/Direct flows over the discovery catalog, sticky auto-derived names, native folder selection, persisted mode; a Pulse-pattern text input gives the fields native IME. `main`'s empty-pane close with tree-collapse semantics.
- **M4 entry**: audited `origin/main` `src/` at `09532d2`, the native surface, and `design/runner.pen` (pencil MCP) → [m4-surface-inventory.md](m4-surface-inventory.md). Corrections: nodes are `project | tab | mission` (no folders, no Projects page); `main` ships four app palettes + three terminal palettes. Pencil conflicts recorded, not promoted. M4.4 (entity pages) swapped ahead of M4.5 (missions); Sparkle/logs/opener/window restore are Phase 5 prerequisites, not mock surfaces.
- **M4.1**: owned chrome (`mac_chrome.rs`, pulse patterns): hidden titlebar, traffic lights, drag regions, double-click zoom, fullscreen. Theme layer (`theme.rs` + `app_settings.rs`): Auto/Light/Dark over `main`'s four variants + terminal palettes, fonts, zoom, focus rings, scrollbars — Tokyo Night gone; `runner-terminal` stays gpui-free. `app_shell.rs`/`toast.rs`/`assets.rs`: route frame, sidebar slot (persisted width/collapse, hover preview, pin), settings takeover layer, queued toasts, loading/error boundary. Mid-mission Jason tightened decision 1: `src/` wins on visual treatment too; canvas-derived values re-derived from `src/`.
- **M4.2**: shared widget layer for inventory F1–F17 (buttons, cards/badges, avatars/presence, fields/textareas, model/cwd fields, selects, toggles/steppers, modal/drawer/confirm shells, popovers, tooltips, session controls, search/list/empty/pager, settings shells, scrollbars) after `main`'s components; Start Chat and existing surfaces re-based on it; IME seam preserved in `TextField` with regressions. Native settings persist default cwd, default runtime, enabled/disabled agents. Design sizes resolve through the window rem size so app zoom scales everything (fixed-pixel sizing was swept after M4.3's zoom testing).

## 2026-08-19 — M4.3: node sidebar and direct chat

- **Sidebar** (tasks 1–2, A6–A7): `main`'s node hierarchy — Workspace entries, brand/search trigger, pinned rows, project containers, root chats/missions, create/context menus, Start Project, project-scoped Start Chat, inline rename, pin/archive/remove, delete confirmation, drag reorder/reparent (hit-bound listeners, end/empty-scope targets); tabs and accordion groups with active/focused treatment, busy/unread priority, rollups, persisted collapse, focus-aware viewed watermarks. Archive kills run off the UI thread. Row type 13px at 100%, rem-based.
- **Direct chat** (tasks 3–6, B1–B11): `main`'s `RunnerChat` topbar (sidebar/layout/panel controls, group menus, stop/resume aggregation, lifecycle locking), the 200–480px runner/runtime panel with 200 ms ease and left-edge resize, restyled six-preset pane tree (hairline gutters, grouped headers with CHAT badge and scoped controls, ⌘[ / ⌘] cycle, ⌘W collapses the focused pane keeping the session), layout picker closes on pick/outside/Escape. Shared session overlay with `Archiving > Resuming > Starting > ended` precedence and React's settlement contract. Start Chat seeds project id + cwd ahead of runner/default fallback. Terminals apply palette/font/size/cursor/scrollback settings; image paste goes through the backend NSPasteboard repair seam. Window size restores from `ui-settings.json` (first launch 1440×900), clamped to the display; a handled double-click no longer also zooms via the shell drag region.
- Still prerequisite-bound after M4.3: copied-file-path paste consumer (M3.8 carry below), ⌘-click URL opening (Phase 5 opener), duplicate-subject/secondary-window overlays (M4.6e).

## 2026-08-20 — M4.4: runners, crews, project entry

- Ported `main`'s paginated runner/crew list ops (closing the M3 pagination seam) and assembled section D on the M4.2 widgets: list-page scaffold (debounced search, count, loading/error/empty/no-match, pager), Runners list/detail/create modal/edit drawer, Crews list/create/delete/editor with inline name/goal/conventions (`TextField::auto_grow`: goal 4→12 rows, conventions 6→24), Add/Edit Slot with runtime/model/effort layering, Set lead, Remove, slot drag ordering. Also bundled Inter + OFL, in-app icon, `TextField` mouse selection.
- Jason's daily drive surfaced a performance thread, root-caused in four steps: a route-blind terminal repaint storm (every PTY chunk woke the root — now gated on `route.terminal_visible()`); a destructive `load_crew_editor` recreating the inline `TextField` on every refresh (reloads now identity-guarded); an unconditional root notify in the Add Slot filter observe (change-guarded, swept across field observes); and the dev profile compiling dependencies at opt-level 0 — ~75% of each frame in taffy layout, fixed with `[profile.dev.package."*"] opt-level = 2` (drawer frames 98→17 ms). The measured pass also found an `Entity::update` inside its own observe callback (guaranteed abort on first keystroke). Rules recorded above and in the memory repo (`coding/graphics/gpui/entity_identity_invalidation.md`).
- Sidebar grouped tab tree removed as stale design: `main`'s `ChatTabGroup` is one flat row per tab; `sidebar_collapsed_tabs` setting dropped (old files still load). Landed as five focused commits (PR #425). Open cosmetic nits: drag-handle tooltip, kebab opacity reveal.

## 2026-08-20 — M3.6–M3.8: backend parity complete

- **M3.6** router/inbox at `276a3a4`: reservation/outbox/listener delivery contract, draft-aware delivery gate (**correction 2026-08-22**: `main` never implemented impl 0041's draft byte buffer or its 10-minute abandonment backstop — the latch is a bool and the park is unbounded; see the M6.1 brief), deferred nudges during local input, reconciliation tick, inbox sender filtering; router/event-bus files byte-identical to `main` apart from the EventChannel/`ops/` seams; `main`'s router and manager suites taken wholesale. Its main-thread DeliveryGate carry was closed by M4.5's queued input mode; the `delivery-blocked` pill consumer arrived with M4.5.
- **M3.7** mission feed: `mission_post_human_message` (0042 backend half, MCP tool + registration) and `7157efc`'s archive-visibility ops. **Open carries**: `to` validation has three disagreeing sources after a mid-mission crew edit (`post_human_message` checks the live crew table, the router the mount-time roster, the CLI `roster.json`); human-channel text has no size cap where `runner msg post` caps at 32 KB.
- **M3.8** misc: `de53950` wake bridge (installed from app init so NSWorkspace registration stays on the main thread; emits `app/woke`), `328da9c` stale-unread startup clear, `a8e7cf3` pasteboard file-path op (`session_clipboard_file_paths`). Update checks excluded (Sparkle). **Open carry**: `app/woke` is an inert emit — `main`'s consumer was a WebGL atlas clear with no GPUI analogue; keep as insurance or drop (Jason's call). The paste-file-paths consumer carry closed in M4.7: both terminal paste handlers share image → AppKit file URLs → text precedence, port `terminalPaste.ts`'s shell quoting, space-join multiple paths, and inject no trailing Enter; unlike `main`'s raw injection, path text uses the native terminal's normal paste path so bracketed-paste-aware TUIs also receive framing.

## 2026-08-20 — M4.5 mission surfaces; width chain; wheel reporting; Meslo

- **M4.5** (full stage in one mission, `feat/m4-5a-mission-workspace`, ~6.9k insertions): mission load/attach state, feed and slot tabs with remembered terminal selection, controls and paused semantics, retained slot terminals with lifecycle/archiving precedence, duplicate-subject and delivery-blocked treatments, resizable runners/mission rail and metadata actions, EventFeed replay/live projection with grouping/signals/ask cards/divider/new-message pill/autoscroll (lagged receivers replay and merge instead of dropping), MissionInput with the IME textarea, `@` picker, target chip, React-equivalent key semantics, Start Mission and amber Reset confirmation. Every mission slot attaches with `UserInputMode::Queued`: key/IME/paste bytes enqueue onto one ordered worker per session so DeliveryGate waits never park the render thread; failures surface via `session/input-error`. ⌘[ / ⌘] cycle Feed and slot tabs. Smoke fixes: uncolored SVGs class-wide. Tab sizing follows `main` (content-sized; a fixed-width interlude was reverted in M4.6a).
- CI had been red since the M3.6 push: the verbatim-ported timing tests oversleep several-fold on GitHub's shared macOS runners — budgets scale 10× under `CI=1`, local budgets stay at parity (deviation, worth upstreaming when `main`'s CI returns).
- **Width chain** (`fix/resume-fork-width-chain`, same day): smoke found resumed claude sessions rendering doubled lines — spawns/resumes forked at 80×24 and replayed transcripts at that width. Ported `main`'s #167/#314/#372 `size_source` group (hint → persisted size → default, source logged per fork), added the sized reset path (`mission_reset_impl_with_size` + grid-hint storage; closes the M4.5 reset gap), split `resume_on_launch` from manual resume (refuses the fresh-conversation fallback), gated the grid-hint producer to the primary mission route/window. Known property: with the Feed tab active the hint falls back to the estimate rather than a cached slot measurement. Zed was checked and rejected as a lead (it never spawns at a wrong size or replays transcripts).
- **Wheel reporting**: mission-slot scrollback appeared stuck because claude/codex enable SGR mouse reporting and xterm.js forwarded wheel events as mouse reports; the GPUI port scrolled alacritty's local viewport, which a mouse-capturing TUI ignores. `mappings::encode_scroll` now routes like a real terminal — SGR (or X10) wheel reports in mouse mode, arrow keys on alt screen with DECSET 1007, local viewport otherwise, shift+wheel forces the viewport.
- **Meslo**: the claude splash difference was claude's own one-shot release-notes screen; our Menlo 13 / 1.4 / carbon palette is faithful to `main`. `TerminalFontFamily::MesloNerdFont` (`MesloLGSDZ Nerd Font`) added as an option; it became the bundled default in M4.6a.

## 2026-08-20 — M4.6a settings core; procedural glyphs; audit, M6, M4.7, decision 11; M4.6 split

- **M4.6a** (landed `dc6034e`): settings takeover shell, General/Appearance/Terminal panes, launch auto-resume wired to the M3 claim seams. Smoke-pass deviations (all in the register above): ellipsized cwd value, visible menu hover surface across both shared menu primitives, bundled MesloLGS NF default with Menlo alternative (legacy font selections normalize to Menlo), 15px Stop glyph. Also: focused terminal cursor at full theme color; rail header truncation group, JetBrains-first UI mono, 14px session-key line height; `main`'s content-sized mission tabs restored.
- **Procedural terminal glyphs** (`8eeac96`): box drawing, blocks, shades, Braille, Powerline PUA, and legacy-computing cells paint as device-snapped geometry, transcribed from `main`'s `@xterm/addon-webgl@0.20.0-beta.219` table under its MIT notice (`crates/runner-app/LICENSE.xterm`); decorated cells stay on the text path. Directory tidy: surfaces under `src/surfaces/`, terminal rendering under `src/terminal/`.
- **Audit verified** at `9a2b218` (confirmed: lock across flocked append in `synthesize_wake_busy`, DEFERRED tx across filesystem work in `mission::start`, IMMEDIATE-tx tree repair on sidebar reads, missing indexes, O(n) feed append, no selection/copy; refuted: missing quit path; overstated: damage tracking, "zero unwrap"). Outcomes: **M6 consolidation** ([m6-consolidation.md](m6-consolidation.md)), **M4.7 terminal selection/copy**, **#347 hook-based status reopened** as M6.1, **decision 11** — `main` frozen, one line of work.
- **Shell split inserted as M4.6b** (Jason): `NativeRoot` was a ~70-field god-object with one `Render`, and multi-window needs app-scoped state outside it.

Mission split for the stage (lettering authoritative, Jason 2026-08-20):

| Mission | Plan tasks | Scope | Status |
|---|---|---|---|
| M4.6a | 1, 2 | settings takeover shell; General, Appearance, Terminal panes | ✓ `dc6034e` |
| M4.6b | — | shell split: app-scoped store, mission-workspace and sidebar as entities | ✓ `b76ed56` |
| M4.6c | 6, 3 | keymap registry + Keyboard Shortcuts pane, command palette + sidebar search wiring | ✓ `9cb159a` |
| M4.6d | 4 | Agents, MCP, Archived panes over the ready runtime/MCP and mission/session restore/delete ops | ✓ `a0d46f4` |
| M4.6e | 7 | multi-window: open/focus, route bootstrap, subject reporting, DuplicateSubjectOverlay, position/state restore | ✓ `30a728e` |
| M4.6f | 5 | Updates/update prompt over Sparkle, Diagnostics over file logging/reveal, About over the opener | ✓ `8dc2843` (one mission, a/b folded back 2026-08-21) |

Then M4.7 as its own mission, the M4 exit gate, and the nightly channel.

## 2026-08-21 — M4.6b shell split landed (`b76ed56`)

- Three reviewed checkpoints: **A** — `Entity<AppStore>` (`app_store.rs`) owns `AppCore`, bridge + waker, runners/nodes/projects/missions/sessions/activity and `AppSettings`, exposing monotonic per-domain revisions (split shell/mission/terminal settings fingerprints); surfaces observe it; PTY wake bursts coalesce into one revision bump gated on `route.terminal_visible()`. **B1** — `MissionWorkspace` is its own entity with its terminals and `Render`; **B2** — `Sidebar` likewise. `NativeRoot` keeps route, `TabSet`/panes, focus, modals, toasts, overlay placement, and the remaining `impl` blocks.
- Upward pattern: children hold `WeakEntity<NativeRoot>`, hops the shell could re-enter are deferred, routes funnel through `set_route` (the B1 rule above). Review caught a double-lease panic on ⌘[ / ⌘] with a slot tab open, mission terminals outliving the route, child terminal font size dropping the zoom multiplier, and mission overlays scoped to the child instead of window chrome — all fixed before smoke. Jason: "UI getting smoother."
- **M6 carries** (efficiency, no behavior change): sidebar and workspace repaint on every settings revision (could gate on a scoped fingerprint); three copies of the `core`/`settings`/`update_app_settings`/`refresh_store` helper set; the settings snapshots are manual field lists with no compile-time tie to `AppSettings`.

## 2026-08-21 — PTY and grid widths must never be guessed apart (`d101d72`)

- Symptom: a codex slot double-spaced — every full-width row followed by one stray background cell at column 0, i.e. alacritty wrapping rows one cell wider than its grid. Two triggers: a slot tab opened during Starting, and a resumed mission after navigating away and back.
- Causes: (1) the two-phase mission spawn inserts the row at registration and forks seconds later with the grid hint baked into the `SpawnSpec`; a push from the measured element landed before the PTY existed, was persisted by `resize` and never applied, and the native terminal had already cached the pushed size so nothing re-pushed (`main`'s xterm hides the same drop via its activation re-fit dance). (2) Re-attach created the `Term` at the layout estimate and replayed the ring into it; codex runs on the alternate screen, which alacritty never reflows, so a wrong-width replay keeps its wrap damage forever (`runner-terminal` replay tests pin this). The resume path shares window (1).
- Fix: mission forks use the row's persisted size when newer than the hint and re-read it after `install_handle` (spawn and resume) — race-free because `resize` persists before it looks for the handle; new op `session_last_size`, and both the mission workspace and direct chats attach at the recorded PTY size. The resize-dance is deliberately not ported. Noted for M4.7: the focused block cursor paints over its glyph (xterm draws it inverted).

## 2026-08-21 — M4.6c keymap registry and command palette landed (`9cb159a`)

- The static binding table is replaced by `main`'s 15-entry registry (`keymap.rs`): scopes, nullable persisted overrides in `ui-settings.json`, effective-binding/conflict logic, immediate binding rebuilds, surface-scoped dispatch with no key literals. The Keyboard shortcuts pane carries `main`'s searchable rows, recording/conflict/unbind/restore/reset states, and the fixed New window row.
- The command palette is a persistent window-chrome entity over the whole shell: missions, direct chats, runners, crews (now app-store-owned), and Settings in `main`'s per-kind order with its exact substring-search composition; wrapped arrow navigation, Enter/Escape/outside click; opens from the sidebar search button and the registry action (blocked on the Settings route).
- Also: direct-chat and mission paused cards consolidated on `ui/session_overlay.rs` (subtitle as a definite-width flex container so wrapped lines push the actions; 1.3× layout regression test); grouped-pane header geometry corrected to `main`'s shape with explicit insets. Deviations recorded in the register: "Empty" copy; layout-dependent override key codes. Jason daily-drove it 2026-08-21.

## 2026-08-21 — M4.6d Agents, MCP, and Archived panes landed (`a0d46f4`)

- Mid-mission amendment (Jason): the three panes are their own entities under `surfaces/settings/` (`agents.rs`, `mcp.rs`, `archived.rs`), each owning its state and `Render`; `SettingsState` holds exactly three lazily created, retained handles; every pane→shell hop is deferred and no pane notify reaches the root (both reviewer hunts pass). The takeover shell and General/Appearance/Terminal/Shortcuts stay as `impl NativeRoot` — hoisting the shell and splitting Shortcuts is a follow-up for M4.6f or M6.
- Agents: `main`'s runtime rows (detected / override / not-found / checking / timeout / invalid) over `runtime_status_list`, Refresh, override set/clear through `BrowseField` (the generalized `WorkingDirField`, `pub type` alias kept), enable toggles and default agent reaching Start Chat live through the existing settings fields, `runtime/changed` refreshing rows. MCP: four client rows over `mcp_integration_status` / `mcp_set_integration`, snippets on `CopyValueButton`, Updating keeps the state tone. Archived: missions + chats merged by archive recency, title/cwd search, All | Missions | Chats, read-only open through a new `AppRoute::ArchivedChat` (no PTY attach), Restore, Delete all behind the shared confirm state in `ui/overlay.rs` (E12); per-row trash deferred as on `main`.
- Review must-fix: the archived chat's session key leaked into the live chat's side panel through a shared `CopyValueButton`; `render_chat_side_panel` now takes its detail and copy entity explicitly and the archived surface owns its own. Jason's mid-mission asks: "Browse" without the ellipsis on the override row (register), MCP warning text wraps instead of overflowing.
- Deferred, non-blocking: the double `runtime_status_list` probe per refresh and the duplicated "available && enabled" predicate (both want a backend change — M6); MCP binding-dir tooltip; locale-aware timestamps in Archived; Enter not blurring the override field; `BrowseField`'s default id `working-dir-browse`. Note for later: adding `.browse_focus()` to the Agents row would reintroduce a blur-commit hazard (Button's mouse-down focus steal would commit the draft before Browse runs).

## 2026-08-21 — Release channels and the cutover bridge recorded (plan decision 12)

- Prompted by "how does a nightly update itself": Sparkle keys on `CFBundleVersion`, so a bundle stamped with the constant `0.6.0-nightly` would never update — `CFBundleVersion` becomes a per-build stamp (`YYYYMMDD.HHMM`) on both channels permanently; the crate version stays put until the one `0.6.0` bump at cutover. Two traps closed in the plan: GitHub's `releases/latest` alias (which `main`'s Tauri updater polls) means nightlies must be prereleases on a rolling `nightly` feed, never the alias; and a pulse-style production `CFBundleVersion = 0.6.0` would compare older than every nightly stamp, killing the cross-channel ordering. Verified from Sparkle 2.x source: the comparator ranks `0.6.0-nightly` < `0.6.0`, and the validator needs EdDSA + Developer ID match, not a bundle-id match. GA `v0.6.0` is the single cutover release (Sparkle artifacts + Tauri bridge trio, the trio emitted until 0.7.0) via a tree-swap merge; no nightly cross-over item is needed because nightly users also hold the Tauri app. M4.6f's packaging sub-mission now points at plan §Release channels.

## 2026-08-21 — M4.7 terminal selection and copy landed (`e836222`)

- xterm-default selection on the GPUI terminal: pixel→cell hit-testing with `Side`, simple/semantic (xterm word separators)/line/block (⌥) selection, Shift-extend, drag autoscroll at xterm's 15-line/50 ms ramp with full-line endpoint snapping, selection quads in the palette colour, the block cursor inverting its glyph (wide cells included); X10/SGR press/release/button-motion reports through `write_user_bytes` when the TUI owns the mouse, Shift forcing local selection, a mode-aware I-beam/arrow cursor; ⌘C as a fixed `Terminal`-scoped registry entry plus Edit→Copy while text fields keep theirs; the #387 Opt+C swallow. Review must-fixes closed before smoke: non-interactive sessions never write mouse bytes; a right-click past a shrunk grid clamps instead of panicking. Folded in mid-mission: the M3.8 paste-file-paths carry (`terminal_paste.rs` — image → AppKit file URLs → text, `terminalPaste.ts` quoting, no Enter, works when gpui returns no clipboard item) and Shift+Enter emitting `ESC CR` as `main` does (Jason's daily drive). Deviations registered above (Shift bypass, 1003 button-motion subset, 1005 limit, paste precedence and framing). Three review rounds, clean; Jason daily-drove it.
- Open nits, none blocking: the fixed Copy row renders among rebindable rows in the Shortcuts pane; the column-select crosshair is not ported; `encode_key`'s doc comment and the Shift+Enter guard test are thin. Follow-up task worth filing: every ⌥+letter sends ESC plus the accented character (readline M-b/M-f broken) — same bug on `main`, fix is `key` over `key_char` when alt is set.

## 2026-08-21 — M6.1 promoted to real input-state tracking; M5 closed (plan decision 13)

- Jason: the byte-inferred draft detection (M3.4 latch, M3.6 backstop) is the most annoying daily defect, and the native line can observe input state instead of guessing — promoted to the first M6 mission; hook-based status moves to M6.2 and supplies `AwaitingHuman` later. M5 retired: `origin/main` HEAD is still `276a3a4`, the backend watermark, and parity was verified layer by layer as it landed; the deviation register above is the parity record. M6.3 gains the two archive-path UX items from today's archive audit (no process leak found).
- M4.8 defined in the plan as the exit-gate mission after M4.6f: the accumulated open nits (⌘-click URLs, ⌥+letter chords, fixed Copy row, M4.4/M4.6d cosmetics, Feed-tab fork estimate, crosshair) plus a row-by-row re-audit of the surface inventory, whose status column dates from M4 entry. Backend carries (M3.7 `to` validation, 32 KB cap, `app/woke`) filed under M6.3.

## 2026-08-21 — M4.6f Sparkle updater, packaging, nightly channel, and the last three panes landed (`8dc2843`)

- Phase 5 services pulled forward as decision 12 specifies: `updater.rs` wraps `SPUStandardUpdaterController` through objc2 behind an `updater` cargo feature (dev builds compile a no-op; `make clippy` checks both), retained for the process lifetime on the AppKit thread, persisted automatic-check applied after `startUpdater()`; `version.rs` composes the display version from `RUNNER_BUILD_STAMP` / `RUNNER_BUILD_SHA`; `logging.rs` installs the tracing file layer in the log dir plus a panic hook; `script/bundle-mac --channel nightly|production [--dev]` builds the stamped .app/DMG with the pinned Sparkle 2.9.5 framework, the two CLI sidecars beside the binary, inside-out signing and notarization (`tests/bundle_mac.rs` pins the plist per channel); `packaging/sparkle-public-key` carries the EdDSA public key; `.github/workflows/nightly.yml` (push to `gpui-nightly` ignoring `docs/**`, dispatch, `concurrency: nightly`) publishes to the rolling `nightly` prerelease with `--prerelease --latest=false`, refuses Tauri-named assets, and asserts after publishing that `releases/latest` still resolves to a `v*` tag. Panes as M4.6d entities: Updates (slim — version, Check, automatic-check toggle, last checked polled off Sparkle's `lastUpdateCheckDate`), Diagnostics (reveal logs), About (links through `open_url`).
- Mid-mission amendments by Jason, all in the register: Sparkle's standard sheet owns the update flow (no custom `SPUUserDriver`, state machine, or prompt card — five review passes, both hunts clean); shared `WorkspaceHeader` + mission-rail transition; `ModelField` capture-phase click fix (disabled fields stay inert); fixed 10,000-line scrollback.
- Still to prove after this landing, fix-forward: the first push is nightly #1 and a second build must arrive through Sparkle on Jason's machine (the native→native half of the updater proof). Carries, none blocking: the last-checked row refreshes only after the in-pane Check (the app-menu item and scheduled checks do not notify it); the workflow re-uploads all retained DMGs each run and its isolation step is a post-publish canary; dead `download.svg`; `WorkspaceHeader`'s unused `IntoElement` derive and the mission rail placeholder's missing `.relative()`; no render tests for the shared header; gpui-ce 0.3.3 logs a zero-size svg during transient layouts (`svg_renderer.rs`) — harmless, deliberately not patched or suppressed.

## 2026-08-21 — First nightlies shipped; updater proven; Jason switches

- `f0b9379`'s push built nightly #1 (`0.6.0-nightly.20260821.1003`, `sparkle:version` `20260821.1003`, signed + notarized on the first CI run; `verify` 17 min cold, bundle 10); a `workflow_dispatch` built `…1018`; Jason installed #1, took #2 through Settings ▸ Updates, relaunched on it. Both halves of the plan's updater criterion that exist today are met (the Tauri→native `bridge-test` remains cutover prep). Observed at the switch and registered: preferences do not migrate from the Tauri app (webview localStorage), so the first boot cleared the Tauri quit's resume claims. Cold-cache cost and the duplicated `verify` job go to M4.8 item 10.

## 2026-08-21 — M4.8 parity fix-ups and inventory re-audit landed (`bfc9eda`) — M4 complete

- The exit-gate mission, hosted by the nightly itself. Fix-ups: ⌘-hover/⌘-click links in terminals (OSC 8 plus `main`'s URL detection across soft wraps, hover state cached per cell/output revision and refreshed from frame-global move/modifier listeners so an idle terminal repaints); ⌥+letter chords (register); ⌥ column-select crosshair; fixed Shortcuts rows as `main` renders them; Agents Enter commit, MCP binding-dir tooltip, locale-aware Archived timestamps; Updates last-checked refresh after menu and scheduled checks; sidebar kebab hover/focus reveal and drag-handle tooltips; **chat-tab selection gated on the Chat route** (Jason's screenshot: the tab you would return to stayed lit on a mission — `main` derives the selected tab from the route; the active *project* staying lit is `main`'s behavior); **feed Markdown wraps** (Jason's screenshot: each line was a flex-wrap row of pieces measured at intrinsic width — now one `InteractiveText` block per paragraph with a narrow-width regression test); Feed-tab forks use the last measured slot size; M4.6f leftovers removed. `nightly.yml` builds in parallel with CI, publishes only after `ci.yaml` is green for the same sha, caches failed builds, uploads only the new DMG.
- Inventory re-audited row by row against `main`: 75 done / 0 partial / 0 missing, every row stamped with its landing milestone; reviewer independently confirmed the rows. Three review passes plus two scope additions mid-mission; clean. Known properties, not defects: the link-hover map has no eviction (bounded to one small entry per hovered line); ⌘ pressed over an *unfocused* pane with the pointer perfectly still underlines on the first pixel of movement; the nightly CI gate takes the newest `ci.yaml` run for the sha and a dispatch on a ref without one waits ten minutes before failing.

## 2026-08-21 — Terminal performance sweep (docs only; nothing started)

- Jason: window resize over a live slot is not as smooth as Zed's terminal. Traced: the resize owner's `prepaint` pushes every frame into a path that does a SQLite `UPDATE` plus a policy `SELECT` on the UI thread and then parks the PTY ioctl 175 ms behind a settle thread for claude/codex, so the TUI never repaints during the drag and snaps at the end; Zed ioctls every frame. Recorded as M6.6 (promoted to first M6 mission; the settle stays only for ring coherence). A read-only audit of the render/ingestion hot paths found larger items — every PTY chunk re-renders the whole window root, the full grid is re-walked and re-shaped every frame under the fair `Term` lock, per-cell allocations outside pure-ASCII runs, procedural glyphs re-tokenized per frame, ~23 scans and ~11 copies + a JSON round-trip per chunk, a lag-storm resync loop — recorded as M6.7 with file:line, absorbing two Tier 2 items. Run order written into the plan: M6.6 → M6.1 → M6.2 → M6.7.

## 2026-08-21 — Hotfix: nightly `…1306` crash loop; nightlies become dispatch-only

- Jason took the M4.8 nightly (`0.6.0-nightly.20260821.1306`) and it crashed four times in a minute: resume-on-launch restored the mission route, the pointer sat over the feed, and the first mouse move hit M4.8's feed-Markdown hover listeners, which called `window.current_view()` from inside the listener (`surfaces/mission_markdown.rs:368`, `:382`) — `Option::unwrap()` on the empty rendered-entity stack (`gpui-ce-0.3.3/src/window.rs:3454`), then `panic_cannot_unwind` inside `handle_view_event` and SIGABRT. Found from the new file log and panic hook in one read — the M4.6f logging paid for itself on day one. Fix: `render_markdown` takes the owning `EntityId` (`cx.entity_id()` at the two workspace call sites) and threads it down to the listeners, which `cx.notify(view)`. Standing rule recorded above. Done in this session rather than a mission: the app that hosts missions was the thing crashing.
- Process change (Jason): `nightly.yml` no longer triggers on push — `workflow_dispatch` only, so a landing is cut into a nightly deliberately (and never mid-mission by accident); the CI-green gate still resolves the dispatched sha's `ci.yaml` run. Decision 12 and §Release channels amended.

## 2026-08-21 — M6.6 terminal resize smoothness landed (`6f020d4`); Claude fullscreen renderer by default

- Contract (replaces M3.3 coalescing): every owner-frame measurement reaches the live PTY immediately, in per-session order, under the state lock so kill/resume cannot retarget it. The 175 ms settle is generation-scoped and does only one background `last_size` write plus, for full-repaint runtimes after a real width change — tracked per storm by `PendingResize.width_changed`, not by comparing endpoints — a ring purge sequenced before the rows-nudge repaint. `clears_on_resize` is cached at handle install; the latest size lives in manager memory so pre-PTY and mid-fork pushes beat stale rows. One captured Claude drag: 190 pushes → 190 immediate ioctls + one 2-ioctl nudge, persisted once, purged once; under M3.3 the same drag was 0 ioctls during and 1 after — the snap. Jason on the feature build mid-mission: "much smoother".
- Root cause of the history duplication (evening trace, Jason: Zed leaves Claude's history alone on resize): the settle's in-band `ESC[2J ESC[H` was broadcast to every live grid, and alacritty 0.26 implements `ESC[2J` on the primary screen as `Grid::clear_viewport` — the whole viewport scrolled into scrollback — so each settled width change filed the stale viewport as history and the TUI's repaint painted a second copy. `main`'s xterm.js `clear()` wiped history instead: a different artifact from the same byte. The clear is gone; zero production `ESC[2J` across the three crates (reviewer grep).
- Claude Code's alternate-screen renderer (`/tui fullscreen`, anthropics/claude-code#58906) is now Runner's default for claude-code spawns: one compact serde-built `--settings {"tui":"fullscreen"}` token pair through the shared argv composer (mission, direct, resume, reset), skipped only when the runner's own args already pass `--settings` — Runner owns the renderer for its spawns because `--settings` outranks the user file and a `/tui default` typed in any slot rewrites that file. Jason flipped his file to `default` to prove it end to end. M6.2's hooks join the same settings object.
- Review (codex coder, claude reviewer; two must-fixes, both test-pinned): `install_handle` fell back to the persisted row, which is now older than the caller's size whenever the settle was skipped (kill → drag dead → resume rolled the new PTY back to the pre-kill size); and the purge gate compared storm endpoints, so a drag out and back left every intermediate repaint in the ring for re-attach replay. Carries: unknown-session `resize` leaves unprunable state (test ids only); `debug!` resize counters compiled in, off at release `info` (`RUST_LOG=debug` turns them on without a rebuild).
- Smoke plan for the nightly: [../../tests/m6-6-resize-smoke.md](../../tests/m6-6-resize-smoke.md). Process first: the mission was hosted by the nightly app, Jason relaunched onto the feature build mid-mission (sessions resumed 14:16), and the crew's review ran inside the build under review.

## 2026-08-21 — M6.8 long-lived terminals landed (`3536c70`, merge `6cfae2d`); the output ring is gone

- Same night as M6.6: its nightly (`…1443`) showed a blank pane on tab switch until the next resize. Traced: `chat.rs` `ensure_owned_active_tab_attached` retained only the active tab's `TerminalSession`s, so a tab switch dropped the `Term` and the return rebuilt one from the backend's output ring; M6.6's purge-then-nudge had emptied the ring because Node and crossterm coalesce two SIGWINCHes microseconds apart into "no change" and never repaint. Jason: "do we still even need the ring buffer?" — no: its only reader in the tree was that re-attach, and Zed has nothing like it. Fixed the architecture rather than the nudge, as one mission (brief `briefs/m6-8-long-lived-terminals.md`).
- Contract: each alacritty `Term` lives in a process-local `TerminalBridge` registry keyed by live session id, created on `session/spawned` (emitted from `install_handle` before the forwarder starts; a first-output fallback stays as the ordering safety net) and fed **synchronously with raw bytes from the PTY ingestion thread** via `CoreSessionEvents::output` — no base64, no JSON, no lagging broadcast on the output path (one M6.7 finding retired). Panes borrow the terminal through viewer leases across tab, route and close/reopen; hidden terminals keep ingesting but wake GPUI only while a lease exists; attach never happens from render. Respawn replaces the `Term`; exit and archive release it — a real-PTY integration test runs 20 start/kill cycles and reads a live count of 0 after each. Deleted: the ring (`output_buffer`, 4096 chunks), snapshot replay and `Lagged` resync, the seq-0 mode prefix and its mode-state scans, the resume purge policy, `clears_on_resize` and the settle's purge + rows-nudge — the settle now persists `last_size` once and nothing else. 24 files, +910/−1977; backend tests 557 → 542, terminal 27.
- Deviation from `main`, Jason's call via the brief (reviewer D1): a stopped chat or mission pane shows the Ended/Resume card over a neutral terminal background, not the final screen — `main` kept the xterm canvas mounted under a transparent overlay (`RunnerChat.tsx:1160`). Revisit by releasing on archive instead of exit if it is missed. Folded in by Jason mid-mission ("Keep this. commit and merge."; reviewer S1 noted it is a delivery-semantics change riding with the ownership change): a `human_response`/`ask_human` delivery to a stopped or resuming slot now queues and flushes on `Respawned`, with exactly one `mission_warning` ("queued until the session resumes") per outbox and a drop warning on exit; stale typing pills clear on the `Unavailable` path. Also fixed: the mission pane no longer infers "Starting chat…" from `started_at ≤ 10 s` — `session/spawned` is the sole source of the Starting transition and preserves an in-flight Resuming.
- Review: two must-fixes on the first pass (exited panes rendered "Unable to attach chat"; a render-driven attach loop redrawing at frame rate with two SQLite queries per frame whenever a pane could not attach — the render hook is gone and `refresh_sessions` is out of the attach path), one on the second (the queued delivery was silent). Carries: `EventProxy::send_event` still wakes on Title/Bell/ColorRequest from hidden terminals (probably right — the tab strip shows titles); `apply_tab_rows` (`main.rs:675`) runs without a window and does not attach — no repro found, checklist item: two windows on one tab set, assign a chat from window B, watch window A; `panes.rs` `Ended => 0.45` opacity branch is dead. Memory: one `Term` with 10,000-line scrollback per live session, tens of MB when full, released on exit/archive. Smoke: `docs/tests/m6-6-resize-smoke.md` M6.8 addendum.
- Process: the crew opened PR #427 by habit after the clean review; Jason had it closed and landed locally (`--no-ff`), consistent with the PR-less landing rule. Third mission of the day hosted by the nightly app itself.

## 2026-08-22 — M6.5 `arch.md` rewritten for the native app; update-prompt card found missing (M6.9); CI grace-test flake hardened

- `docs/arch/arch.md` rewritten end to end (760 lines; the Tauri-era text is `git show 0e5ea18:docs/arch/arch.md`). What changed in substance: one-process runtime picture with the three layers (orchestration, runtime, presentation) and the two channels out of the core — raw terminal bytes synchronously into the `TerminalBridge` `Term`s, everything else as `AppEvent`s through the `native-app-events` thread; the tech-stack table (GPUI, alacritty_terminal, custom element, tracing, Sparkle, `script/bundle-mac`, native IME); §3.2 argv composition incl. the `--settings {"tui":"fullscreen"}` default; §3.6 `nodes` tree and the duplicate-chat placeholder for secondary windows; §3.7 native settings, `ui-settings.json`, no preference migration; §5 rewritten around the `Term` registry, immediate resize ioctls, the size resolution order, queued input workers, quit/orphan-sweep semantics, and the 750 ms / 500 ms idle detector as the fallback tier; §7.1.1 the no-`fsync` decision; §7.2/§8 the truth about messages — they **do** trigger a router action (the one-line inbox nudge) and every push goes through the per-slot delivery gate and outbox (draft-aware latch, 80 ms cooldown, 30 s reconciliation, queued-until-resume, never on replay); §9.5 MCP as the external control surface; §10 schema through migration 0020 with `nodes`, slot overrides, `last_cols/rows`, `resume_on_launch`, agent options, and the new filesystem layout; §11 the one-process thread model; bet 10 is now "one native process, alacritty as the model, GPUI paints, the terminal outlives its views", bet 13 "the core is UI-agnostic"; new §14 program state (branches, landing rule, channels, cutover pointer). Crews planning M6.1 onward read this file as ground truth — that was the point of doing it first.
- **Found while writing §3.7 (Jason's question): `main`'s in-app update prompt was never ported.** The native `Updater` wraps `SPUStandardUpdaterController` with only `check_for_updates` and the automatic-checks flag — no `SPUUpdaterDelegate`, so the app never learns an update was found; the dialogs Jason has seen are Sparkle's standard user driver. `main`'s Arc-style pill above the sidebar Settings row ("New Runner version available", hover → card, per-launch dismiss, auto-install checkbox — `src/components/UpdatePromptCard.tsx`) has no counterpart, and the M4.8 inventory (75/0/0) missed it. Recorded in the deviation register and as **M6.9** in the consolidation (S/M: objc2 delegate for `didFindValidUpdate` / `didNotFindUpdate` / `willInstallUpdateOnQuit`, an `available` state on the `Updater` entity, the pill + card in the sidebar, and the auto-install toggle wired to `automaticallyDownloadsUpdates`).
- **M6.8 nightly did not publish on the first dispatch.** Run 32501659053 built `…1611` but the CI gate refused: CI for `206b3b5` (docs-only on top of `6cfae2d`, which had passed six minutes earlier) failed on `router::tests::input_clear_flushes_after_quiet_500ms_grace` — a fixed 450 ms sleep racing the 500 ms `INPUT_CLEAR_FLUSH_GRACE`; the M4.5 CI scaling only covers `wait_until`. `main`-era test, pure flake. Hardened: sleep 250 ms and assert "still parked" only while measured elapsed time is inside the grace; the flush half keeps its own wait. Re-dispatched after the fix as run 32503087772 from `6a9c9bd`: CI green, nightly **`0.6.0-nightly.20260821.1628`** published — the build carrying M6.6 + M6.8 + the fullscreen default that Jason drives from 2026-08-22.

## 2026-08-22 — Sidebar overflows into the Settings footer (M6.10, recorded; Jason fixes it)

- Reported 01:34 with a screenshot: six projects in the sidebar, CHATS & MISSIONS header pushed to the bottom, its first row ("Codex") painted over the Settings footer row. Traced in `sidebar.rs`: the PROJECTS and CHATS & MISSIONS sections are `min_h_0 flex_1` inside the one `overflow_y_scroll` container, so inside the scroll viewport they are sized to the viewport rather than their content — `max_offset` is 0, the custom `Scrollbar::app` overlay hides, and the squeezed chats section's unclipped rows spill past the container and the footer. Two product shapes recorded in §M6.10 (one scrolling list, or two independently scrolling panes with a divider); either way the entity root gets an `overflow_hidden` clip. Standing rule added to the GPUI list: never `flex_1` a child inside a scroll container and expect the container to scroll.

## 2026-08-22 — Starting pill settles before Claude paints (M6.11, recorded)

- Jason, 01:50: the starting pill disappears, then the Claude session takes a few more seconds to appear. Cause: the pill settles on `chunk_indicates_tui_ready` — the first `?2004h`/`?1049h`/`?47h` — and the fullscreen renderer (our default since M6.6) writes `?1049h` at process start, long before its first frame; the default renderer's `?2004h` came with the first frame, so the old timing was a coincidence. Orca has no boot overlay at all and gets readiness from hooks. Fix recorded in §M6.11: a first-paint signal from the `Term` (first visible non-blank line) replaces the mode-escape signal for Starting/Resuming, backstops unchanged; M6.2's `SessionStart` hook supersedes it later. Check item: first-turn paste timing under the fullscreen renderer.

## 2026-08-22 — M6.10 sidebar overflow + M6.11 first-paint pill landed (`e3e6739`, PR #428 → `c50b907`)

- **M6.10, real cause corrected in review:** not the section min-heights. A GPUI `div()` is `Display::Block` until `.flex()` is called (`gpui/style.rs:734`), and both the `relative min_h_0 flex_1` frame around `#sidebar-node-scroll` and the scroll container itself were block boxes — a block child grows to its content height, so the scroll viewport measured 620 px tall with `max_offset` 0, the scrollbar hid, and rows painted past the footer. Under a block parent the sections' `flex_1` / `min_h_0` were inert, which is why the first negative control did not discriminate. Fix: `sidebar_scroll_frame` + `sidebar_scroll_container` helpers that set `.flex().flex_col()`, the section min-heights removed (now load-bearing), `overflow_hidden` on the entity root as the clip. Layout test on the production chain: long list viewport 160 / `max_offset` 460; block-frame regression 620 / 0; restored section `min_h_0` → `max_offset` 100 with a 0-height chats box; short list → chats box fills the column (100 px), so right-click "New chat" and drag-to-empty-space work over the whole list area as in `main` (`Sidebar.tsx:2085/2264/2316`). The 2026-08-22 standing rule is corrected accordingly (below). Carry: 16 of the other 18 `overflow_y_scroll` containers in the app crate are built without `.flex()` (reviewer's list: `ui/overlay.rs`, `ui/select.rs`, `ui/field.rs`, `ui/menu.rs`, `ui/model_field.rs`, `crews.rs`, `panes.rs`, `settings_page.rs`, `mission_workspace.rs` ×4, `runners.rs`) — only wrong where a child leans on `flex_1` or flex min-height; recorded as M6.12, a sweep. Note for whoever adds an in-tree popover to the sidebar: the root clip also shrinks hitboxes (GPUI intersects a hitbox with its content mask), so anything painted outside the root is invisible *and* unclickable — overlays stay on `NativeRoot`.
- **M6.11:** `first_paint_seq` on `TerminalSession`, child-scoped: after `parser.advance`, until the first paint, scan the visible grid for a non-whitespace cell and record the chunk's seq; stops forever after. `TerminalOutputActivity` exposes it; Starting and Resuming settle on first paint after the baseline, and the direct/mission spawn handlers reset an in-flight Resume's baseline to the replacement child's sequence space. Tests: alt-screen enter + clear, SGR, cursor-only and a line of spaces are not paint; the first printable cell is; seq stable after paint; respawn resets. `tui_ready_seq` has no production reader now and is retained with a comment. First turns go through positional argv (the verified-paste loop was retired earlier), so no paint race with the fullscreen renderer. Backstops (10 s timeout, output-idle) unchanged.
- Review: round 1 no must-fix (two should-fixes: dead `tui_ready_seq`, a replica-based layout test); round 2 found the block-frame cause; round 3 the block scroll container one level down; clean at 04:11. Landed via PR #428 (CI green, 3m14s) — the crew's PR habit again; the human merged. Process note: the crew's post-merge convention then parked this worktree on `main`; it was switched back to `gpui-nightly` on Jason's "yes", and four generated Tauri schema JSONs from the `main` checkout were moved to `/tmp/runner-gpui-main-schemas.XMqIwn`, not deleted. Nightly **`0.6.0-nightly.20260822.0427`** (run 32551720876 from `5b72acc`, CI green) carries M6.10 + M6.11.

## 2026-08-22 — M6.9 update hint + mission reset removed (`518a7bf`); design consolidated; M6.13 queued; bridge decision refined

- **Design first** (Jason's ask: "try the design in the pen file, then start a mission"): three variants of the hint at the right end of the Settings row (accent icon / icon + version pill / dot on the gear); Jason picked the accent icon. While there, the canvas was consolidated: `cmp/TrafficLights` is a component instanced by `cmp/SidebarC` and `cmp/SettingsNav`; the panel-collapse toggle moved from the footer into the titlebar band as shipped; all 17 sidebar slots are now `cmp/SidebarC` instances — five were byte-identical copies, twelve mission/direct-chat screens carried an older hand-built sidebar and now express their state (selected chat, mission row, deselected nav) as descendant overrides, their context-menu overlays re-seated under the selected row; the reset-confirm screen was deleted with the feature. `design/runner.pen` copied from the main worktree (1.77 → 1.48 MB).
- **M6.9** (mission `01M0M2W8DE7Q5XT278HF0AHWH8`, codex peer; brief [briefs/m6-9-update-hint.md](briefs/m6-9-update-hint.md)): a retained `MainThreadOnly` objc2 `SPUUpdaterDelegate` (`didFindValidUpdate` reading `displayVersionString`/`versionString`, `updaterDidNotFindUpdate`, `userDidMakeChoice` skip, `didAbortWithError`) feeds `Updater.available: Option<UpdateInfo>`; every Sparkle → GPUI hop is deferred through a detached `AsyncApp` foreground task (review must-fix: Sparkle can call delegates synchronously from inside `startUpdater`/`checkForUpdates:`, which run inside a live `App` borrow — an inline `cx.update` would panic on re-entry, and a post-teardown callback would `expect`), and the Rust → Sparkle entry points read the entity instead of taking an `update` lease. `apply_available_transition` is pure and returns whether state changed so the shell notifies only on change. The footer is two sibling hit targets (Settings `flex_1`, the hint a 32 px accent target with its own hover — Jason's live feedback), `circle-arrow-down.svg` registered, tooltip with the version, the Updates pane shows `Update available: v…`; `RUNNER_DEV_UPDATE_AVAILABLE=<ver>` seeds the state in debug builds. Added after the clean verdict (Jason): `checkForUpdatesInBackground` immediately after `startUpdater` when automatic checks are on — Sparkle's documented every-launch recipe (`SPUUpdater.h:119-121`), so a newer build on the channel's feed surfaces the icon on the next launch as well as on Sparkle's 24 h schedule; it is a no-op while a session is in progress.
- **Scope changed twice mid-mission** (both posted as human messages): first "restyle the amber reset dialog as a `cmp/ConfirmDialog` instance", then — after Jason asked what reset actually does (kill PTYs, archive the session rows, delete `events.ndjson`, respawn under the same id) — "remove it entirely". Removed: `MissionMenuAction::Reset` and the dialog/state/handlers, `mission_reset_impl*`, `reset_to_running`, `archive_all_for_mission`, the MCP tool and its registry entry; lifecycle doc comments rewritten. Tests: `reset_clears_archived_at` and `reset_to_running_clears_terminal_stamps` deleted (guarded only the removed transition); `mission_archive_step_is_atomic_against_reset_revival` split — its non-reset half survives as `archive_mission_step_stamps_and_deletes_node_atomically` (reviewer hunt 10: a deleted test partly guarding a live invariant; `archive_mission_step`'s constraints got their because-clauses back — reconciliation can still recreate a node between the stamp and the delete).
- Review: part 1 three must-fixes (re-entrancy above; a dead parameter; `Aborted ⇒ None` — kept as brief-mandated, registered), part 2 clean on the removal with the test-coverage record fix, final clean 07:30, launch-check addition clean 07:34; gates re-run independently by the reviewer (208 app / 540 backend tests, clippy `-D warnings`, fmt, `git diff --check`, reset-symbol audit empty). Jason approved a direct commit: `518a7bf` fast-forwarded onto `gpui-nightly` and pushed by the coder; CI green (run 32560067590). Docs, brief and design ride in the next commit.
- **Also this session**: M6.13 feed text fidelity written up and queued next ([brief](briefs/m6-13-feed-text.md)) — root cause of the "flat, compacted" feed is that the bundled variable Inter resolves every weight to Regular in gpui-ce's CoreText matching, plus Markdown rhythm drift and a non-recursing inline parser, plus no text selection; and plan decision 12 refined: the Tauri bridge tarball ships on `v0.6.0` only, every later 0.6.x production release re-uploads the 1 KB `latest.json` pointing at it (two-hop path for dormant 0.5.x installs), hard cutoff at `0.7.0`. Nightly **`0.6.0-nightly.20260822.0744`** (run 32560379906 from `434bf81`) carries M6.9.

## 2026-08-22 — M6.3 mutex item landed (`1b1d77a`, merged `ba33bd7`); M6.14/M6.15 specs; a codex-TUI lag that is not ours

- **M6.3, first bullet** (mission `01M0M7E7WCPPWSRYSF94ZWVEPG`, branch `fix/m6-3-session-lock`, [brief](briefs/m6-3-session-lock.md)): `synthesize_wake_busy` held the per-session mutex across the flocked `runner_status` append and its 8 × 5 ms retry sleeps, so `record_output` for that session parked behind every router wake under log contention. Now: clone the sink and an `activity_revision` under the lock, append unlocked, re-take and set `Busy` only if the revision is unchanged. The revision bumps on every `activity` write (forwarder, direct input, install, teardown); chosen over compare-and-set because a Busy→Idle→Busy during the append is an ABA the CAS misses — the reviewer patched a CAS in and watched the test fail. Prune-during-append cannot strand the Busy: a state only becomes prunable through a teardown that bumps the revision (commented at the site). Also: map scans clone `Arc`s before per-session locks, `prune_empty_session_state` is the one state→map nesting and the field comment states the rule, `inject_reserved` writes to the PTY unlocked. `record_output` under a contended wake: p50 41.5 ms → **7.7 µs** (p99 42.2 ms → 24 µs). Tests: contended-wake (fails on `434bf81`, 0/20 false passes in the reviewer's worktree), ABA, failure paths unchanged.
- **Accurate claim:** the router wake path no longer blocks ingestion; the session lock is not I/O-free. Deferred to the post-GA remainder: `install_handle` (resize + DB), `inject_direct_stdin` (PTY write), `inject_paste` (Enter), `resize`, `settle_pending_resize` (DB) — a concurrent resize/settle can still stall `record_output`. Brief corrected: the codebase's lock order is **gate → session**, not the reverse.
- Review: no must-fix; two doc should-fixes applied. No final "clean" was posted — both slots exited first (mission stopped 08:26); Jason called it done. Gate: local `make verify` exit 0; `1b1d77a`, `--no-ff` merge `ba33bd7`.
- **The "codex lag" was the host, and the host was us.** The coder pane felt sluggish all afternoon; Runner was cleared step by step (Metal HUD at 120 fps, no append contention in the mission log, same lag in Ghostty, an empty session lagged, 0.148.0 lagged the same — version reverted) before `uptime` said load **306** on 18 cores: the M6.3 *reviewer* had stress-tested the new tests "under 8× CPU oversubscription" with `NCPU*2` background `while :; do :; done` subshells per run and never killed them — **180 orphaned busy loops** (ppid 1) ran for three hours after its session ended. Killed; runnable threads 235 → 5, and the pane was fine. Lessons: check `uptime` and orphaned agent shells before any app theory; crew prompts get a "kill every background process you spawn before ending your turn" line; and the transcript-size theory (3.1 MB, 13 > 50 KB command-output cells) stays recorded only as a codex-side cost, not today's cause.
- **M6.14 / M6.15** registered from the specs (`docs/features/62-settings-file.md`, `63-tab-shortcut-hints.md`), after M6.12, before `v0.6.0`.

## 2026-08-22 — M6.13 feed text fidelity + M6.12 scroll-container audit + three M6.3 backend items landed (`5b974ed`, PR #429 → `7b12ced`)

- **Fonts (the app-wide root cause):** `Inter-Variable.ttf` is gone; eight Inter 4.1 static faces (Regular/Medium/SemiBold/Bold + italics) are registered together, so gpui-ce's `find_best_match` finally has real candidates — headings, `**bold**` and the sidebar brand render at their weights for the first time. A macOS test pins that Regular and SemiBold resolve to different faces and that every face reports family `Inter`; OFL header cleaned. `zed-font-kit` is a macOS dev-dependency for the test.
- **Markdown rhythm:** `block_margins` per block kind with collapsed margins (paragraph→paragraph 12 px, heading after paragraph 12 not 18, list items 4 px apart, h3 muted, `---`/`***` rules parsed); inline parsing recurses into marker bodies — links nest in bold, code stays literal, unbalanced markers stay literal. Layout test on `debug_bounds` pins 12/12/6 px; parser tests cover the screenshot cases (`**\`runner-app\` …**`).
- **Feed selection + ⌘C:** per-message `FeedSelection { event_id, anchor, head }` keyed by block index + byte offset, pointer mapped through `TextLayout::index_for_position`, highlights merged into the existing style runs only for the selected message in the terminal's selection colour, copy assembles rendered plain text (list markers as rendered, blocks joined by `\n`); `MissionFeed` key context so the Terminal-scoped ⌘C and text fields are untouched; Escape / click-elsewhere / tab switch clear; cleared when the event leaves the feed. Drag simulated in a `TestAppContext`. Out of scope as briefed: cross-message selection, word/paragraph double-click, edge autoscroll. Nit carried: the feed container and the workspace root both `track_focus(&self.root_focus)` — works because the feed node is a descendant registered last; a second feed view would make the context ambiguous.
- **M6.12 audit, no conversions:** all 16 `overflow_y_scroll` sites checked (the crew's table lived in the mission feed; re-checked by the human at merge): every block container has content-sized children, so `Display::Block` is correct there; the one lookalike, the Settings nav scroll, is a flex column filling a block frame via `size_full`. Converting popover/editor containers would have changed their sizing for nothing. Closed.
- **Backend (the three M6.3 items moved here):** `inbox_read` canonicalizes the ULID through `parse::<Ulid>()?.to_string()` before comparing (lowercase watermark test); `inject_direct_stdin` waits at most `DIRECT_INPUT_GATE_TIMEOUT` 5 s and returns a typed `Error::DirectInputTimeout`, with a cancelled-ticket set so a timed-out waiter can never pin the queue (cleared on reset/kill; test); crew rosters load in two queries via `repo::runner::list_for_crew` (traced query-count test, plus a missing-runner error instead of a silent drop). `rusqlite` gains the `trace` feature for the test.
- **Makefile:** `CARGO_BUILD_JOBS ?= 12`, exported — crew builds no longer starve the UI (load 208 seen 2026-08-22).
- Landed via PR #429 after the crew's clean review and the human's own `make verify` on `5b974ed` (547 backend, 28 terminal, clippy, fmt). Nightly **`0.6.0-nightly.20260822.1305`** (run 32574751856 from `b8771ea`, CI green).

## 2026-08-22 — License changed: MIT → GPL-3.0-only (Jason)

- Jason's call after weighing MIT ("a bit too broad for a solo dev"), GPL-3.0 and FSL-1.1-Apache-2.0: the goal is "it must stay open", not "nobody else may build on it commercially" — GPL lets anyone use Runner at work or sell it, but whoever distributes a modified Runner must publish the source under the same license; FSL would have been the choice for blocking competing products. Safe to do: every commit in the repo is Jason's (two identities), so no contributor consent is needed; the dependency graph is Apache-2.0 / MIT / CC0 throughout (`gpui-ce`, `alacritty_terminal`, `portable-pty`, `rusqlite`, `rmcp`, `notify`; the two copyleft-looking crates are dual-licensed with MIT/Apache options); bundled fonts are OFL (Inter) and Apache/MIT (MesloLGS NF). Releases already shipped under MIT stay MIT; `v0.6.0` and every nightly after `c8435b2` carry GPL-3.0-only. Mechanics: `LICENSE` is the verbatim GPL-3.0 text, `license = "GPL-3.0-only"` in `[workspace.package]` inherited by all five crates, README §License rewritten. `main`'s MIT `LICENSE` is replaced by the cutover tree swap.

## 2026-08-22 — M6.1 brief drafted; two premise corrections

- Brief: [briefs/m6-1-input-state.md](briefs/m6-1-input-state.md). Design: a per-session `InputTracker` on `TerminalSession` — keys trigger, the grid decides. A content key from Idle snapshots the visible rows and *probes*; the echo locates the composer row (the row that changed, shares its prompt prefix with its old content, and now carries the typed text past that prefix) and records the row's empty form; `Drafting` ends only when the grid shows the row back at an empty form (typed-then-deleted, Ctrl-U, Ctrl-C, post-submit redraw), `Submitted` re-captures the empty form because claude rotates its placeholder, an unanswered probe (menu, y/n prompt) times out unreported. IME marked text reports `Drafting` directly. The backend stores the observation above the byte latch, which stays as the fallback tier; no wall-clock rule at all (Jason, 2026-08-22 evening: a draft parks deliveries until the composer is observed empty; the pane's pill is the hint to submit or clear it — the `PendingInput` retry and 60 s backstop in the first draft were dropped with it). `AwaitingHuman` waits for M6.2.
- **Premise corrections found by the two read-only sweeps (HEAD `c8435b2`):** (1) the 10-minute abandonment backstop from impl 0041 was **never implemented** on `main` or here — `local_input_pending` is a bool, `reserve_delivery` returns `PendingInput` with no bound, the router parks with no retry and reconciliation skips pending sessions, so a typed-then-deleted draft parks a slot until Enter, Ctrl-C, respawn or exit; Backspace and Ctrl-U are `ActivityOnly` and never un-latch; the pill's "Clear input" sends a bare `\r`, i.e. submits the draft. §M6.1, `arch.md` §8.5 and the M3.6 entry above corrected. (2) The composer is not the cursor line: Ink parks the cursor at the bottom-left (`claude-session` fixture, `cursor=29,0`) and paints its own caret, so the plan's "read the composer row relative to the cursor" would read nothing — hence the echo-locate design. Also recorded: IME marked text never reaches the backend today (only commits do); the router learns input state through the in-process `StdinInjector` / `SessionDeliveryListener` seams, not events; the fixture corpus is output-only and the Phase 1 `record-fixture` tool went with the spike, so the brief adds an input-event record and an in-app recorder.

## 2026-08-22 — M6.1 observed input state landed (`9deca7c`)

- **`runner-terminal`:** `input_state.rs` — per-session `InputTracker` on `TerminalSession`; keys trigger, the grid decides. `classify_key` (`mappings.rs`) sorts keys into Content / Edit / Submit / Cancel / Navigate before encoding; `send_text` (IME commit, also `submit_text`) and `paste` feed Content; `set_composing` mirrors marked text; `write_user_bytes` stays raw for mouse reports and the `\x16` image marker (the chat image path now goes through it instead of `inject_stdin`). A content key from Idle snapshots the visible rows by grid index and probes; `locate_echo` picks the changed row whose old content is a prompt-shaped prefix of the new one and whose remainder starts with the typed text, ranking Exact > Decorated (stable right-side chrome such as codex's `<ret> send`) > Weak (collapsed pastes) and preferring the row that was empty before; Drafting exits only when that row reads as an empty form or the bare prefix; a row that no longer starts with the prefix relocates to the nearest prefixed row or reports `composer_visible: false`; Submitted keeps the submitted form so an Enter swallowed by a popup returns to Drafting while any other redraw (a rotated placeholder) reads Idle; Content while Submitted re-probes. Probes expire unreported after `ECHO_WINDOW` 500 ms. The tracker runs in `feed_output` under the `term` lock; transitions are reported after release, never per key or chunk. The fixture format gains `Input` events; `RUNNER_RECORD_INPUT_FIXTURE=<prefix>` records `<prefix>.<session>.ndjson` from the app at zero cost when unset; three replay fixtures (`input-claude-delete`, `input-codex-menu`, `input-zsh-submit`) pin the transition lists.
- **`runner-backend`:** `SessionState.observed_input` above the latch; `report_input_state` stores it (only with a live handle) and emits `InputCleared` on any non-Idle → Idle; `reserve_delivery` consults it first — Drafting parks (visible or not), Submitted is `RecentlyTyping` for the 2 s window, Idle falls through to `last_local_input_at`, `None` keeps the byte latch byte-for-byte (`local_input_byte_classes_remain_the_unobserved_fallback`). Enter while observed Idle stamps activity instead of clearing it. Cleared with the latch in `install_handle`, kill and exit reconcile.
- **`runner-app`:** pill copy "— a draft in the composer is holding delivery; submit or clear it", button "Submit draft" (sends Enter through `send_key`). No new status states, no new `AppEvent`.
- Review: the reviewer's three must-fixes (lowest-changed-row tiebreak losing to a transcript bullet; a hidden composer never relocating; Content while Submitted a no-op) are fixed and pinned by `transcript_repaint_cannot_steal_the_composer_echo`, `hidden_composer_stays_drafting_until_the_grid_restores_it` and `content_before_submit_redraw_rearms_drafting_without_an_idle_transition`. `make verify` green (551 backend, 188 app, terminal suites including the 10-fixture corpus and the input replays). Smoke plan: [docs/tests/m6-1-input-state-smoke.md](../../tests/m6-1-input-state-smoke.md) — human lane passed on the nightly (Jason, 2026-08-22).
- **Known edges, to watch in daily drive:** a draft whose first echo lags more than `ECHO_WINDOW` (a heavily streaming TUI) reads Idle for the rest of that draft — the prefix rule cannot re-find a composer that already holds text — and the 2 s window is the only guard until the composer is emptied and retyped; the brief's "must fail on `c8435b2`" test is satisfied by new fake-injector plumbing rather than by behaviour; the brief's µs/ms measurements live in the mission feed, not here.

## 2026-08-22 — M6.17 universal builds and M6.18 silent update checks added; M6 doc reorganized

- **M6.17** (Jason: Intel must be supported): the nightly is arm64-only — `script/bundle-mac:6` pins `aarch64-apple-darwin`, `nightly.yml` adds one target, and the planned bridge `latest.json` carried only `darwin-aarch64`, so an Intel 0.5.2 install would never have been offered 0.6.0. Decision: one universal DMG (a Sparkle item has one enclosure and no arch field; two DMGs would mean two feeds picked at runtime), Rosetta-tested on Apple Silicon since there is no Intel machine; pulse is arm64-only and not a template. The updater is unaffected — Sparkle compares `CFBundleVersion` and verifies signatures, never architecture. It is the next nightly.
- **M6.18** (Jason): the M6.9 delegate only observes, so Sparkle's standard driver still opens its alert on the launch and scheduled background checks; the fix is `updater:shouldProceedWithUpdate:updateCheck:error:` declining `SPUUpdateCheckUpdatesInBackground` after lighting the icon, the click path unchanged.
- `m6-consolidation.md` reorganized (Jason): pending missions on top, split before-`v0.6.0` / post-GA; a Landed section at the bottom holding every shipped mission with its commit; the run-order paragraph became a status block; the M6.3 session-lock bullet and the two S items that rode M6.12 moved under Landed; M6.9's header now says landed (`518a7bf`).
- **M6.14 dropped** (Jason, 2026-08-22, after checking the code): settings already are a plain JSON file (`app_settings.rs` → `ui-settings.json`, `serde_json`, read once at launch, written whole on change; runtime overrides in SQLite `_app_state`) and nobody edits it by hand, so the YAML move, the `~/.runner/` location and the watcher bought nothing. `docs/features/62-settings-file.md` deleted; recorded under Not doing in the M6 doc.
- Spec numbering: `main` holds `62-monokai-terminal-theme.md` (#406) while this branch had used 62 for the settings file, so the cutover tree swap would have deleted `main`'s spec. With M6.14 gone, `main`'s 62 is copied across verbatim and `docs/features/README.md` indexes 62 and 63 (neither had been indexed here).

## 2026-08-23 — M6.18 silent background update checks landed (`6b9bbe4`, merge `8b29dad`); RECENT and Archived nav

- **Updater (`runner-app/src/updater.rs`):** `UpdaterDelegate` implements `updater:shouldProceedWithUpdate:updateCheck:error:` — records `Found(version)` through the display-version-or-version helper now shared with `didFindValidUpdate`, returns YES for user-initiated (0) and informational (2) checks, and for background checks (1) writes an autoreleased NSError (domain `com.wycstudios.runner.updater`, code 1; `Retained::autorelease_ptr` into the `Option<&mut *mut NSError>` out-parameter — objc2's documented shape for this case) and returns NO, after which Sparkle neither shows nor downloads the item. `didAbortWithError` takes `&NSError` and ignores Runner's own domain so the hint stays lit; every other abort still clears it. Pure `should_proceed` / `transition_for_abort` carry the decision, with tests. Sparkle 2.9.5 call order, from source: `SPUBasicUpdateDriver.m:153-157` consults `shouldProceed` first and returns on NO, `didFindValidUpdate` follows at `:164-166` — so on a decline only `shouldProceed`'s `Found` lights the icon. Known and accepted: Sparkle logs each declined background check at error level (`SPUUpdater.m:794-797`; any code other than 1001/4007/4008) — Console noise only, and borrowing 1001 would change `SPUUIBasedUpdateDriver.m:461-470`'s branch for a future user-initiated decline; a resumable update left from an earlier session bypasses the gate by design (`SPUBasicUpdateDriver.m:96,149`, `resuming:YES`) and shows Sparkle's UI again, icon lit. Scheduling survives the decline (`SPUUpdater.m:868-892`) and no error alert is shown for it (`SPUScheduledUpdateDriver.m:106`). `objc2-foundation` lists `NSDictionary` and `NSError` explicitly instead of riding app-kit's implied features.
- **Copy/nav (Jason, mid-mission):** sidebar `CHATS & MISSIONS` → `RECENT`; the Settings nav's one-row ARCHIVED group is gone and the Archived pane is the last App-group row, labelled "Archived" in the nav and the page heading. The "Tabs & missions" label first asked for made the pane unreachable from Settings search — `filtered_nav_groups` matches pane labels only — so Jason chose "Archived"; the nav test now pins the App group's order and the "archived" query. Both in the register above. `docs/features/63-tab-shortcut-hints.md` still named the old section; fixed here.
- Crew codex-crew, mission `01M0N2BJHRJSS0HC3TWQADJC8A`. The reviewer verified against Sparkle and objc2 *source*; two must-fixes (the autorelease helper, the search hole) and one should-fix (Cargo features) were applied before the clean review. `make verify` green including the `--features updater` clippy. Human verification on the nightly: the icon lights on launch with no window; the click, the Updates pane, and the app menu open Sparkle's sheet — passed (Jason, 2026-08-23, on the universal nightly).

## 2026-08-23 — M6.17 brief; M6.19 and M6.20 registered

- **M6.17 brief:** [briefs/m6-17-universal-builds.md](briefs/m6-17-universal-builds.md) — both triples in `rust-toolchain.toml`, `bundle-mac` building each and `lipo -create`ing the three binaries with a both-slices assertion (and the same check on the embedded Sparkle binary), `-universal` names, the nightly workflow's second target, widened prune regex and a 90-minute timeout, the two README lines. Proof is a local `--dev` bundle with `lipo -archs`, never a launch (the human's nightly is running) and never a dispatch; the Rosetta launch is the human's check on the next nightly.
- **M6.19** (Jason): the Appearance pane's App font picker promises Geist / Roboto / System UI, but only Inter is bundled (`assets/fonts/`: Inter ×8, MesloLGS NF ×4) — the others resolve to system fonts through the fallback chain. Pin Inter, delete the picker, accept the legacy values on load. README corrected the same day: it had said Geist and Roboto were bundled.
- **M6.20** (Jason): the sidebar's drop indicator line stays painted after a release that lands on nothing; pointers and the likely cause (clear only on `on_drop`, no root mouse-up clear) in the M6 doc.

## 2026-08-23 — M6.17 universal builds landed (`59a8b87`, merge `c457cc0`)

- `script/bundle-mac` builds `aarch64-apple-darwin` and `x86_64-apple-darwin` (same `--locked`, `RUSTFLAGS`, `MACOSX_DEPLOYMENT_TARGET` 12.0), `lipo -create`s `runner-app`, `runner-agent-cli`, `runner-mcp` into the bundle, and `require_universal_binary` checks set membership of both slices on them and on the embedded `Sparkle.framework/Versions/B/Sparkle` before the build starts; artifacts are `-universal.dmg` / `.zip`. `nightly.yml` installs both targets, prunes across `-(arm64|universal).dmg` so the older arm64 items still count toward the ten, and has `timeout-minutes: 90`. `rust-toolchain.toml` lists both targets. README: "Apple Silicon and Intel (one universal build)".
- **Finding, fixed in the diff:** signing `runner-app` first failed on the lipo'd bundle ("code object is not signed at all / In subcomponent: runner-mcp") — `codesign` resolves the main executable to the enclosing bundle and seals its nested Mach-Os, and a lipo'd sidecar reads as unsigned because only its arm64 slice carries the linker's ad-hoc signature (`install`ed thin arm64 binaries had one, which is why the old order worked). Sidecars are signed before `runner-app`; the reviewer reproduced both halves with throwaway bundles and the comment states the cause.
- Proof, local `--dev` bundle: all three binaries and Sparkle `x86_64 arm64`; `codesign --verify --deep --strict` clean; arm64 63 s / x86_64 68 s warm (no cross-build issue); app 94 MB universal vs ~48 MB arm64 (+46 MB, the x86_64 slices). Deferred nice-to-haves: the redundant `runner-app` sign inside the loop, deriving the install paths from `TARGET_TRIPLES`, guarding Sparkle's helper binaries too (all five are universal today). Risk noted by the reviewer: 90 min is untested against a cold CI run that now builds twice and notarizes twice — if the first universal nightly times out, raise the timeout, not the script.
- Crew codex-crew, mission `01M0N5WG1AQD2GJF25J93XP5X6`; review clean; `make verify` green; no bundle launched, no workflow dispatched by the crew. First universal nightly `0.6.0-nightly.20260823.0341` built in 22 min cold (43 MB DMG vs 21 MB arm64; the feed kept nine arm64 items plus it); Jason took it through Sparkle and ran the Intel slice under Rosetta — passed (2026-08-23).

## 2026-08-23 — Cutover pulled forward (decision 14); Cutover 1 brief

- Jason: the product is ready to switch; the two-week soak and the throwaway bridge rehearsal protect almost nobody at ~15 lifetime downloads, so the remaining M6 items ride post-GA. What cannot be skipped is structural: this branch has no `release.yml` (tagging `v0.6.0` would fire nothing; `main`'s Tauri workflow dies in the swap), `ci.yaml` only triggers on `gpui-nightly`, and the 0.5.x installs poll `releases/latest/download/latest.json` so the first native release must carry the Tauri-format trio with both platform keys. The Tauri `Runner.app` is no longer installed on Jason's machine — only `Runner Nightly.app` — so the bridge is proven on cutover day with the real artifacts: install v0.5.2 from the releases page, launch, watch it hop.
- Brief: [briefs/cutover-1-release-workflow.md](briefs/cutover-1-release-workflow.md) — tag-driven and `workflow_dispatch` dry-run, universal production bundle via `bundle-mac --channel production`, per-release `appcast.xml`, `Runner.app.tar.gz` signed with the locked `@tauri-apps/cli@'@tauri-apps/cli@2.10.1` `signer sign`, `latest.json` with `darwin-aarch64` and `darwin-x86_64` both at the absolute `v0.6.0` tarball URL, all five assets to a **draft** (publishing stays human), `ci.yaml` gaining `main`, nightly's notes URL moving to `main`. Pulse's `release.yml` is the native reference; the nightly workflow's cert/Sparkle/CI-gate steps are the ones to copy.
