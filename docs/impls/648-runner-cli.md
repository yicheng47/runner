# General `runner` CLI — implementation plan

Plan for feature [648](../features/648-runner-cli.md) ([#648](https://github.com/yicheng47/runner/issues/648), milestone 0.11; with pi it makes 0.11.0). The spec carries the principles, the command reference, the tool table, the eight decisions and the six phases; this file is the mission sequence, what reading the code changed, and what has landed.

## Status (2026-09-19)

Spec and command set designed with Jason on 2026-09-18; the identity model (decision 7: a caller is the person at the app or a roster handle, never a location) and the release plan were decided the same day, and `peek` was dropped from the reserved verbs. The spec landed on `main` in `756a60f`. Later that day Jason decided that the MCP integration is removed in 0.11.0 itself rather than a release later (decision 8: two ways in at once would be confusing) and that TRAE gets a skills root so no runtime is cut off; the spec, this plan and the mission 1 brief were updated on the branch. Mission 1 ran the same day on `codex-crew` (`01M2SW0NRV9W8PTFWR673JZH8S`, brief [`briefs/648-m1-command-tree.md`](./briefs/648-m1-command-tree.md)): two review rounds to a clean verdict in 68 minutes, then a smoke of about 115 commands against the dev app sent four fixes back for a third clean round. `make verify` is green on the final tree (1416 tests). It lands as [PR #650](https://github.com/yicheng47/runner/pull/650). Jason's window checks of the app's own paths after the tool rename (the mission composer, an ask card click, Resume) are outstanding and wanted before the next nightly. Mission 2 ran the same evening on `codex-crew` (`01M2T4KVFBTP3MG9SMVJRG6433`, brief [`briefs/648-m2-agent-skill.md`](./briefs/648-m2-agent-skill.md)): two review rounds to a clean verdict plus one wording delta, `make verify` green (1439 tests), live smoke against `make run` passed, and it lands as [PR #652](https://github.com/yicheng47/runner/pull/652). A nightly cut from that merge carries Jason's four-runtime gate, which decides when mission 3 starts.

On 2026-09-19 Jason installed that nightly (`nightly.5b6030a.20260918.1333`). The release skill landed in `~/.claude/skills` and `~/.agents/skills`, `runner` answered in a Runner terminal with no install, and a Claude Code session drove the production app through the skill and the CLI alone. He then switched the Runner MCP entry off, which starts the gate. The same day reshaped mission 4: the `runner` command is installed by default where that needs no prompt, the explicit Install covers the rest, and the Runner skill's control moved out of the Skills pane to one switch beside the command in Settings → General (see "Decided on 2026-09-19"). The spec, this plan and the mission 4 brief ([`briefs/648-m4-command-install.md`](./briefs/648-m4-command-install.md)) were written on `feat/648-command-install`, and the Command line section was drawn in `design/specs/648-runner-cli.pen` the same afternoon, signed off, and mission 4 ran on `codex-crew` (`01M2W85MNTBMWMTBPXMZJQBQQN`): two review rounds to a clean verdict in 70 minutes, `make verify` green on the final tree (1459 tests), and it lands as [PR #654](https://github.com/yicheng47/runner/pull/654). Jason's smoke against `make run` ([`tests/648-install-smoke.md`](../tests/648-install-smoke.md)) is outstanding. It was the first mission launched, watched and steered through the `runner` CLI alone, with the Runner MCP entry switched off. Mission 3 ran on 2026-09-20 on the codex peer crew (`01M2YAW36SAP7B8QT8E01J9Q3Y`, brief [`briefs/648-m3-mcp-removal.md`](./briefs/648-m3-mcp-removal.md)), ahead of the gate's last legs by Jason's call, after #658 moved the CLI to `crates/runner-cli`: two review rounds to a clean verdict, `make verify` green (1459 tests), Jason's `make run` smoke passed, and it lands as [PR #664](https://github.com/yicheng47/runner/pull/664). The nightly cut from that merge proves the production removal and carries the three remaining gate legs.

## Missions

| Mission | Scope | Crew | State |
| --- | --- | --- | --- |
| 1 | Spec phase 1: a shared socket client for both binaries, the `runner <noun> <verb>` tree over every tool, references, `--json` / `-q` / tables, exit codes 0–3, the caller handle (`RUNNER_HANDLE` inside, `--as` outside), `mission_post` and `mission_signal` with `from` (a clean rename of the `_human_` tools, no aliases), `mission_resume`, `session_list`, runtime-only `session_start_direct`, the parity test, `status` as the health check with `status busy\|idle` removed, arch §9. | codex-crew | Done 2026-09-18, [PR #650](https://github.com/yicheng47/runner/pull/650); window checks outstanding |
| 2 | Spec phases 2 and 3: `mission feed --follow` with `--types` and `--from`, `runner help agents`, the embedded `runner` skill (template, three roots, `.runner-managed` marker, startup refresh, `runner-dev` for debug builds, installed once by default), and TRAE's skills root (`skills_dirs`, the Skills pane caption); plus the spawning app's sidecar on PATH for chats and terminals, `session show` with the agent status, and `session stop` and `session archive`. `session_status` and `inbox_read` are hidden from the feed views by default. Ends with Jason's smoke: the Runner MCP entry switched off, then Claude Code, Codex, pi and TRAE each find the skill and drive a mission through the CLI. | codex-crew | Done 2026-09-18, [PR #652](https://github.com/yicheng47/runner/pull/652); gate pending on the nightly |
| 3 | Spec phase 4: the upgrade step that unregisters the `runner` entry Runner wrote from every client, the end of `initialize_mcp_defaults`, the Runner row and config snippet out of Settings → MCP, and the `runner-mcp` bin target, `install_mcp_cli`, the stale sidecar and its packaging gone. From the nightly that carries it, this repo's own mission driving and the mission-watch skill run on the CLI. Brief: [`briefs/648-m3-mcp-removal.md`](./briefs/648-m3-mcp-removal.md). | codex peer | Done 2026-09-20, [PR #664](https://github.com/yicheng47/runner/pull/664), after Jason's `make run` smoke passed |
| 4 | Spec phase 5, design first. The `runner` command on PATH: installed by default where no prompt is needed (macOS `~/.local/bin` on the login PATH, else a writable `/usr/local/bin`; the user PATH on Windows), once, with a removal respected; otherwise an explicit Install that links `/usr/local/bin` through the administrator prompt, from a row in Settings → General and from the command palette; ownership and shadow detection; `runner-dev` for debug builds; the state in `runner status`. And the Runner skill as one switch beside it: both rows form a Command line section in Settings → General, the skill follows `runnerSkillEnabled` in place of mission 2's once-per-root record, and the Skills pane only badges the managed folder. The section is drawn in `design/specs/648-runner-cli.pen` (frames `TPSwV` and `dJlVA`), signed off by Jason on 2026-09-19. Brief: [`briefs/648-m4-command-install.md`](./briefs/648-m4-command-install.md). Can run while Jason does the gate, ahead of mission 3; one checkout, so never beside it. | codex-crew | Done 2026-09-19, [PR #654](https://github.com/yicheng47/runner/pull/654), merged as `6aeaeee` after Jason's `make run` smoke passed |
| 5 | Spec phase 6: vision §4.9, both READMEs, the mission-watch skill switched to `mission feed --follow --json`, Jason's smoke on macOS and JASONPC, archive the spec and this plan. One JASONPC session also covers pi's mission 3 checklist ([539](./539-pi-runtime.md)). | Jason + inline | Not started |

Arch §9 moves into mission 1 instead of the docs phase: the command tree is how the system works the moment mission 1 merges, and arch describes today.

## Decisions that bind

The spec's decisions 1–8 and its principles. The ones a crew is most likely to get wrong:

- **Inside a mission nothing changes.** `msg post`, `msg read`, `signal` and `ask` append to the event log directly, with no socket, no tokio runtime and no dependency on the app accepting connections. Only the control commands and the outside path use `mcp.sock`.
- **Identity is a handle or the person, never a location (decision 7).** Inside a mission the handle is `RUNNER_HANDLE`. Outside, `--as <handle>` becomes the tool's `from`, validated against the mission's roster by the tool, and no handle means `human`. `mission answer` is the person's verb and takes no `--as`. With a `from` handle the signal tool refuses the person's and the app's own types (`human_said`, `human_response`, `human_question`, `mission_goal`); without one it keeps today's whitelist.
- **A clean rename, no aliases.** `mission_post_human_message` and `mission_post_human_signal` become `mission_post` and `mission_signal`, as the `role_*` cutover did (#604). With decision 8 the CLI is the socket's only client.
- **The order inside 0.11.0 is fixed (decision 8).** Command tree, then the skill on all five runtimes and Jason's smoke of an agent driving a mission with no MCP entry, then the removal. `runner-mcp` keeps working until mission 3, because it is how missions 1 and 2 are launched and watched.
- **One binary.** The control commands live in the same `runner` the mission sessions already have on PATH. No second binary, no scoping by packaging.
- **No new dependencies without a reason in the handoff.** `clap`, `tokio` and `rmcp` are already in `crates/runner-cli/Cargo.toml`; tables are hand-rolled.

## What reading the code changed (2026-09-18)

- **The mission-wide Resume is UI code.** `mission_workspace/actions.rs::resume_open_mission` lists the mission's sessions and calls `ops::session::session_resume` for each stopped one. `mission_resume` is therefore a new backend op with the same loop, not a wrapper. The UI keeps its own loop in mission 1, because its per-session transitions drive the pane state; sharing the op is a follow-up.
- **`ops::session::session_list` already exists and is mission-scoped.** The new `session_list` tool lists direct chats from `repo::session::list_recent_direct`, so its op needs another name.
- **`mission_list` omits archived missions** and `MissionListArgs` has no include-archived flag, so prefix resolution for `mission unarchive` and for `mission show` on an archived mission needs a decision in mission 1.
- **The CLI crate has no lib target and does not depend on the backend,** and the tool registry is `pub(crate)`. The parity test needs a shared list of tool names (a constant in `runner-core` that the backend's registry test and the CLI's command map both assert against) rather than a cross-crate import.
- **`mission answer` takes the `human_question` event id.** `mission_status` already reports it as `pending_asks[].question_id`, which is the id the router matches a `human_response` on.
- **The sidecar is `<app data>/bin/runner`** (`cli_install::install_binary`), and a debug build of the CLI resolves the dev app's socket through `cfg!(debug_assertions)`. Crews never run the dev app, so a crew's live evidence is limited to the not-running path; the wire proof is Jason's smoke against `make run`.
- **Outside a mission with no `--mission`, a mission-scoped command is a usage error (exit 2),** replacing today's soft notice and exit 0. A general CLI that reports success after doing nothing is a trap for scripts. `Partial` env stays exit 2.

## Added to mission 2 (Jason, 2026-09-18)

Proposed after mission 1's smoke and taken into the mission 2 brief when Jason said to start it.

- **Chats get the spawning app's sidecar folder on PATH, as mission slots do.** Today a direct chat passes no `bundled_bin_dir` (`spawn.rs`, "off-bus"), so an agent in a dev-app chat finds Runner through the global skills and can load the production `runner` skill. With the folder on PATH and the skill preferring `runner` on PATH, a session always drives the app that spawned it. The socket stays a compile-time choice (`cfg!(debug_assertions)`); an environment override was considered and rejected because it would let a debug binary reach production.
- **`session show` with the agent status.** No tool returns a direct chat's `AgentStatus` (activity, source, outcome, detail), so a wrong chat status cannot be read from outside the window. A `session show` would have settled a status question on 2026-09-18 in one command.
- **A way to stop or archive a chat.** There is no socket tool for it, so a role with a CLI-started chat cannot be deleted from the CLI.

## Decided on 2026-09-18 after mission 2's smoke

- **The Runner skill is pinned at the top of the Skills pane,** one row per agent, mirroring the MCP pane's pinned Runner row. It replaces the spec's single skill toggle in Settings → General. Open design point for the frame: Codex, Copilot and pi share `~/.agents/skills`, so the pinned row installs or removes the skill for that root, while each agent's own visibility toggle hides it for one agent. **Superseded on 2026-09-19:** one switch in Settings → General, see below.
- **TRAE gets no skill toggle.** Installing the Runner skill into `~/.trae/skills` is enough. TRAE's only switch is `disable-model-invocation` in a skill's own frontmatter, a softer "never pick this by yourself".
- **pi could be toggleable and is not yet decided.** pi's `~/.pi/agent/settings.json` `skills` array supports exclusions, including `-path` to force-exclude an exact path, which the pi spec missed. Unproven: whether an exclusion removes a skill pi discovered on its own in `~/.agents/skills`. If wanted it is Skills pane work for its own issue, not part of 648.
- **The mission 3 gate runs on a nightly, not on `make run`,** because the dev skill fires only on dev wording. The `make run` checklist proves mechanics; the nightly proves the release skill's trigger on a plain request in Claude Code, Codex, pi and TRAE with the Runner MCP entry switched off.

## Decided on 2026-09-19 on the first 0.11 nightly

- **The command stays `runner`,** not `runner-cli`. Checked the registries: Homebrew has no formula or cask of that name and the well-known runners all carry a prefix (`gitlab-runner`, `gitea-runner`, `forgejo-runner`); crates.io `runner` (a Rust snippet runner) had 85 recent downloads, npm `runner` 781 a month and untouched since 2022, PyPI `Runner` is obscure. Product CLIs are named after the product, agents type the name constantly, and a collision degrades safely: a foreign `runner` is never replaced, the skill carries the absolute path, and Runner's own sessions put the app's sidecar first on PATH.
- **The command is installed by default where that needs no prompt,** under the skill's contract: once, recorded in settings, never over something Runner did not create, a removal respected. Jason asked for a setting, then whether it could be the default. macOS targets in order: `~/.local/bin` when it is on the login PATH (where `claude` and `traecli` already live on his machine), else `/usr/local/bin` when it is on the login PATH and writable without escalation. Windows: the sidecar directory on the user PATH. No other folder is a target: the other writable entries on his PATH are nvm's version folder, Homebrew's and Cargo's, which belong to other tools.
- **Where the default cannot act, the explicit Install does what VS Code and Zed do.** Neither detects a PATH: Zed's `install_cli` (read in `~/repos/gui/zed/crates/install_cli`) links a fixed `/usr/local/bin/zed`, trying a plain symlink and then `osascript … with administrator privileges`, and the `code` and `cursor` links on Jason's machine are root-owned in the same folder. `/usr/local/bin` is in `/etc/paths`, so one password puts the command on every Mac's PATH. Runner never edits a shell profile and never prompts at launch. The action lives in the Settings → General row and in the command palette.
- **The Runner skill is one switch in Settings → General, not a row in the Skills pane.** Jason, the same afternoon: a toggle per runtime is wrong, a single toggle is better, and it may not belong in Skills at all. It does not: every other row in that pane hides a skill from one agent, while this installs and removes folders, and the per-agent catalogs would have shown one shared `~/.agents/skills` switch three times. The skill is the agent half of the CLI, so it sits with the command in a Command line section, as Orca does (its General pane has an "Orca CLI" section with a "Shell command" row and an "Agent skills" row; read in `~/repos/gui/orca`). The switch is the truth: on installs for every detected agent regardless of its Settings → Agents switch, off removes all three folders and stays off, and while it is on a folder deleted by hand returns at the next launch. That replaces mission 2's once-per-root record, which respected a hand deletion; `runnerSkillEnabled` defaults on, so a settings file from the nightly keeps the skill. The Skills pane keeps the managed folder as an ordinary row with a "Managed by Runner" badge, so hiding it from one agent still works. This is close to the spec before the pinned-row decision of 2026-09-18, now with reasons.
- **A Windows debug build installs nothing** (my call, open to overrule): a directory on PATH cannot rename `runner.exe` to `runner-dev`, and a debug sidecar directory on PATH would shadow or be shadowed by the release one.
- **Detection alone decides where the skill goes, and the row has no status line.** Jason, reviewing the frame: listing the agents under the switch is redundant, and the skill should be installed for every runtime that is detected. So mission 4 drops the `is_agent_enabled` half of mission 2's eligibility. The Agents switch is about Runner launching an agent; the skill is about an agent in any terminal finding Runner; and the shared `~/.agents/skills` root already leaked past a switched-off Codex whenever pi was on. The description carries the rule ("Installs a skill for every detected agent…"), and a line appears only for a foreign folder or when no agent is detected. Until a build carries this, the nightly still needs TRAE switched on before TRAE's leg of the gate.
- **TRAE's root was skipped on the nightly because TRAE is switched off in Settings → Agents** (`disabledAgents: ["trae"]` in both apps). That is the specified rule, and switching TRAE on installs the skill at once, since `initialize_skill_defaults` reruns when the agent switches change. It also means the default install into `~/.trae/skills` has never run on this machine: the `runner-dev` folder there came from mission 2's stray test write and was only refreshed at startup. The pinned row's install path for TRAE gets its first real proof when Jason switches TRAE on for the gate.
- **Filed and moved:** [#653](https://github.com/yicheng47/runner/issues/653) (a live mission's flag draws in the accent; 0.11), and #630 left the 0.11 milestone for the backlog. `docs/roadmap.md` has not caught up with #630, #651 or #653.
- **Two CLI nits from driving the production app,** for whichever mission touches `output.rs` next: a stopped mission prints `STATUS running` because the mission model has no stopped value, where the curated view could say stopped when no session is live; and `mission stop` prints the crew id where `mission show` prints the crew name.

## Risks

- **The skill is the only way an agent finds Runner once MCP is gone.** MCP tools sit in an agent's tool list; a skill fires only when its description matches what the user said. Mission 2's smoke across four runtimes is the gate for mission 3, and the skill's description is tuned there, not after release.
- **The upgrade step edits three or four user config files.** It goes through the writer Settings → MCP already uses, touches only the `runner` entry of clients in `initialized_mcp_clients`, and completes once after final outcomes. It leaves a config it cannot read or parse untouched for that launch and retries on the next launch. A hand-written entry pointing at the deleted bridge will show as a failed server in that agent; the release notes say so.
- **Windows is unproven for the skill path.** The skill carries an absolute sidecar path with spaces and agents there run it under PowerShell or Git Bash; JASONPC proves it in mission 5, which is after the removal lands on `main` but before 0.11.0 is cut.
- **TRAE's skills root comes from its own manual** (`traecli doc skills`, 0.120.52: `~/.trae/skills/<name>/SKILL.md`), not from a live run; mission 2 proves a TRAE session loads the Runner skill.

## Log

- 2026-09-20 — mission 3 (`codex peer`, `01M2YAW36SAP7B8QT8E01J9Q3Y`, launched with `runner mission start --crew "codex peer" --project runner --goal-file …`, the goal a pointer to the brief): the removal. Round 1 found one must-fix that turned out not to be the coder's: `docs/product/vision.md` changed in the checkout after the handoff, and the coder refused to revert it under the repo's "do not revert user changes" rule and asked instead; it was Jason's own positioning draft from another session, so it stayed out of the diff and the commit was staged by path, never `git add -A`. The coder had briefly applied the driver's first, wrong revert instruction and restored the external diff itself; the reviewer checked the blob against its first-pass capture rather than take that on trust. The decision the round surfaced: the brief listed an unparseable config as a completion reason, so a config mid-rewrite at launch would have been marked done forever with an entry pointing at a bridge `boot_core` had already deleted; the driver chose the reviewer's Deferred variant, and the brief and spec were corrected in place. Also taken: an Inno Setup `[InstallDelete]` for the old `runner-mcp.exe`, restored Windows coverage for the bridge path in `matches_current`, `mcp_set_integration` renamed to `remove_runner_entry`, and the smoke checklist accepting either log wording. Round 2 was clean. Beyond the brief, the crew moved every MCP op onto the store's injected home, so the four agent config files became canaries in the startup guard; that is why `session/codex_trust.rs` changed. `make verify` green on the final tree (22 test binaries, 1459 passed). Jason's smoke against `make run`: the Claude Code and Codex entries, which point at the production bridge, left alone; the Copilot entry removed, because the dev app had registered it on 2026-09-16 while building #540 and production never overwrote it; the stale dev bridge gone; `mcpRegistrationsRemoved` recorded; a mission started, stopped and archived through `runner-dev`. Two observations for later: a deferred client is also retried on a runtime refresh within the session, not only at launch, and a permanently unparseable config keeps the step pending and re-logs once per refresh, the intended cost of the policy.
- 2026-09-20 — mission 3 started ahead of the gate. Jason switched the production app back to the first 0.11 nightly (`5b6030a`) and chose not to cut a fresh one; the gate's Claude Code and TRAE legs are unrun and Codex needs a build with #655, so all three run on the nightly that carries this mission. His call, on the ground that mission slots were never affected by the sandbox finding: they run Codex in bypass mode and write the event log without the socket. #658 landed first (`01953b7`, [PR #660](https://github.com/yicheng47/runner/pull/660)): the CLI now lives in `crates/runner-cli`, so the brief names `crates/runner-cli/src/mcp.rs` and `mcp_main.rs`. Brief written on `feat/648-mcp-removal` after reading `app_store/mcp_defaults.rs`, `ops/mcp.rs` (`catalog_at` seeds a `runner` entry and `McpCatalog` carries the integration status, so the pane change is a catalog change too), `settings/mcp.rs`, `cli_install.rs`, `bootstrap.rs`, the CLI crate and the four packaging scripts. One rule fixed in the brief that the spec left implicit: the step removes an entry only when its client is in `initialized_mcp_clients` and the entry's command is this app's own bridge path, so a dev and a production app never remove each other's registration and a hand edit is left alone; both READMEs and vision stay with mission 5.
- 2026-09-19 — the gate's first two legs, and exit code 5. On `nightly.5b6030a` in Ghostty, pi passed outright. Codex found the skill unprompted and read the guide, then reported that Runner was not running while it was: its default sandbox (`workspace-write`, network off) denies `connect()` to `mcp.sock` with `EPERM`, the socket client mapped every connect failure to not-running, and the skill's rule 2 then told the agent to ask Jason to open an app that was open. Proven from the rollout (the policy, and `pgrep` failing with the Seatbelt signature), with `codex sandbox -- <sidecar> status`, and with a probe that sees the socket file and gets `EPERM`. Orca avoids the case rather than solving it: it launches its agents in its yolo mode (`--dangerously-bypass-approvals-and-sandbox` for Codex) and its skill never mentions sandboxes. Runner's default Codex role launches chats sandboxed, so Runner's own chats would have hit it too. The fix, inline and before mission 3 because it gates it: a denied connection to a socket that exists is `ClientError::Blocked`, exit 5 (4 is 562's `wait`), with a message that does not claim Runner is running, since a stale socket file answers the same way; rule 2 of the skill and `help agents` tell the agent to run the command again outside the sandbox before telling the user anything. The Claude Code and TRAE legs are still unrun.
- 2026-09-19 — mission 4 (`codex-crew`, `01M2W85MNTBMWMTBPXMZJQBQQN`, launched with `runner mission start --crew codex-crew --project runner --goal-file …`): the Command line section. The goal carried every string from the Pencil frames, because a crew cannot open a `.pen` file, and the reviewer checked copy against the spec text. Round 1 found five must-fixes worth recording. `mission resume` returns `{mission_id, resumed_session_ids, sessions}`, not a mission row, so rendering it as a mission printed dashes, and the test had fed all three lifecycle tools the same fake shape; `mission stop` returns the row unchanged, so the new `stopped` display never fired in the one case it was for. `runner status` reported the command as shadowed in every Runner session, because Runner itself puts the sidecar folder, and a mission shim, first on PATH; a dev smoke cannot see this since the dev command has another name, so the CLI now skips PATH entries under the app data dir. On Windows the same check read the process PATH and would say installed after an uninstall; it reads `HKCU\Environment` now. The General pane called runtime detection, config parsing, a PATH walk and `access(2)` on every render, which is every keystroke in the settings search; the statuses are cached on the store and recomputed where the defaults run, after an action, and when Settings opens. The store could still reach the real `/usr/local/bin`, registry and `osascript` from a test, saved only by the fixture's PATH and by `debug_assertions`; the system folder and the choice of real or fake integration are now resolved in `main.rs` beside `home_dir` and injected, so a test gets none. And the brief's "waits for a finished discovery" test was missing. Round 2 was clean. My own gate: `make verify` green, 1459 passed, and a snapshot of `~/.local/bin`, `/usr/local/bin` and the three skills roots taken before and after the full suite was identical, as it was after the crew's runs. Left as notes, not fixes: a `mission_get` failure after a successful resume surfaces as an error; the palette labels say "runner" in a debug build, as the signed-off frames do; the one-time Windows default writes the registry and broadcasts on the UI thread. While the crew worked, the design housekeeping ran in the same checkout (shipped chat specs archived, the Windows files merged into `runner.pen`, settings and Windows components) and landed on `main` as `ced1111` through a throwaway index, so the crew's tree was never touched; two feed notes told the crew that `design/**` and three archived docs were Jason's, and both handoffs excluded them. `mission show` prints the project id where it should print the name, for the next pass over `output.rs`.
- 2026-09-19 — first 0.11 nightly installed (`nightly.5b6030a.20260918.1333`). The release skill was written to `~/.claude/skills/runner` and `~/.agents/skills/runner` at first launch with the production sidecar path; TRAE's root was skipped because TRAE is switched off. `runner` answered in a Runner terminal tab with no install and was absent from a plain shell, which is what started the install-by-default decision. A Claude Code session loaded the skill, fell back to the quoted absolute path (the space caused no trouble), read `help agents`, listed crews, missions and projects, started "CLI hello check" on `codex-crew` with the goal on stdin (`01M2W3E4MXKGWNSJTK7N0AM706`), followed hello, hello, "hello done" within 45 seconds and stopped it: no MCP tool was used. That session already knew the CLI, so it proves the mechanics and not the unprompted trigger; the four fresh sessions still do. Jason then switched the Runner MCP entry off. Pencil had no file open, so the mission 4 frames were not started.
- 2026-09-18 — mission 2 (`codex-crew`, `01M2T4KVFBTP3MG9SMVJRG6433`): spec phases 2 and 3 plus the three additions. Sixteen minutes in, a test wrote `runner-dev` skill folders into Jason's real `~/.claude/skills`, `~/.agents/skills` and `~/.trae/skills` with a dead temp sidecar path: a test core over a temporary app-data directory still resolved the roots from the real home. The fix is structural: `agent_skill` never looks up the home, `AppStore` takes an optional skill home, only `main` passes the real one, test builds force it to none, and a guard test runs the app's own startup path against a temp home with an untouched canary. Jason kept the folders (the marked-folder refresh repairs them on the next `make run`) and asked that the dev skill say it is for the dev environment, so the debug render's description leads with the development build and fires only on dev, development build, `make run` or `runner-dev`. The coder's turn then died on a Codex server error ("Selected model is at capacity") and the slot stayed Working for 34 minutes because Codex runs no `Stop` hook on an errored turn; a human message restarted it. Round 1 found four must-fixes: the debug render told agents to use a bare `runner` from PATH, which outside a dev session can be the installed app (it now names the absolute dev sidecar for every call); `session archive` on a Runner terminal killed the shell and then refused (it refuses first); `help agents` posted `--to lead`, an alias that does not exist; and the gate could not fire a dev-only skill on `make run` (moved to a nightly). The reviewer also launched the dev app by accident for two minutes through `target/debug/runner`, the GUI binary on this case-insensitive volume, and disclosed it at once; the CLI in the build tree is `target/debug/runner-agent-cli`. A last wording fix was the driver's own: the skill's fourth rule and the guide's example told outside agents to post with `--as`, which before #562's seats means speaking as a crew slot; outside a mission an agent acts for the user and its posts appear as the person. Found on the way and left unfiled: an armed Codex session whose turn ends in an error never returns to Idle.
- 2026-09-18 — mission 1 (`codex-crew`, `01M2SW0NRV9W8PTFWR673JZH8S`): spec phase 1 in one sitting. Round 1 found six must-fixes worth recording: direct chats export `RUNNER_HANDLE`, so the mode switch read every chat as a broken mission environment and exited 2 (the brief had repeated a stale comment in `env.rs` saying chats set nothing; handle-only is now outside a mission, and no handle is passed from it); `session restart` resolved through the new direct-chat list although restart accepts only mission slots (a full 26-character id now passes through); an empty value on `role update` and `crew update` sent null, which those inputs read as "keep" (they send an empty string now, `slot_update` keeps null); `session_start_direct` refused a role with a runtime override, a documented capability; `mission_resume` reported the app's own concurrent-resume skips as failures; and a not-running test depended on whether the dev app was open. A should-fix closed an identity hole: inside a mission, a prefix of the caller's own mission id went out over the socket and posted as the person. The smoke then found the default directory stored with a trailing `/.`, default output that printed multi-line prompts and one-line JSON blobs (each noun now has a curated view), a handshake sharing the 500 ms connect limit (now 3 s of its own), and archived missions resolving by full id for `show` but not for `msg post`. In-mission changes accepted as intended: an unknown signal type exits 2 instead of 1, and `--payload` must be a JSON object. The coder wrote its final handoff in its own terminal, not on the feed; the reviewer's verdicts are the record. Found on the way and left unfiled: an Enter written to an Idle baseline session flips the manager to Busy through `input-submit` without arming the PTY idle detector, so a silent agent leaves the row Working.
- 2026-09-18 — Jason: remove MCP in the same release, and TRAE needs skills too. Checked TRAE CLI 0.120.52 on this machine: its manual documents user-level skills at `~/.trae/skills/` (also `~/.coco/skills/` and `~/.trae-cn/skills/`), project-level `.trae/skills/` with `.agents/skills/` read as a compatibility path, and `disable-model-invocation` as the only per-skill switch. Spec gained decision 8 and a removal phase (now six phases); the tool rename lost its aliases; this plan went from four missions to five.
- 2026-09-18 — spec, identity decision and roadmap landed on `main` (`756a60f`) with the docs housekeeping. Plan and mission 1 brief written on `feat/648-runner-cli` after reading `cli/src/`, `mcp/server.rs`, `mcp/tools/{mission,session}.rs`, `ops/{mission,session}.rs` and the mission workspace's resume action.
