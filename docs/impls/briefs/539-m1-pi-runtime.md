# 539 — Mission 1: pi as a runtime, with the system-prompt channel

Jason requested a codex peer crew mission on 2026-09-18. Work in `/Users/jason/repos/yicheng47/runner` on branch `feat/539-pi-runtime`, cut from `main` right after the commit that carries the spec, plan, this brief and the design files. Build on it; do not rebase, squash or create another branch. Read `AGENTS.md`, `docs/features/539-pi-runtime.md` (its "What a new runtime touches" tables are this mission's checklist, its "Probe evidence" is the ground truth for every flag, path and file shape, and decisions 1–3, 6–9 and 11 bind), `docs/impls/539-pi-runtime.md`, and `docs/impls/briefs/540-m1-copilot-runtime.md` for the last runtime added, whose shape this mission follows. The design is `design/specs/539-pi-runtime.pen`; the spec transcribes it, so you do not need Pencil.

## Ownership and authorization

The coder owns implementation and checks; the reviewer waits for an explicit Runner handoff, then audits the whole working-tree diff against the branch tip. Iterate through Runner until no must-fix findings remain. No additional crew, nested subagents, new checkout or worktree. Stop at a clean working-tree review with a smoke checklist for Jason. Commits, push, PR, merge, and launching or restarting Runner are not authorized; leave the work uncommitted on the branch.

`pi` 0.85.1 is installed at `/Users/jason/.nvm/versions/node/v24.19.0/bin/pi`, with credentials for `openai-codex` (the configured default, `gpt-5.5`), DeepSeek and MiniMax. Probes that spend tokens are allowed but minimal, in print mode against a scratch session directory: `PI_SKIP_VERSION_CHECK=1 pi -p --session-dir <scratch> …`. Never write into `~/.pi` by hand and delete nothing under `~/.pi/agent/sessions`. Runner's own repository has `.agents/skills/`, so an interactive probe here meets the trust dialog: use `--approve` or print mode.

## Deliverable

Spec phase 1 end to end on macOS and Windows, without hook status: every phase-1 row of the spec's inventory tables. In detail:

- `Runtime::Pi`, wire name `pi`, in `model.rs`; `RUNTIME_DEFINITIONS` entry (display `pi`, command `pi`, `native_fork: true`, `skills_dirs: [".pi/agent/skills", ".agents/skills"]`), appended last; the catalog order test follows.
- `model_effort_args`: `--model <value>` verbatim; `--thinking <level>` lowercased, levels `off minimal low medium high xhigh max`.
- The system-prompt channel (decision 1). A split composer in `router/prompt.rs` returning `(system_prompt, first_turn)` per session kind: direct chat → the persona and no first turn; worker → today's `compose_worker_first_turn` body and no first turn; lead → today's launch prompt minus `== Mission ==`, with `== Mission ==` and the goal as the first turn. The existing composers are untouched, and a test that pins their output byte-identical for claude-code, codex, trae and copilot lands before the pi branches. `ops/mission.rs::start` and `ops/session.rs` (`session_start_direct`, `session_resume`, restart) write the body to `<app data>/session-prompts/<session id>.md` before every spawn, resume included, from the current role, crew and roster rows; the file is removed when the session ends and swept at startup; `ensure_first_turn_fits` applies to the first turn only. `system_prompt_args` → `--append-system-prompt <file>` on every pi spawn, and `trailing_runtime_args` stops suppressing it on resume for pi; `first_turn_argv` → `["--", body]` when there is a body, empty otherwise.
- Keys and fork (decisions 2, 3): `resume_plan` in Copilot's shape, a fresh spawn pre-assigns a UUID and passes `--session-id <uuid>` with `assigned_key` set so the key is persisted before the process starts, resume passes the same flag; `fork_plan` → `ForkPlan::Direct` with `--fork <source> --session-id <new uuid>`, the fork getting its own prompt file; `pi_conversation_exists(cwd, key)` globs `~/.pi/agent/sessions/<slug>/*_<key>.jsonl` with the slug from the spec; the conversation-missing check gains `(Pi, Some(key))` keeping the key; `sessions_root_for` → `None`.
- Permissions and trust (decisions 6, 7, 8): every permission function empty or Default for pi and `mission_bus_sandbox_args` empty; `--approve` beside `mission_bus_sandbox_args` in `spawn.rs` for mission slots only, never chats; `permission_modes` → `&[]` so the role form and slot overrides hide the control; `PI_SKIP_VERSION_CHECK=1` in the spawn env; the first-turn warning gate and the Windows batch fallback list gain `Pi`.
- `runtime_defaults.rs`: `~/.pi/agent/settings.json` → `defaultProvider` + `defaultModel` as `provider/model` (the model alone without a provider), `defaultThinkingLevel` → effort.
- `ops/runtime.rs` catalog: description `pi coding agent (bring your own model provider)`, `default_enabled: true` on both platforms, only the default model option statically, the seven thinking levels. `DISCOVERY_RUNTIMES` gains pi with a new `models/pi.rs` that runs `pi --offline --list-models` and parses its aligned table (key on the header row, first two columns → `provider/model`) in the shape of `models/codex.rs`; record in the handoff whether the list is credentialed providers only.
- Skills: the pane caption names the two roots; `supports_global_skill_toggle` stays false. No MCP: no `McpClientId`, no column.
- Identity (decision 11): `assets.rs` `PI` as one path in `viewBox -2 -2 28 28`, `M0 0h18v12h-6v6H6v6H0zM6 6v6h6V6zM18 12h6v12h-6z`, with a licence line for pi (MIT, earendil-works, traced from pi.dev/logo.svg) and an `ASSETS` row `pi.svg`; `chat_icon.rs` `Some(Runtime::Pi) => ("pi.svg", theme::text())`, the Codex treatment; every icon test table gains the row; `header_fork_state` treats pi as forkable.
- Every runtime-enumerating test and error string: `router/runtime.rs` loops and the permission matrix, `ops/role.rs`, `ops/slot.rs`, `mcp/tools/session.rs` doc comment, `runtime_status.rs`, `ops/runtime.rs`, `chat_icon.rs`, `command_palette.rs`, `settings/archived.rs`, `panes.rs`, `sidebar/tests.rs`, `roles/tests.rs`, `ui/select.rs`, `app_settings.rs`, `skills.rs`.
- Docs: `README.md` and `README.zh-CN.md` together (a pi column in Supported agents: no permission modes, hook status estimated until mission 2); `docs/arch/arch.md` enumerations at §1.1, §3, §5 and §5.10, pi's first-turn delivery in §5, the §6.2 table row and the §6.3 correction the spec names; `docs/arch/windows.md` with pi's Git Bash requirement; the `preferred_title` comment's system-prompt aside.

Out of this mission: the status extension, `HookStatusWatcher::Pi` and the rekey drop file (mission 2), the terminal fixture and the real-binary smoke test (mission 3), honouring `--session-dir` or `sessionDir`, the token ledger. `hooks_supported(Pi)` stays false. Do not pre-build any of them.

## Verification

`make verify` green, plus `cargo test --locked -p runner-backend -p runner-app --no-fail-fast --profile ci`, `cargo clippy --locked --workspace --all-targets --profile ci -- -D warnings`, `cargo fmt --all --check` and `git diff --check`. Cover the spec's phase-1 checklist: `runtime_list` includes `pi` with command `pi` and `native_fork: true`, and `Runtime::parse("pi")` round-trips; `deepseek/deepseek-v4-pro` with effort `High` → `--model deepseek/deepseek-v4-pro --thinking high`; a fresh lead spawn's argv carries `--session-id <uuid>` before the role's args, then `--append-system-prompt <file>`, then `-- == Mission == …` last, the key persisted before spawn, a goal starting with `@` or `-` delivered as text; a worker and a role chat carry the file and no body; the three file bodies equal today's composers' output as the spec's verification lists them, and the four other runtimes' bodies are byte-identical to before; resume carries the file and no body, recomposed from the current rows; a missing conversation keeps its key; the fork plan; `--approve` on a mission slot and never on a chat; every permission function empty; the settings defaults with and without a provider and without a thinking level; the `--list-models` parser on the recorded table and on garbage; the probe slug for `/Users/jason` and `C:\Users\x`; every icon table.

Prove the spawn end to end once with the real binary: a direct chat with a role opens at pi's prompt with the persona in force (a codename in the role's prompt is known without a first turn); a mission slot starts in Runner's repository without the trust dialog and the lead's goal arrives once; a relaunch resumes by id and sees a role edit made after the chat was created. Report what you saw, not what the tests imply.

## Handoff

Final Runner handoff: branch and base commit; what changed and where; checks with results; the exact manual smoke checklist for Jason (macOS now, JASONPC in mission 3: a role chat, a crew with a pi slot and a `runner msg` round trip, resume after relaunch, edit the role then relaunch, fork to a pane, the model picker from `--list-models`, the Agents row, the mark at 12 px in both themes); anything you could not prove; and the reviewer's explicit no-remaining-must-fix verdict. Leave the work uncommitted.
