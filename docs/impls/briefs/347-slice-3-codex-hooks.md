# 347 — Slice 3: Codex hook integration

Jason requested a Codex crew mission on 2026-09-14 to take over this phase after initial inline research and draft implementation. Work in `/Users/jason/repos/yicheng47/runner` on the existing branch `feat/347-codex-hooks`. Phase 2, PR #588, merged at `36dc88881e84119df63241e045f4870eb888a920`; Jason passed its smoke test. Keep the existing status vocabulary and presentation.

Read `AGENTS.md`, `docs/features/archive/347-hook-based-session-status.md`, and `docs/impls/347-hook-status/{README,plan,impl_log}.md`. Their Codex capability audit contains assumptions that the reference audit below supersedes; verify and correct them before treating them as implementation requirements.

## Ownership and authorization

The coder owns implementation and checks; the reviewer waits for an explicit Runner handoff, then audits the entire working-tree diff, including inherited draft changes. Iterate through Runner until no must-fix findings remain. No additional crew, nested subagents, new checkout, or worktree. The supervising session has stopped implementation. Existing dirty files are this task's draft and are explicitly handed to the crew; preserve unrelated changes if any appear.

Stop at a clean working-tree review and provide Jason a focused smoke checklist. Commits, push, PR, merge, branch deletion, and restarting the development app are not authorized. This mission brief is also uncommitted: automatic approval review rejected its preparation commit. Do not retry that commit. PR/merge permission for #588 applied to phase 2 only.

## Deliverable

Implement the Codex adapter through Runner's existing normalized `AgentObservation`, runtime event path, and status UI. Cover fresh launch/resume, genuine work, completion/continuation, native interruption, generation/session/turn ownership, child-event isolation, cleanup, and explicit bridge failure. Add human-wait coverage only where the actual CLI and correlated resolution evidence justify it. Preserve Claude behavior, all user hook/configuration/authentication/conversation settings, and the independent typed-draft protection. Only an unresolved human interaction may add a crew delivery hold; Working and Idle do not.

Use per-invocation additive hook configuration, with short bounded observer commands that consume stdin, fail open, and never approve, deny, block, or alter a turn. Codex supports `--enable hooks --dangerously-bypass-hook-trust -c hooks.<Event>=...`; hook timeout is in seconds. Do not persist hooks or trust entries, redirect normal sessions to a mirrored CODEX_HOME, or substitute an app-server UI for the PTY.

Native Windows transport, TRAE, title-spinner removal (slice 6), new UI states/layout, and broad transcript-derived failure classification remain outside this mission. Document Windows baseline-only explicitly. Assess question and approval limitations honestly instead of inventing timing guarantees. Update the program docs and a phase-3 smoke checklist with what is actually implemented and proven.

## Required reference audit

Jason explicitly selected both repositories as references. Read their implementations, not just these notes; neither is a specification for Runner's delivery semantics. They already exist locally; do not clone again.

- cmux: `/Users/jason/repos/harness/cmux`, inspected at `015991fbd90cef2ce16b46a58d6f1a265ef81998`. See `CLI/CMUXCLI+CodexFireAndForgetHooks.swift` (launch-time additive injection), `CLI/CMUXCLI+AgentHookCatalog.swift`, `CLI/FeedEventClassifier.swift` (Codex native approval attention), and `CLI/cmux.swift` around `readCodexTranscriptUserInput`, `codexUserInputFunctionCallCandidate`, `codexTranscriptTerminalTurnIds`, and `publishCodexMonitorUserInput`. Tests include `tests/test_codex_wrapper_hook_append.py` and `tests/test_codex_permission_prompt_notification.py`.
- Orca: `/Users/jason/repos/ai/orca`, inspected at `c1e15c40` (2026-09-07), origin `stablyai/orca`. See `src/main/codex/codex-hook-{definition,script,status}.ts`, `src/shared/agent-hook-listener/providers/codex-{events,state}.ts`, `src/shared/agent-question-answered-intent.ts`, `src/main/agent-hooks/server-codex-normalization.test.ts`, and interrupt inference in `src/main/agent-hooks/server/`. Orca uses managed hook installation/trust and immediately maps permission requests and `PreToolUse(request_user_input)` to waiting; its subagent rollup differs from Runner's parent-turn ownership.

The earlier spec says Codex publishes no question signal at all: that is wrong. Orca recognizes `PreToolUse(request_user_input)`; cmux additionally reads structured rollout records. Installed Codex 0.154.0 emits that PreToolUse even when Default mode rejects the tool, with no PostToolUse in the rejection probe. A hook alone therefore cannot prove a shown question. A real Plan-mode question's rollout contains `turn_context.collaboration_mode.mode=plan`, a `response_item/function_call` named `request_user_input`, and later a correlated `function_call_output`. Verify the TUI behavior, resolution/cancellation, and any nonblocking case before asserting delivery protection.

The earlier approval settle-interval proposal is also unproven. Codex `PermissionRequest` runs before other hook decisions/automatic review. cmux raises native prompt attention but does not turn it into its own blocking approval UI; it clears attention on tool completion. Orca maps the raw request directly to waiting. Neither proves that elapsed time means a human dialog is open. The inline draft currently defers Approval needed for Codex; this is an implementation proposal, not a decision Jason approved. Investigate and report the supported boundary; never make an arbitrary timeout the basis for a delivery guarantee.

## Inherited draft and evidence

Seven dirty files are handed over: `session/{claude_status,codex_status,hook_feed,pty_runtime,mod}.rs`, `session/manager/spawn.rs`, and `router/runtime.rs` under `crates/runner-backend/src/`. `codex_status.rs` and `hook_feed.rs` are new. The draft extracts the existing Claude feed/side-file reader for reuse, wires a Codex watcher, injects launch args/env, and sketches question tracking with Plan-mode rollout correlation. Treat it as unfinished and revise freely within scope; no Codex regression tests or final review exist yet. Review the helper reuse, parser semantics, same-invocation custom hook overrides/opt-out, stale events, question identity, and interruption ordering carefully.

Known immediate failure: latest `cargo test --locked -p runner-backend --profile ci --lib` does not compile because a Claude test still uses `File::create` after its import was moved (`claude_status.rs`, around line 558). The full log is `/tmp/runner-347-codex-tests.log`. Before the later draft edits, the first shared-feed extraction passed all 24 Claude status tests (`/tmp/runner-347-feed-tests.log`); that is not validation of the current tree. No build or fixture process remains running from the supervising session.

Local temporary fixtures used a localhost canned Responses server with an isolated temporary Codex home; no paid model calls and no user config writes. Scripts: `/tmp/runner-codex-hook-probe.py`, `/tmp/runner-codex-question-probe.py`, `/tmp/runner-codex-plan-probe.py`. Successful CLI tool probe `/tmp/runner-codex-hook-probe-5_i65fv5` captures SessionStart/UserPromptSubmit/PreToolUse/PostToolUse/Stop and verifies the user's UserPromptSubmit hook also ran once without changing hooks.json. `exec_command` is reported as Bash; asynchronous execution only yielded PostToolUse once completed. Default-mode question rejection is `/tmp/runner-codex-hook-probe-39kgscf6`. Plan-mode app-server question/answer and rollout evidence is `/tmp/runner-codex-hook-probe-byvc8ohv`; this app-server probe did not capture hooks, so it proves rollout shape only, not TUI hook behavior. Keep fixture prompts/rollouts out of normal logs and commit only minimal sanitized regression fixtures if needed.

Official hook reference: `https://learn.chatgpt.com/docs/hooks` (redirect from the official Codex hooks page); a local fetched copy is `/tmp/runner-codex-hooks-reference.md`. Installed binary is `/Users/jason/.local/bin/codex`, version 0.154.0. Check the actual CLI before copying reference timeouts or event assumptions.

## Verification and handoff

Add meaningful backend regressions for lifecycle/late events, native interrupt without successful-completion attention, resumed/stale/child isolation, config composition and explicit overrides, large/malformed/partial hook payloads, paths with spaces/quotes, fail-open commands, bridge teardown/fallback, and correlated questions including rejection, answer and cancellation. Prove only unresolved supported waits hold crew delivery and resolution releases it without overwriting typed drafts. Re-run Claude regression coverage after the shared transport change. Use backend tests, workspace Clippy and formatting; app tests if a shared consumer changes. Run socket/PTY tests with the needed local permissions and disclose environmental limits.

Final Runner handoff: branch, implemented behavior and unsupported cases, relevant checks/results, exact manual smoke checklist, and reviewer's explicit no-remaining-must-fix verdict. Leave implementation uncommitted for Jason to smoke-test.
