# 648 — Mission 5: the 0.11 wrap-up docs, and pi's smoke test

Spec phase 6 of [648](../../features/archive/648-runner-cli.md) plus the code half of [539](../../features/archive/539-pi-runtime.md)'s phase 3, started by Jason on 2026-09-20 after the first nightly carrying the MCP removal (`a61af39`) proved the production upgrade step, the default `runner` on PATH and the skill in all three roots. Work in `/Users/jason/repos/yicheng47/runner` on the existing branch `docs/648-m5-wrap-up`, cut from `main`; its first commit carries this brief and the plan update. Build on it; do not rebase, squash or create another branch.

Read first: this brief; the 648 spec (the "Docs" bullets under phase 6's inputs at the "arch §9", "vision §4.9", "`README.md` and `README.zh-CN.md`" lines, and Verification); `docs/product/vision.md` §4.9 as landed in [PR #664](https://github.com/yicheng47/runner/pull/664), which is the voice and the facts the READMEs follow; `docs/arch/arch.md` §9 for the command reference; `runner help agents` from the installed nightly (`~/.local/bin/runner help agents`, read-only) for the exact flow; `docs/impls/648-runner-cli.md` (the mission table, "Decided on 2026-09-19") and `docs/impls/539-pi-runtime.md`; `AGENTS.md` for the README rule (both files change together, `<!-- TODO zh-CN -->` marks a paragraph not yet translated); `crates/runner-app/tests/copilot_runtime_smoke.rs` as the shape the pi smoke test copies; `crates/runner-backend/src/router/runtime.rs` and `runtime_defaults.rs` for pi's argv, `PI_SKIP_VERSION_CHECK`, `--session-id`, `--append-system-prompt`, `--approve`, and the conversation probe; `docs/roadmap.md`.

## Ownership and authorization

The coder owns the changes and checks; the reviewer waits for an explicit Runner handoff, then audits the whole working-tree diff against the branch tip, reading both READMEs side by side. Iterate through Runner until no must-fix findings remain, and post the final handoff on the Runner feed. No additional crew, nested subagents, new checkout or worktree. Commits, push, PR, merge, and launching or restarting Runner are not authorized; leave the work uncommitted on the branch. `target/debug/runner` is the GUI app on this case-insensitive volume; the CLI is `target/debug/runner-agent-cli`.

Nothing in this mission touches Jason's machine outside the repo and temporary directories, with one exception below for the pi smoke test. Never write under `~/.pi`, `~/.claude`, `~/.agents`, `~/.trae`, `~/.local/bin` or the Runner app-data directories. Do not edit `docs/tests/`, the terminal fixture corpus, `docs/features/`, the plan files beyond what this brief names, or anything in the memory repo; those are the driver's.

## Deliverable

**Both READMEs, together.** Every mention of Runner's own MCP server as a way in goes, and a CLI section takes its place, in the same register as the sections around it and in the facts of vision §4.9:

- The feature list's "**MCP** — everything above is also a tool…" bullet becomes the CLI: everything above is a command too, for agents, scripts and people at a terminal.
- "Drive Runner from your agents" is rewritten around the CLI and the skill. What it says: `runner` is on PATH after the first launch (macOS `~/.local/bin`, or one click in Settings → General; the sidecar directory on the user PATH on Windows); agents need no setup because Runner installs a `runner` skill into each detected agent's skills root (three folders cover Claude Code, Codex, Copilot, pi and TRAE) and the skill points at `runner help agents`; the shape of a session, shown as one short shell example taken from `help agents`: list crews, start a mission with a goal, follow the feed, post to the lead, answer an ask, stop and archive; `--json` for agents, tables for people, and the exit codes. Inside a mission the same binary is what crew members use to message and signal, so keep the "Bundled `runner` CLI" bullet under "Also in the box" and make it say both halves.
- "MCP servers and skills, managed in one place" keeps its title and stays about the user's own servers and skills; drop "with Runner's own pinned first" from the screenshot alt text and any sentence that says Runner registers itself. The screenshot itself still shows the pinned row; leave the file alone and say so in the handoff, since retaking it on the nightly is Jason's.
- The Projects bullet says agents manage projects through the CLI, not "over MCP".
- The supported-agents matrix row "Runner registered as an MCP server" becomes "Runner skill installed" with ✓ in all five columns, and its footnotes stay consistent.
- Settings → General's Command line section (the `runner` command row and the Runner skill switch) gets a sentence wherever the README lists the Settings panes.
- `README.zh-CN.md` changes in the same places with the same facts. Translate what you can in the file's existing voice; a paragraph you cannot render faithfully is marked `<!-- TODO zh-CN -->` above the English, never silently dropped.

**`docs/roadmap.md`.** A fresh snapshot dated 2026-09-20 with the same shape: "Where the project is" (latest release still 0.10.0; landed on `main` since: the pi runtime and its hook status, the `runner` CLI missions 1 to 4, the MCP removal in #664 with its Windows test fix #665, the CLI crate moved to `crates/runner-cli` in #660; in flight: this wrap-up, the gate legs and the JASONPC session), the "0.11 in detail" table brought to the milestone as it is today (#539 and #648 P1 with their real states; #647, #651, #653, #658 now in the milestone with #658 closed; #617 and #592 unchanged; #630 moved to the backlog and out of the table), and one sentence after the table saying the `release-blocker` label marks the issues that gate the .0 while everything else in the milestone may ship in a patch. Keep the decisions section; update the MCP decision's wording to past tense now that it has landed.

**`crates/runner-app/tests/pi_runtime_smoke.rs`.** The pi twin of the Copilot smoke test, `#[ignore]` with a reason, gated on `RUNNER_PI_SMOKE_BINARY`: a direct chat with a role whose first turn must reply with an exact token, a relaunch through a new manager that resumes by the persisted session key and paints the old reply without replaying the first turn, then a mission slot under Bypass whose first turn replies with its own token. pi's home is isolated through `PI_CODING_AGENT_DIR` in the role's env pointing at a temporary directory, and `PI_SKIP_VERSION_CHECK` is what the runtime already sets. Credentials: when `RUNNER_PI_SMOKE_AUTH` names a file, the test copies it to `auth.json` inside that temporary agent directory before the first spawn; it never reads or writes the real `~/.pi`. The transcript assertions read pi's session files under the temporary directory (the runtime's conversation probe knows the layout). The exception to the machine rule: the coder may run this test once against the real `pi` on PATH with the real `~/.pi/agent/auth.json` named through that variable, to prove the test passes; record the run in the handoff with its duration and the model used, and use pi's cheapest model with thinking off.

**The 539 plan.** The mission 3 row gains the test file and notes that the fixture recording, the JASONPC pass and the archive stay with Jason and the driver.

Out of this mission: the smoke records under `docs/tests/`, `pi-first-turn.ndjson` and its snapshot, the gate rows, the mission-watch skill (memory repo), archiving the specs and plans, the README screenshot, and any code change outside the new test.

## Verification

`make verify` green, plus `cargo test --locked --workspace --no-fail-fast --profile ci` and `cargo clippy --locked --workspace --all-targets --profile ci -- -D warnings`, `cargo fmt --all --check` and `git diff --check`; the new test compiles on both platforms and is skipped by default. `grep -n -i "mcp" README.md README.zh-CN.md` shows only the Settings → MCP catalog section and its nav link. Every command in the README's shell example exists in `runner help agents` with those flags. Both READMEs have the same section list and the same matrix rows. `docs/roadmap.md` names no issue state that `gh issue view` contradicts.

## Handoff

Final Runner handoff, posted on the feed: branch and base commit; what changed and where; every place a README claim could not be verified against the running nightly or the code, and what you wrote instead; the `<!-- TODO zh-CN -->` marks left, if any; the pi smoke run (command, duration, model, result) or why it did not run; checks with results; and the reviewer's explicit no-remaining-must-fix verdict. Leave the work uncommitted.
